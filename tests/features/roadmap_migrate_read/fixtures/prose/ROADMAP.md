# Fixture — prose only

## Notes

This file carries no bullets at all. Zero items is the signal `empty_source`
turns on, whatever else the file holds.

| Key | Value |
|---|---|
| a | b |

Trailing prose after the first element, which is a narration element.

```text
A fence at section level with no preceding item is narration too.
```
