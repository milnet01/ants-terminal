# Corpus fixture — unchanged_value

**Status:** shipped 2026-08-02.
**Kind:** doc.

## 1. Body

A single-line Status inside the header block: both the old per-line matcher
and the shared rule read the same value, so this document does not move.
