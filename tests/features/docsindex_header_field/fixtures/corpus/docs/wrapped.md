# Corpus fixture — truncated_now_whole

**Status:** accepted 2026-08-02 — the value continues onto a second
physical line, and onto a third, because the corpus hard-wraps at about
eighty columns.
**Kind:** doc.

## 1. Body

The class this change exists for: the old matcher keeps only the first
physical line, the shared rule joins all three.
