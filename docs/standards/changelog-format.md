<!-- ants-changelog-format: mirror -->
# Changelog Format

> **MIRROR — do not edit this file.** The owner is
> `~/.claude/standards/changelog-format.md`; everything between the `MIRROR BEGIN` and
> `MIRROR END` markers below is a verbatim copy of it, kept in the repo
> because this repo is public and a reader who has only the checkout cannot
> follow a pointer into a private home directory.
>
> **A correction goes upstream, then gets re-copied here.** Editing this copy
> makes it a second standard. Refresh with
> `tools/check-standard-mirrors.sh --write`, which re-copies the owner into
> the marked region and leaves this header alone; `tools/hooks/pre-commit`
> refuses a commit whose mirror has drifted from its owner (ANTS-4133).
>
> **Why this file is mirrored (ANTS-4761).** A link from a mirrored half to an
> un-mirrored sibling dead-ends for exactly the reader the mirrors exist to
> serve. The rule that settles which files are carried is in
> [`README.md`](README.md) — every top-level global standard a mirrored half
> links to.

---

<!-- MIRROR BEGIN ~/.claude/standards/changelog-format.md -->
# CHANGELOG.md format spec (v1.1)

> Detailed format spec for `CHANGELOG.md` — the Keep-a-Changelog structure,
> its conventions, and the release flow that moves entries out of
> `[Unreleased]` while the roadmap's bullets flip.
>
> Read this file when authoring a `CHANGELOG.md` entry, cutting a release,
> or building tooling that consumes the format. For `ROADMAP.md` itself see
> [`roadmap-format.md`](roadmap-format.md); the two are cross-referenced
> because a release touches both.
>
> **Split out of `roadmap-format.md` on 2026-08-12 (CFG-0036)**, after three
> cold loops hit the cap on a document too large for one cold read. **Section
> numbering is deliberately unchanged** — § 4.1–4.3 keep their numbers, so
> every existing `§ 4.3` citation still resolves and only the filename moved.
> The `ants-roadmap-format-spec` pin stays in `roadmap-format.md`, which is
> where `skills/_shared/roadmap-fold-in.md` reads it.

## Contents

- [4. CHANGELOG.md format spec](#4-changelogmd-format-spec)
  - [4.1 Structure](#41-structure)
  - [4.2 Conventions](#42-conventions)
  - [4.3 Release flow with ROADMAP integration](#43-release-flow-with-roadmap-integration)
- [What checks this](#what-checks-this)
- [Cold-eyes loop log](#cold-eyes-loop-log)

## 4. CHANGELOG.md format spec

A conforming project keeps a Keep-a-Changelog-style
`CHANGELOG.md` at the repo root. The format is defined at
<https://keepachangelog.com/> and pinned here as a sub-spec.

### 4.1 Structure

```markdown
# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- New feature.

### Fixed
- Bug fix.

## [X.Y.Z] - YYYY-MM-DD

**Theme:** one-line summary of the release.

### Added
…

### Changed
…

### Deprecated
…

### Removed
…

### Fixed
…

### Security
…

## [X.Y.Z-1] - YYYY-MM-DD
…
```

**A dated release heading takes an ASCII hyphen, not an em dash** —
`## [X.Y.Z] - YYYY-MM-DD`. This repo's prose uses em dashes everywhere else, so
the hyphen looks like a slip. It is not. `changelog_log op:"release"` writes it,
and Keep a Changelog uses it. **Do not "correct" it back.**

**Published em-dash sections conform. Do not rewrite them.** This standard
prescribed the em dash until 2026-08-20 (CFG-0152). The rule binds headings
written from that date.

**Only the writer is bound. A consumer matches either dash and lets the date
decide** — as `cut-release` Phase 0e does.

**It does not reach the feature-grouped `### <date> <Category> — <headline>`
subsection.** That em dash is what its writer emits.

**The flat category structure in the fence above is the only `[Unreleased]`
shape this standard permits.** The writer verb also
supports a *feature-grouped* `[Unreleased]` — `###` subsections that are dated
topics rather than categories, written by `changelog_log op:"add_subsection"`
— and **a project under this foundation does not use it.** Ruled out
2026-08-12 (CFG-0073) rather than specified: one shape is what lets a reader,
a parser and § What checks this all bind to the same structure.

**So: do not call `op:"add_subsection"`.** It writes the shape this standard
does not permit.

**A `[Unreleased]` that is already feature-grouped is converted by moving its
topics out and then writing flat.** Not by cutting a release: executed
2026-08-20, `op:"release"` carries the dated-topic headings into the dated
section verbatim, so the shape is relocated rather than removed — and § 4.2
binds every dated section too. Do not hand-write a flat block beside a dated
topic either: the verb's own refusal says a mixed section locks the file, and
executed 2026-08-20, `op:"add"` against one succeeds — so nothing stops the mix
and nothing reports it.

**This is a scope decision, not a judgement about the layout.** Executed
2026-08-20: `op:"add_subsection"` refuses `flat_section` against a flat
`[Unreleased]`, and `op:"add"` refuses `feature_grouped_section` against a
dated-topic one, naming the shape that section uses. **Both refusals need
existing `###` headings to fire, so an EMPTY `[Unreleased]` is unguarded** —
and § 4.3 step 2 leaves one empty after every release. One call there still
converts the project silently, so the exclusion rests on the author rather
than on the verb (CFG-0172).

**Re-decided 2026-08-20 by the user, and kept.** The verb's guard becoming
symmetric was this rule's own trigger to revisit it, and it was revisited. Two
reasons to keep it, and neither is the one it was written under: the one-shape
argument above does not depend on the tooling at all, and the empty-section
hole is a live author-side reason. Lifting it would first have to answer what a
release does to a dated-topic `[Unreleased]` (CFG-0073), which nobody has
defined. **The trigger is spent** — do not reopen this on the strength of the
guard alone.

### 4.2 Conventions

- `[Unreleased]` block at the top, **always** — even if empty.
  The ROADMAP viewer reads it for current-work signaling per
  `roadmap-format.md` § 3.6.2.
- Dated sections in **reverse chronological order**.
- `**Theme:**` line is one sentence; sets the release's
  character.
- Bullets categorical: Added / Changed / Deprecated / Removed / Fixed /
  Security. Don't invent new categories. **That is also the heading order, in
  `[Unreleased]` and in every dated section.**
- Bullets terse — **one line, plus at most one indented continuation.**
  Anything longer goes in the commit. **This one is entirely on the
  author**: `changelog_log`'s `body` takes up to 4000 characters and
  writes as many continuation lines as it is given, and
  `add_from_roadmap` reuses the roadmap bullet's `Layman:` line by
  default. Neither enforces a limit, so "I wrote it through the verb" is
  not evidence of conformance.
- **Cite ROADMAP IDs** in bullets when applicable: `- live search
  filter (ANTS-1042).` under `### Added`. The category is the heading,
  never repeated in the bullet. The bidirectional link helps readers
  move between the changelog and the roadmap.

### 4.3 Release flow with ROADMAP integration

When a release ships:

1. `[Unreleased]` block contents move to a new dated section
   `## [X.Y.Z] - YYYY-MM-DD`, **and its `**Theme:**` line is written.** The
   theme is not carried over — § 4.1's fence puts it in every dated section and
   in no `[Unreleased]` block, so there is nothing to move, and
   `changelog_log op:"release"` does not author one either. Somebody writes it
   at this step or the section ships without it.
2. Empty `[Unreleased]` section is left at the top (with an
   empty-state hint or just the heading).
3. ROADMAP bullets that were 🚧 flip to ✅.
4. Released ROADMAP block changes from `(target: YYYY-MM)` to
   `shipped (YYYY-MM-DD)`.

**All four steps are the author's on a file-backed project** — see the
store-backed carve-out below for steps 3 and 4. **Except where the bump recipe
calls `changelog_log op:"release"`: that verb closes `[Unreleased]` into the
dated section and opens a fresh empty one above it, so it performs steps 1 and
2.** It authors no `**Theme:**` line, so step 1's second half stays the
author's on every route. `cut-release` carves out the same split at Phase 0e —
*"Unless the recipe performs the cut itself — then 0e passes."*

**Absent that recipe step, `cut-release` performs none of them, and checks
two.** Its Phase 0e stops unless a **dated** section for the new version
already exists with content, and its Phase 0f stops on any ROADMAP ID the
section claims shipped that is not ✅. Neither check is an edit, and on no
route does the skill draft a section or flip a bullet: where `[Unreleased]`
moves inside a run, the recipe's verb moves it.

**Until 2026-08-13 the predecessor did neither.** `/release` never read or wrote
a roadmap at all — a `grep -c 'roadmap'` over its `SKILL.md` returned 0, checked
2026-08-12, the file since deleted — and its step 2 would dispatch `changelog-writer` to draft a missing
section from the commit range, which § 7 of `releases.md` names an anti-pattern.
`cut-release` dropped that dispatch and claimed step 3 as a check; charter 17
required one or the other. The first run found the case immediately: a 0.7.105
changelog bullet claiming ANTS-4065 shipped, against a roadmap still holding it
🚧 (`~/.claude/docs/cut-release-first-run-2026-08-13.md`).

**On a store-backed project** (`roadmap-format.md` § 3.12), steps 1–2 are unchanged —
`CHANGELOG.md` is not generated. **Step 3** is a roadmap edit and goes
through the store's write verb, which mutates the store and re-renders;
editing the rendered file by hand loses the change at the next render.
**Step 4 is not yet performable** — `roadmap-format.md` § 3.12 owns that and says why.

## What checks this

**Almost nothing, and that is the honest summary.** `roadmap-format.md`'s
half of the original document is checked hard because a parser binds to it.
This half is checked mostly at write time, inside `changelog_log`, or not at
all.

**Read the rows; this section does not summarise them.** Every gloss written
here went stale when the next row landed. `documentation.md` § 2.8 item 3.

| Rule | What catches a breach |
|------|----------------------|
| § 4.1 the dated heading's ASCII hyphen | **Almost nothing.** `cut-release` Phase 0e matches either dash on purpose. A bump recipe's `post_check` may grep `^## \[$V\] - `, but `post_check` is optional, so a project without one catches nothing |
| § 4.1 structure | Partial: `changelog_log`'s `add` / `add_from_roadmap` / `add_batch` write the flat shape. **Nothing** validates a hand-edited changelog at rest — `format_mismatch` fires on a *YAML* changelog and `no_changelog` on a missing one, neither on a malformed Markdown one |
| § 4.1 nobody calls `op:"add_subsection"` | **Partial, and the gap is the state this standard leaves after every release.** The op refuses `flat_section` against a flat `[Unreleased]`, and `op:"add"` refuses `feature_grouped_section` against a dated-topic one. Both need existing `###` headings, so against an **empty** `[Unreleased]` the op writes and **nothing** catches it. A hand-written dated topic is caught by nothing either. Exercised 2026-08-20 |
| § 4.2 bullets terse — one line plus at most one continuation | **nothing.** `body` accepts 4000 characters and writes as many continuation lines as it is given; `add_from_roadmap` adds one by default. No op counts lines |
| § 4.2 the six categories, and no invented ones | `changelog_log`'s own enum, for every op that takes a category. A hand-added `### Notes` heading is accepted by every reader |
| § 4.2 the heading order | `op:"normalize"`, and **only inside `[Unreleased]`** — nothing orders a dated release section, so one written out of order stays that way. **And nothing calls that op**: no skill, hook or recipe here invokes it, so it runs only when someone does by hand |
| § 4.2 bullets cite ROADMAP IDs where applicable | **nothing** — an uncited bullet is well-formed, and the bidirectional link this rule exists for simply is not there |
| § 4.3 step 1 — a dated section exists with content | `cut-release` Phase 0e, which stops the release — **unless the recipe performs the cut itself, when 0e passes and the check moves to the recipe's `post_check`, which is optional**. It reads for readiness, not format: any dash, and it does not check the section's internal shape |
| § 4.3 step 3 — bullets flipped to ✅ | `cut-release` Phase 0f, which stops on any ROADMAP ID the section claims shipped that is not ✅ |
| § 4.3 step 2 — `[Unreleased]` was actually emptied | **nothing** — a changelog whose `[Unreleased]` was never emptied looks exactly like one with nothing unreleased |
| § 4.3 step 4 on a store-backed project | **`roadmap-format.md` § 3.12**, which owns why it is not yet performable |

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-08-12 | 3, cold — first gate of this file in its own right (CFG-0064), packet carrying the live `changelog_log` / `roadmap_log` schemas, `/release`'s numbered steps verbatim, and the cited passages of `roadmap-format.md` | 3 | 1 | 2 | n/a | **Six verified, six fixed, two dismissed.** The headline defect was found independently by all three lanes and by the packet build: § 4.3 claimed `/release` "automates steps 1–4" when the skill contains no occurrence of `roadmap` at all and its step 2 only *confirms* a dated section someone else wrote — so a conformer would ship with `[Unreleased]` full and every bullet still 🚧. `skills/app-workflow/SKILL.md` routed on that claim and was corrected in place. **The largest gap was a whole layout this document did not mention**: `changelog_log op:"add_subsection"` writes a feature-grouped `[Unreleased]` (dated `### <date> <Category> — <headline>` topics), the flat ops and `normalize` both refuse against one, and § 4.1 described only the flat shape — so a conformer using the project's own writer could not tell whether they conformed. § 4.1 now states both layouts, that the choice is per file and exclusive, and that the release-time move of a feature-grouped block is specified nowhere. Also: two checks-table rows overstated their checks, § 4.2's category list stated a non-canonical order that `normalize` silently rewrites inside `[Unreleased]`, and its ID-citation example duplicated the category in the bullet against § 4.1's own shape. **A lane's finding was dismissed and replaced by a better one from verifying it:** lane 2 argued `format_mismatch` / `no_changelog` validate a hand-edited changelog — read against `remotecontrol_changelog.cpp` they fire on a *YAML* changelog and a *missing* one, neither on a malformed Markdown one, so the row was right for that reason; but the same read found `feature_grouped_section`, which **is** a real structural refusal, so the row was overstated after all. **A lane dissent was upheld:** two lanes called the category order a live contradiction, one correctly observed `normalize` never touches a dated section — the finding survives narrowed to `[Unreleased]`. **Three claims executed before landing** (4a step 3): the flat-insert refusal, the `normalize` refusal, and `add_subsection`'s emitted heading, all run against a purpose-built feature-grouped fixture. **One filed upstream:** the `feature_grouped_section` refusal's own hint tells the caller to write `### <id> — <topic> (<date>)`, a third heading shape neither `add_subsection` nor this standard uses. |
| 2 | 2026-08-12 | 3, cold — identical brief, packet rebuilt from disk carrying loop 1's fixture results as source facts | 3 | 4 | 0 | n/a | **Seven verified, seven fixed, none dismissed — and six of the seven were loop 1's own collateral, which is the loop working.** The sharpest was found by all three lanes and settled by running it: loop 1 wrote that the two `[Unreleased]` layouts are "per file, exclusive, and **enforced**", having exercised only one direction. `op:"add_subsection"` against a *flat* fixture returns `ok:true` and writes the dated topic above the category blocks — leaving a mixed section this standard says cannot exist, after which every flat insert refuses `feature_grouped_section` and `normalize` does too. **One call permanently disables a flat project's write path, and nothing warns.** Filed upstream; § 4.1 now states the guard is directional and that a flat project must not call the op. Two lanes independently caught loop 1's other over-claim: `/release` step 2 does not "only *confirm*" a dated section — it can dispatch `changelog-writer` to draft one from the commit range, so a section **can** be authored inside a `/release` run, though never moved out of `[Unreleased]`. Corrected here and in `skills/app-workflow/SKILL.md`, which loop 1 had left carrying the same wording. Loop 1 also described `add_subsection` as writing "`**Bold**` category runs beneath" — the fixture's output has none; those are a hand-edit convention the *refusal message* describes, not the writer's emission. And three of loop 1's own sentences were unscoped: § 4.2's heading-order rule ignored that `normalize` refuses a feature-grouped block outright, § 4.3's "all four steps are the author's, **on every project**" invited a store-backed project to hand-edit a generated file, and § What checks this still opened "Nothing parses a `CHANGELOG.md` for conformance" eight lines above a new row naming a real one. **The lesson is 4a-min's:** every finding this loop landed in text loop 1 *added*, none in text it deleted or left alone. |
| 3 | 2026-08-12 | 3, cold — identical brief, packet rebuilt from disk carrying loop 2's flat-fixture result | 2 | 2 | 2 | n/a | **At the cap. Six verified, five fixed, one surfaced.** All three lanes independently refused to accept loop 2's central sentence and asked for the one call that would settle it — and they were right to: `op:"add"` against the mixed file returns **`ok:true`**, so "every flat insert after it refuses" was **false**. Reading `firstFeatureGroupedTopicLine` explains why and replaces guesswork with a predicate: **any `###` heading naming one of the six categories returns "not feature-grouped" outright**, so a mixed section classifies as *flat*, later flat inserts succeed, and `normalize` demotes the stray dated topic below the category blocks instead of reporting it. **A mixed file never announces itself** — worse than loop 2's version, which at least failed loudly. That predicate also answers a Q3 two lanes raised (where is the layout recorded?): nowhere — it is derived from the file, and an empty `[Unreleased]` is flat. Also fixed: "one line plus at most one continuation" was attributed to `changelog_log`'s `body`, which accepts 4000 characters and writes as many lines as it is given — the rule is the author's alone and the table now carries a **nothing** row for it; § What checks this claimed one check where the table lists three, now split into the one that reads the file's shape and the two that only check a caller's arguments; and `app-workflow` Step 8 mandated a flat category heading unconditionally, which on a feature-grouped project is the mixed-file breach. **Surfaced, not fixed:** what § 4.3 step 1 does with a feature-grouped block — folding dated topics into six categories is a mapping nobody has defined, and choosing it is a design call. **Diagnosis for the cap: not size.** Every lane's findings ranged over the whole document, the closing table included — none ran out of document, which is the symptom size produces. Loop 1 discovered an undocumented layout and loops 2 and 3 were spent describing it wrongly, because the layout's own tooling is incoherent — an asymmetric guard, a mixed state that reads as its opposite, and a refusal hint naming a third shape. Three upstream filings, no further loop: the contract cannot settle while the thing it describes does not. |
| 4 | 2026-08-20 | 3, cold — genre pinned `standard`; packet carried the live `changelog_log` schema, `cut-release` 0e/0f, and the em-dash change being gated | 2 | 3 | 1 | n/a | **Six verified, six fixed; none dismissed. Trigger CFG-0152**, which changed the dated heading's separator from an em dash to an ASCII hyphen. **Four of the six landed on text the pre-run fix itself wrote** — the measured pattern, and every one in ADDED text. **All three lanes independently found the same defect, the run's most consequential**: the new paragraph said *"a conformer following an em-dash prescription writes a heading the release gate then refuses"*, and `cut-release` Phase 0e matches **either** dash on purpose. Two lanes raised the same open question and the orchestrator's own search settled it harder than either knew — the `post_check` recipe the paragraph cited exists on **no project on this machine**, and `bump-recipe.md` marks `post_check` optional, so **nothing reliably catches a wrong separator at all**. The claim was deleted rather than hedged (4a step 3: an empirical claim you cannot run is not written down hopefully), the honest position stated, and the rule given its own row in § What checks this. Its sibling Q2: the same paragraph asserted a release-time grep of the file at rest while § What checks this says *"Nothing validates a `CHANGELOG.md` at rest"*. **One Q3, also collateral**: the rule never said whether it binds sections already published under the old prescription, so one maintainer rewrites history and another does not. **Two pre-existing, both real.** § What checks this claimed *"The rows beneath it check the caller's own arguments"* when most of its rows name no check whatever, and `op:"normalize"` — which reads and rewrites the file — is the one entry a hand edit can trip; the replacement deliberately states no count, because **the fix that wrote the count added a row to the table it was counting**, caught by 4a step 3 in the same pass. And § 4.3's four steps never author the `**Theme:**` line that § 4.1's fence requires of every dated section — verified against the live schema that `op:"release"` does not author one either, so the gap applies to every route. **Log order repaired**: this document's rows ran newest-first, alone among the six gated documents on this machine and against 4d's own rule. **Collateral in three neighbouring documents**, none carried into the subject: `cut-release` explained the dash by citing the standard's em dash (found at Phase 1b, before a lane was spent), `app-workflow`'s CHANGELOG **template** propagated the em dash into every project scaffolded from it (found by the 4b repo-wide sweep, invisible to every lane), and a prose mention tripped an `ungranted_tool` check. `skeleton/files/CHANGELOG.md` checked and clean. |
| 5 | 2026-08-20 | 3, cold — identical brief, packet and scrubbed copy rebuilt from disk; the loop-1 already-logged item removed once fixed | 1 | 4 | 2 | n/a | **Seven verified, seven fixed. Five of the seven landed on loop 4's own fixes** — the pattern for a second consecutive loop, and the diagnosis is one thing rather than five: **loop 4 wrote 16 lines of ENFORCEMENT prose into § 4.1 while § What checks this already owned enforcement.** Two statements of one subject is the duplication § 4b exists to stop, and it generated four of this loop's Q2s on its own. **Fixed by deletion, not by patching each contradiction** — § 4.1 now states the rule and points at the table, and the table carries the enforcement. **Two lanes each found the two worst.** Loop 4's paragraph made two mutually exclusive *the only X* claims five lines apart — *"the one thing that can read the finished file is a bump recipe's `post_check`"* against *"`op:"normalize"` … is the only entry here a hand edit can trip"* — and `cut-release` Phase 0e/0f read the finished file too, so both were false; the replacement names all three readers and says which one checks FORMAT. And loop 4's own hyphen paragraphs were inserted between § 4.1's fence and **"That shape is the only one this standard permits"**, moving that sentence's antecedent onto *"Sections already published with an em dash conform"* — so it read as licence to rewrite published headings, the exact reverse of the rule being introduced. **A Q3 nobody had asked**: the rule said *dated heading*, and `add_subsection` writes a second dated heading whose em dash is by design, so a project with legacy feature-grouped subsections could not tell whether it breached. Now scoped to the dated RELEASE heading. **Two pre-existing, both found by two lanes**: § 4.3's pointer sent store-backed readers to the carve-out *for step 4* when the carve-out changes step 3's mechanism too — hand-editing the rendered roadmap loses the flip — and the flow's only table row said *nothing* catches a breach while the body two screens up names Phase 0e and 0f as hard release stops; the row is now three, one per step, and steps 1 and 3 name their check. **The run's own instrument failed twice in the same way.** A quantity claim about this table has now been wrong three times — *"five of the eight rows"* (the fix that wrote it added a ninth), then *"most rows name no check"* (this loop's two new rows made it false). Both were caught by 4a step 3 rather than by a lane. The sentence now states no quantity at all, and says why. |
| 6 | 2026-08-20 | 3, cold — identical brief, packet rebuilt from disk | 0 | 1 | 2 | n/a | **Three verified, three fixed. Cap reached (3 for a standard); the run files its tail and exits.** **A CALM cap by the measure § At the cap prescribes** — checked against the earlier loops' ledger rows, one of the three landed on text this run wrote, against five of seven last loop. The consolidation broke the cycle. **The run's real lesson is one sentence that was wrong four times.** Each rewrite was the fix for the previous wrong version, and each invalidated its own count by adding the row it was describing: *"five of the eight rows"*, then *"most rows name no check"*, then *"two are `cut-release` release-time stops"* sitting one line above *(Deliberately no count)*. The fourth was caught by a lane; the others by 4a step 3. **`documentation.md` § 2.8 item 3 already governed it** — *never summarise a structure you are standing next to* — and that document's own loop 6 hit the identical failure. The summary is now DELETED rather than rewritten a fifth time, and a fifth over-claim was caught in the replacement before it shipped: *what the rows share is that a breach is the author's to avoid* is false of the two `cut-release` rows. **Two Q3s from one lane, both real.** Loop 5's consolidation over-deleted — removing the contradiction took the READER rule with it, so a parser builder would anchor hyphen-only and skip every grandfathered section; restored as *only the writer is bound*. And § 4.2's heading order read as binding `[Unreleased]` alone while the table presupposed it binds dated sections too. **Process error, disclosed by a lane rather than by the orchestrator**: fixes were applied to the live file while two lanes were still reading, so their copy diverged mid-loop and one lane's citations carry a 2-line offset. Phase 2's rule is to rebuild the copy once every Phase 4 edit has landed; waiting for all three lanes is what that means in practice. **Deferred:** § 4.3's *All four steps are the author's* is false where a recipe calls `op:"release"`, which performs steps 1 and 2 — a design call about how this section describes verb-driven releases, filed rather than decided at the cap. |
| 7 | 2026-08-20 | 3, cold — genre pinned `standard`; packet carried the live `changelog_log` schema, `cut-release` 0e/0f, `bump-recipe.md`'s `post_check` rows, and two guard probes executed the same morning | 2 | 2 | 0 | n/a | **Four verified, four fixed; one dismissed. First loop of a NEW run** (the 2026-08-20 run reached its cap at loop 6). **Trigger CFG-0170** — § 4.3's *All four steps are the author's* was false where a recipe calls `op:"release"`, the tail that run deferred at the cap. **The run's largest finding was found at Phase 1b, before a lane was spent, and all three lanes then found it independently.** § 4.1 rested the feature-grouped exclusion on a guard running one way only — *"One call is enough to do permanent damage, and nothing will tell you"* — and `op:"add_subsection"` against a purpose-built flat fixture returns `ok:false` / `flat_section`, naming the exact damage it prevents. The mirror probe settled the other direction and a third stale clause with it: `op:"add"` against a dated-topic fixture refuses `feature_grouped_section` recommending `### <date> <Category> — <headline>`, which is the shape the writer **does** emit, against the document's *"a third heading shape the writer never emits"*. All three upstream filings from the 2026-08-12 run have been fixed upstream. **The exclusion's own reversal trigger has therefore fired**; the false evidence was deleted, the rule left standing on its scope ground, and lifting it filed as CFG-0172 rather than decided — 4a surfaces a design call. **Both Q2s were the pre-run fix's own collateral**, and every lane found the first: the new carve-out sits four lines above **"`cut-release` performs none of them"** and *"never moves `[Unreleased]`"*, so an author on a recipe-cut project hand-moves the section and the run then refuses `nothing_to_release`. Its sibling in § What checks this made Phase 0e an unconditional step-1 stop, where 0e passes on that route and the check moves to a `post_check` `bump-recipe.md` marks optional. **One dismissed, and the fault was the packet's**: two lanes read *"Step 4 is not yet performable"* as false against a § 3.12 window that says the section is settled — § 3.12 states it three times further down, outside the window I cut. Both lanes flagged the window's extent as an open question, which is the lanes working. **Never assert a section's content from its opening.** **Collateral in one neighbouring document, found by the 4b sweep and invisible to every lane**: `app-workflow` Step 8 carried both corrected claims — *"the damage is silent and permanent"* and *"`cut-release` never performs the move"*. `review-contract`'s `rationale.md` holds the old behaviour inside a dated worked case and was deliberately left: it records what was measured on 2026-08-12 and is true as a record. |
| 8 | 2026-08-20 | 3, cold — identical brief; packet rebuilt from disk, its § 3.12 window repaired and the mirror-direction probe added as a source fact | 3 | 0 | 1 | n/a | **Four verified, four fixed; none dismissed. Three of the four landed on loop 7's own fixes** — the measured pattern, and every one in ADDED text. **All three lanes independently found the same defect, and one named the exact call that settles it.** Loop 7 wrote *"Both directions refuse"* off two probes against POPULATED fixtures. Both refusals need existing `###` headings to fire, so against an **empty** `[Unreleased]` the guard does not run at all — and § 4.3 step 2 leaves one empty after every release while § 4.2 requires it always be present. Executed: `op:"add_subsection"` against a bare empty section returns `ok:true`. **The unguarded state is the one this standard's own flow guarantees**, which is the opposite of what loop 7 claimed, and it had reached CFG-0172 as that bullet's premise — annotated there rather than left to be decided on it. **This is loop 7's boundary-word failure exactly as 4a step 3 describes it**: *both* is a claim about the cases you did not try, and loop 7 ran the two it had in mind. **A second probe corrected the TOOL rather than the document.** The `flat_section` refusal says a mixed section leaves `op:"add"` and `op:"normalize"` both refusing and *"the flat write path is gone for good"*; executed against a mixed fixture, `op:"add"` returns `ok:true` — the 2026-08-12 measurement still holds and the verb is wrong about itself. The new conversion route says what was run, not what the message claims. **The Q3 was raised by two lanes**: the standard forbade the feature-grouped shape and gave a project that already has one no route out, while `op:"add"` refuses such a section — so a conformer's only remaining move was the hand-written mix. § 4.1 now names emptying the section as the route. **One pre-existing Q1, from resolving a lane's open question rather than from a finding**: § 4.2 said a wrong heading order in `[Unreleased]` is *"temporary"* because `op:"normalize"` rewrites it, and a repo-wide search finds no skill or recipe that calls that op — nothing runs unprompted, so a wrong order stays until someone runs it by hand. **Two open questions resolved clean, counted nowhere**: `add_batch`, `format_mismatch` and `no_changelog` are all real (schema), and an empty-state hint line adds no `###` heading, so a hinted section is unguarded exactly like a bare one. |
| 9 | 2026-08-20 | 3, cold — identical brief; packet rebuilt from disk with the empty-section, mixed-section and release-on-feature-grouped probes added as source facts | 0 | 2 | 0 | n/a | **Two verified, two fixed. Cap reached (3 for a standard); the run files its tail and exits. A VIOLENT cap by the measure § At the cap prescribes** — both findings landed on text loop 8 wrote, checked against the earlier loops' ledger rows: 2 of 2, against 3 of 4 in loop 8 and 2 of 4 in loop 7. The finding COUNT fell each loop (4, 4, 2) while the collateral SHARE rose (50%, 75%, 100%), which is the run repairing itself rather than the document. **All three lanes found both, and one of them by executing the packet fact the orchestrator had run one loop earlier.** Loop 8's conversion route offered *"cut the release, or move its topics out"*, and F10 shows `op:"release"` carries the dated-topic headings into the dated section **verbatim** — so that route empties `[Unreleased]` and deposits the forbidden shape permanently in a dated section, which § 4.2 binds too and which the table says nothing orders. **The route relocated the breach and called it a conversion.** Fixed by deleting the route rather than adding a rewrite step. The second is 4a-min's exact failure in the same loop that quoted it: loop 8 corrected the normalize claim in § 4.2 and left the § What checks this row stating the rule the old way, so the document's authoritative enforcement list named a check that never runs — while every neighbouring row states its escape. **Fixed at the root**: § 4.2 now states the rule and nothing else, and the row carries the whole of what catches it, which is what loop 5 already learned about § 4.1. **Collateral in one neighbour, flagged by all three lanes and filed against none of them**: `cut-release` Phase 0e cited `changelog-format.md` **§ 4.3** as the home of the ASCII-hyphen rule, which this document states in **§ 4.1** — corrected there. **Diagnosis for the cap, and it is not size.** At ~220 lines this document is a fifth of the budget and every lane reached its closing table on every loop. All nine collateral findings across three loops sit in ONE subject — the feature-grouped layout and the behaviour of its guard — and the rest of the document has come back clean for two consecutive loops. Each loop asserted a boundary about the verb and the next found the case it had not probed. **So the route is not a split: it is CFG-0172.** While this standard describes a layout it forbids, in terms of a guard it must probe case by case, it will keep oscillating. Do not re-gate this document until that decision is made. **Three open questions resolved clean, counted nowhere**: `add_batch`, `format_mismatch` and `no_changelog` all exist in the live schema (what the latter two fire on was measured 2026-08-12 against source not on this machine, and is left as the dated record it is); an empty-state hint line adds no `###` heading, so a hinted section is unguarded exactly like a bare one. |

**Rows before this one live in `roadmap-format.md`.** This content was gated
three times as part of that document, whose log holds those rows and keeps
them — they record loops against a document that no longer exists in that
shape, so they are not copied here and not rewritten there. The split was
made *because* those three loops hit the cap without converging on a document
too large for one cold read.

<!-- MIRROR END -->
