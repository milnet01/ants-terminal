<!-- ants-local-gate-standards: mirror -->
# Local Gate Standards

> **MIRROR — do not edit this file.** The owner is
> `~/.claude/standards/local-gate.md`; everything between the `MIRROR BEGIN` and
> `MIRROR END` markers below is a verbatim copy of it, kept in the repo
> because this repo is public and a reader who has only the checkout cannot
> follow a pointer into a private home directory.
>
> **A correction goes upstream, then gets re-copied here.** Editing this copy
> makes it a second standard. Refresh with
> `tools/check-standard-mirrors.sh --write`, which re-copies the owner into
> the marked region and leaves this header alone; `tools/hooks/pre-commit`
> refuses a commit whose mirror has drifted from its owner (ANTS-4133).
>
> **Why this file is mirrored (ANTS-4761).** A link from a mirrored half to an
> un-mirrored sibling dead-ends for exactly the reader the mirrors exist to
> serve. The rule that settles which files are carried is in
> [`README.md`](README.md) — every top-level global standard a mirrored half
> links to.

---

<!-- MIRROR BEGIN ~/.claude/standards/local-gate.md -->
# Local Gate Standard — v1

The rules for running a repository's CI pipeline on your own machine
before a push. It binds every repository that has a pipeline, and every
hook, skill or session that pushes on one's behalf.
[commits.md](commits.md) § 4.2 states the obligation, points here and
restates none of it. **Four documents do carry summaries and must be
amended with any rule they restate**: [`workflow.md`](../workflow.md) § 6,
the machine's `CLAUDE.md` rule 6, [releases.md](releases.md) § 6 step 5,
and this standard's own row in `commits.md` § What checks this. Editing a
rule here and stopping is the re-encoding drift § 1.1 exists to prevent.

## 1. Principles

**1.1 Read the source of truth, never re-encode it.** Every rule below
follows from this. A copy of a pipeline is correct on the day it is
written and drifts from then on, silently.

**1.2 A hook does it, not a person.** A rule a person has to remember
holds exactly as often as they remember it. Nine projects on this
machine wrote this rule into a document and every one of them later
wrote a `pre-push` hook anyway.

**1.3 Answer for the commits being pushed.** Any other tree is one the
remote will never see, so a verdict about it is not a verdict about the
push.

**1.4 A wrong guess must cost time, never coverage.** Where a rule below
leaves a case undecided, run more rather than less.

## 2. The gate is a hook, not a habit

**If the repository has a CI pipeline, run it locally first.** A failure
found on your machine costs seconds; the same failure found by CI costs
a push, a wait, a red notification, a fix commit and a second push — and
on a metered repository it costs the minutes twice.

**A repository with a pipeline, a gate script and no reachable
`pre-push` hook is in breach of this standard**, not merely unlucky.

**A repository with a pipeline and no way to run it locally has a gap
worth fixing** before the next feature, not a rule to argue with.

**A bypass is [commits.md](commits.md) § 2.3's, and this standard adds
nothing to it** — including no summary, because the one written here on
2026-08-31 narrowed that section's rule within the day. It covers
`git push --no-verify` and the `SKIP_LOCAL_CI=1` form a hook offers, and
it is where a conformer finds out whether a skip needed the user and where
the reason gets written. Read it there.

## 3. Execute the pipeline's own definition

**The local run must execute the pipeline's own definition, not a copy
of it.** This is the whole rule, and it is the part that gets skipped. A
hand-written `ci-local.sh` mirroring the workflow drifts, and **a
drifted mirror is worse than no mirror** — it returns green for a
pipeline that will fail, so you push with more confidence than if you
had never run it.

**So invert it: put the checks in one script the repository owns, and
have `.github/workflows/ci.yml` set up a machine and call that script.**
Then there is one list of steps, the local and remote runs cannot
disagree, and the script can take a flag — which § 6 asks for where the
gate can offer one, and a
container full of YAML cannot offer. LocalWebServerManager is the worked
example: its `ci.yml` header is reported to say outright that the script
owns every actual check. That project is not on this machine, so the claim
is carried from `workflow.md` § 6 rather than checked here.

**A repository-owned gate script is only a mirror when the workflow does
not call it.** Without that distinction the paragraph above appears to
forbid the arrangement this one prescribes.

**Where the pipeline cannot be inverted** — a workflow you do not
control, a job matrix with no single entry point —
[`act`](https://github.com/nektos/act) runs the real workflow in a
container and is the fallback.

## 4. Jobs that cannot run locally

**Where a job genuinely cannot run locally** — a self-hosted runner, a
secret that is not on your machine, a platform you do not have — cover
what you can and **write down which jobs are not covered**, next to the
command that runs the rest. An uncovered job nobody has named is
indistinguishable from a covered one, which is the same false confidence
in a different place.

## 5. Which tree the run answers for

**Run it over the commits you are pushing, not over your working tree.**
Those are the same tree only when the tree is clean, and nothing makes
it clean. An uncommitted fix turns the run green for commits that will
go red; unrelated scratch work turns it red for commits that were fine.
Neither answer announces itself.

**One sequence legitimately gates an uncommitted tree, and it is not an
exception.** [releases.md](releases.md) § 6 step 5 runs the pipeline from
the bumped tree before step 6 commits it — there the working tree *is*
the commit about to be made. What that run does not do is discharge the
push-time one: the gate that runs when those commits are pushed is still
over the pushed tip. Two runs, two questions.

A pre-push hook is where this bites. git hands the hook the exact range
on stdin — one `<local ref> <local sha> <remote ref> <remote sha>` line
per ref — and a hook that then runs the gate from the repo root has
thrown it away. **The same range decides § 7's skip**, so a hook that
reads it for one and not the other is already half right.

### 5.1 Route 1 — check the pushed commits out somewhere else

```bash
tree=$(mktemp -d)
trap 'git worktree remove --force "$tree" 2>/dev/null' EXIT
git worktree add --detach --quiet "$tree" "$local_sha"
( cd "$tree" && ./scripts/local-ci.sh )
```

That runs against what the remote will see and never touches the working
tree. The cost is a checkout with none of your ignored files in it, so a
gate needing a virtualenv, a build tree or a downloaded fixture pays to
make one on every push.

### 5.2 Route 2 — refuse a dirty tree instead

Where route 1's cost is too high, exit non-zero when
`git status --porcelain` is non-empty, and name the files. It is a worse
experience and an honest one: the developer is told the run cannot answer
for this push, rather than shown a verdict that does not.

**A clean tree is not enough on its own, and this is the condition that
gets left out.** It proves the tree matches `HEAD` — never that `HEAD` is
what you are pushing. `git push origin other-branch` from `main`,
`git push origin HEAD~2:main`, and a push carrying several refs all pass
the porcelain check while the gate answers for a commit the remote will
never receive. So route 2 is available **only when every pushed tip
equals `HEAD`**: compare each `<local sha>` on stdin against
`git rev-parse HEAD`, and where any differs, refuse or take route 1.

### 5.3 Never stash to manufacture a clean tree

`git stash` exits 0 having stashed nothing when there is nothing to
stash, so the hook cannot tell a clean tree from a failed stash —
[`languages/cpp.md`](languages/cpp.md) § Tests records the same trap
producing a false pass. A stash also mutates the developer's tree
mid-hook, and the `pop` has to survive a gate that may have just exited
non-zero, on a machine where the push was interrupted.

## 6. Documentation-only pushes

**A docs-only push runs the documentation checks — not a subset the gate
does not offer, and not nothing.** Both extremes are wrong in the same
way. Where the gate has a documentation mode this is less than the whole
pipeline; where it has none, running all of it is the compliant answer
and § 8's hand-picking anti-pattern is what forbids the alternative. A
full run charges ninety seconds of engine tests for a typo fix, and that
is how a person learns to reach for `--no-verify` by reflex; a blanket
skip walks past the markdown lint, the link check, the docs build and the
test that counts something in a README.

Measured on this machine (OneUp, 2026-08-21): that project's
documentation mode runs in 0.14s where its full gate takes about 90s.
Read its step list to see why that is not a coverage cut — `--docs` keeps
the version lockstep (its `CHANGELOG.md` is one of six sites that must
agree), the bump-tool test and the documentation checker, and drops the
engine suite, the GUI smoke test, byte-compilation, lint and packaging
validation. **What a documentation mode keeps is decided per project and
this standard does not certify it** — that gate's own header claims it
covers every check a `.md` edit can reach, and only that project can say
so.

So **give the gate a documentation mode and select it by the paths in the
push**. Where the gate has no such mode, run all of it and say so — never
quietly select a subset the gate does not offer. A hook cannot invent a
check, and a skipped check followed by a green line is indistinguishable
from a clean run.

### 6.1 Which files count as documentation

**A per-project answer, and getting it wrong is not symmetric.** A file
the pipeline READS is a pipeline input whatever its extension: a test
that asserts against `README.md`, a check that counts headings, a docs
site that builds from `docs/`. On 2026-08-19 a LocalWebServerManager push
edited `CLAUDE.md`, took the exemption on the strength of the `.md`, and
GitHub found the prose count that project's own suite forbids. So decide
by what the pipeline reads, never by the extension, and let every
uncertain case run the full gate.

### 6.2 A shared hook must be told

One hook shared between repositories has no way to know what a given
pipeline reads, and its default will therefore be an extension list —
which § 6.1 forbids. `~/.claude/githooks/pre-push` reads two keys about
documentation, and they are not symmetric.

**`ants.gate.docsGlob` says which paths count. Leaving it unset costs
COVERAGE**: the hook falls back to an extension list, which is what § 6.1
forbids and what produced the 2026-08-19 failure above. So a repository
**relying on a shared hook** that has not set `ants.gate.docsGlob` has not
satisfied this standard. A repository keeping its own hook satisfies this
section by encoding the answer in that hook instead; it never reads the
key.

**`ants.gate.docsMode` names the gate's own documentation flag. Leaving it
unset costs TIME, and only sometimes**: untold, the hook greps the gate
script for `--docs` and `--lint`, so a gate spelling its flag either way
still gets documentation mode. A gate spelling it otherwise pays for the
full run. That is not a breach — § 1.4 spends time rather than coverage —
but setting the key removes the cost.

**A third key is not about documentation and is named here because nothing
else names it**: `ants.gate.command` tells the hook where the gate script
is. A repository whose script sits outside the hook's own discovery list
and has not set it is reported as having no gate at all, which is § 2's
breach with a gate script present.

The fallback is a deliberate compromise rather than an oversight: a hook
that refused to run until configured would be uninstalled, and one that
guesses at least says so in its own source. **A repository whose pipeline
reads markdown must narrow that glob to exclude those paths, or keep its
own hook.** Nothing checks that it did.

## 7. The complete skip — three conditions

Where the pipeline has no job that acts on documentation, the
documentation checks are the empty set and the push may skip the local
run entirely — **if all three hold**:

0. **The push is documentation-only by § 6.1's test.** Stated as a
   condition because the two below are not sufficient on their own: a
   *code* push into a directory no job reads satisfies both, and a hook
   built from them alone would wave it through untested.

1. **The last push's CI run on the remote succeeded** —
   `gh run list --branch <the branch being pushed> --event push
   --limit 1 --json conclusion`. **Take the branch from the ref being
   pushed, never from `git branch --show-current`** — that reads the
   working tree, which § 5 has just finished saying is not what is being
   pushed, so a hook run for `git push origin feature` from `main` would
   check `main`'s last conclusion and grant the skip on it. The
   `<remote ref>` field of the hook's own stdin line names it — **with
   the prefix stripped**, `${remote_ref#refs/heads/}`. git supplies that
   field fully qualified and `--branch` wants a bare branch name.
   Measured 2026-08-21: `--branch main` returned two runs on a repository
   where `--branch refs/heads/main` returned `[]`, so a hook substituting
   the field directly gets an empty list on every push and this condition
   can never hold — a skip that silently never fires, indistinguishable
   from one denied on the merits. **Both filters are load-bearing.**
   `gh run list` scopes by neither branch nor event, so `--limit 1` alone
   returns the most recent run of any kind on any branch — a green
   scheduled run while the last push went red is exactly the case this
   condition excludes.

2. **No job in the pipeline acts on the documentation paths in this
   push.** Check the pipeline's definition — the workflow, **and where
   the workflow delegates to a gate script, that script**. In an inverted
   repository the workflow is one thin job that runs on every push, so
   reading it alone answers *"a job acts on my paths"* yes every time and
   the skip becomes unreachable in exactly the shape § 3 tells you to
   build.

**Taking this skip is not a bypass, and taking it BY HAND is.** A hook
that checks the three conditions and exits 0 has run the gate this
standard asks for. Reaching for `SKIP_LOCAL_CI=1` or `git push
--no-verify` because you believe the conditions hold is `commits.md`
§ 2.3's bypass and needs the user, whatever you believe. No hook on this
machine implements the skip, so by hand is the only route there is today.

**A release push is never documentation-only, whatever its diff looks
like** — so neither this skip nor § 6's documentation mode is available to
one. [releases.md](releases.md) § 6 step 5 owns the reason.

**Condition 2 is the one that matters, and it is why this is not simply
"docs are safe".** Plenty of pipelines lint markdown, check links,
spell-check, or build a docs site — and against one of those, a docs-only
change is exactly as capable of going red as a code change. Taking the
skip without checking is how the rule produces the failure it exists to
prevent. If any job touches your paths, this skip is not available — run
those jobs, which is § 6's documentation mode, whatever the diff looks
like.

**This skip outranks the whole of § 6, documentation mode included.**
Both fire on a docs-only push that meets all three conditions below, to a
repository whose pipeline has no documentation job — whether or not its
gate has a documentation mode — and they prescribe opposite things. Having
a documentation mode is no more a reason to run it than lacking one is,
when there is provably nothing in the pipeline for your paths.
**§ 1.4's run-more-rather-than-less does not reach this**: condition 2 is
proof, not uncertainty.

## 8. Anti-patterns

- **A hand-written `ci-local.sh` the workflow does not call.** § 3. It
  returns green for a pipeline that will fail.
- **Running the gate from the repo root inside a pre-push hook, without
  § 5.2's two guards.** § 5. It then answers for the working tree rather
  than the push. With both guards — a clean tree, and every pushed tip
  equal to `HEAD` — the two are the same tree and route 2 is compliant.
- **`git stash` before the run.** § 5.3. It cannot be distinguished from
  a no-op, and it mutates the developer's tree.
- **A blanket skip for a `.md` diff.** § 6. The extension is not the
  test; what the pipeline reads is.
- **Hand-picking a subset of steps the gate does not expose as a mode.**
  § 6. A hook cannot invent a check.
- **`gh run list --limit 1` with no `--branch` and no `--event push`.**
  § 7 condition 1. It grants the skip on an unrelated green run.
- **Taking the § 7 skip without reading the gate script the workflow
  delegates to.** § 7 condition 2.

## What checks this

| Rule | What catches a breach |
|------|----------------------|
| § 2 the pipeline runs locally before a push | A `pre-push` hook, where one is installed **and reached**. It refuses the push, so it catches the breach rather than the failure. Three ways it fails to gate, **two of them silent**. `core.hooksPath` holds **one** value and the repository-local one wins, so a project setting it for its own `commit-msg` never runs `~/.claude/githooks/pre-push` unless a `pre-push` in its own hooks directory reaches it. `skeleton/files/.githooks/pre-push` does exactly that — it delegates — so a project scaffolded from 2026-08-21 is covered and one scaffolded before that date is not, silently, until somebody copies the file in. Nothing checks that `core.hooksPath` was set at all; it is per-clone and cannot be committed. The third is reached and is **not** silent: on discovering no gate script in a repository that has a pipeline, the machine-wide hook prints `NO LOCAL GATE, BUT THIS REPO HAS A PIPELINE — nothing was checked` and two lines citing this standard. What it lacks there is a *block*, not a voice — it exits 0, so § 2's "gap worth fixing" is announced on every push and stops none of them |
| § 3 the local run executes the pipeline's own definition | **nothing** — whether `ci.yml` calls the repository's gate script is readable, and no check reads it |
| § 4 uncovered jobs are named | **nothing** — the absence of a sentence is what would have to be detected |
| § 5 the run is over the pushed commits, not the working tree | `~/.claude/githooks/pre-push` and LocalWebServerManager's hook take route 1 unconditionally, so where either runs the rule cannot be breached. Everywhere else **nothing**, and this one is invisible from both sides — a gate run over a dirty tree returns an ordinary verdict with no sign that it answered for a tree nobody is pushing. Checked 2026-08-21: no project has a test asserting its hook takes either route |
| § 6.2 a repository sets `ants.gate.docsGlob` | **nothing** — the machine-wide hook falls back silently, and § 6.2 says so in its own text. The same is true of `ants.gate.docsMode` and `ants.gate.command`: each has a silent fallback, so an unset key and a correctly-set one produce the same push |
| § 7 all three conditions hold before the skip | **nothing** — no hook on this machine implements this skip. `~/.claude/githooks/pre-push` classifies a push as documentation-only, but it does that to select § 6's documentation mode: it holds no `gh` call, and after classifying it always runs the gate. So condition 1 is checked by nothing, condition 2 is a read of the pipeline's definition, and the skip is taken by hand or not at all |

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-08-31 | 3, cold — genre pinned `standard`, first gate on a document created that day by splitting `commits.md` § 4.2 (CFG-0185). Packet carried both `pre-push` hooks in full, `commits.md` §§ 4.1-4.2 and its § 4.x coverage rows, `workflow.md` § 6, `releases.md` § 6, `languages/cpp.md` § Tests and `CLAUDE.md` rule 6 | 2 | 2 | 1 | n/a | **Five verified, five fixed; none dismissed. Three defects were found independently by all three lanes** — the highest agreement this gate produces, and on a first loop. **The worst was written by the split itself**: the § 7 coverage row claimed `~/.claude/githooks/pre-push` *"implements conditions 0 and 1 mechanically"*, and that hook holds no `gh` call and no skip path at all — after classifying a push as documentation-only it always runs the gate, because the classification serves § 6's documentation mode. So a hook author would have left out the last-push-green check believing it was already mechanised, and taken the skip after a red push — the one case condition 1 exists to exclude. **The second is pre-existing and survived nine loops of `commits.md`'s own gate**: § 6.2 said a repository not setting `ants.gate.docsGlob` is in breach *"whatever hook is installed"*, two paragraphs above offering *"or keep its own hook"* as the remedy — and a bespoke hook never reads that key, so one conformer sets a dead key and another is graded in breach. The flat form had propagated verbatim into `workflow.md` § 6 and `CLAUDE.md` rule 6; all three swept. **The third was this document's own opening claim** that neither citing document restates its rules — false of `workflow.md` § 6, which summarises §§ 2, 3, 6 and 7, and of `CLAUDE.md` rule 6. A sentence licensing exactly the fix-one-copy failure § 1.1 exists to prevent. **Two lanes found the release gap**: `releases.md` § 6 step 5 says a release is never docs-only, and §§ 6-7 carried no carve-out, so a hook built from this document alone grants the skip to a release push whose diff matches the docs glob. **One lane alone found the bypass gap**, and correctly predicted its own shrink: `commits.md` § 2.3 owns bypasses and names this gate, so what was missing is a pointer rather than a rule — but a conformer reading only this standard could not tell whether `SKIP_LOCAL_CI=1` breached it. **Collateral from the split, found by that finding**: `commits.md` § 2.3 still named the gate as *"the §4.2 gate"* after its rules moved. **Unrunnable region, stated rather than looped on**: OneUp and LocalWebServerManager are not on this machine, so § 6's 0.14s/90s figures and the § 5 row's claim about that project's hook cannot be checked here. Both are dated measurements inherited from `commits.md` § 4.2 and converged on there. **Three lane open questions resolved clean and are not in the tally**: both hooks landed 2026-08-21 so the coverage date holds; `releases.md` § 6 numbers its steps 1-7 so *step 5* is right; and `commits.md` § 2.3 does own the bypass rule. **One packet defect, mine**: my `releases.md` window was captioned *steps 3-6* and rendered 1-7. No finding rested on it. |
| 2 | 2026-08-31 | 3, cold — identical brief, packet rebuilt from disk and extended with `commits.md` § 2.3, the whole of `releases.md` § 6, and a stated unrunnable region | 0 | 4 | 2 | n/a | **Six verified, six fixed; none dismissed. Not one Q1** — every defect was two passages disagreeing or a rule nobody could be caught breaching. **Five of the six landed on text the split or loop 1 wrote**, and the lanes overlapped on only one, so the corroboration this loop bought was breadth rather than agreement. **The sharpest was loop 1's own repair.** Loop 1 fixed the false claim that no document restates these rules by naming two documents and four sections; `releases.md` § 6 step 5 is a third site and it summarises § 4, which appeared in no amend-list at all — so an editor rewriting § 4's naming rule had no amendment duty anywhere and would leave that step prescribing the superseded text. Two lanes found it. The list now names the four copies and enumerates no sections, because a section list is one more thing to keep true. **The second was also loop 1's**: § 2's new bypass pointer restated two of `commits.md` § 2.3's rules and narrowed one within the day — *a bypass of a check that actually failed always needs the user* reads as licensing a PRE-EMPTIVE `SKIP_LOCAL_CI=1`, which § 2.3 requires the user for, since a hook's header names no circumstance. The summary is deleted rather than corrected: the section already said it adds nothing to § 2.3, and the restatement is what made that false. **One lane found § 7's precedence sentence resolving only half its own overlap** — it named the no-documentation-mode shape, leaving a gate that HAS one, in a repository whose pipeline has no documentation job, with § 6 saying run the mode and § 7 saying skip. Two hooks were buildable from it. The skip now outranks the whole of § 6, and `workflow.md` § 6's copy of the old narrow form was swept with it. **The same lane found § 3 claiming § 6 *requires* a documentation flag**, where § 6 permits running all of it and saying so — and `act`, § 3's own sanctioned fallback, can never offer a flag, so § 3 put every un-invertible pipeline in permanent breach and pointed it at the mirror § 3 forbids. **One lane found § 6.2 naming one config key where the hook reads two**: `ants.gate.docsMode` is unnamed anywhere in the standard, and untold the hook greps the gate script for `--docs` and `--lint` and finds no other spelling — so a conformer sets the glob, believes § 6.2 satisfied, and pays for the full gate on every documentation push. **And one lane found § 8's route-2 anti-pattern unqualified** against § 5.2, which sanctions exactly that arrangement under two guards — so a hook author on a repository where route 1's rebuild is too costly would read their compliant hook as a breach. **Unrunnable region, widened**: two lanes noted that § 3's claim about LocalWebServerManager's `ci.yml` header is the same unverifiable class as § 6's timing figures, and loop 1's note had not named it. Attributed to `commits.md` § 4.2 rather than asserted. **One packet defect, mine, and all three lanes caught it**: my verified-facts sheet said `releases.md` § 6 numbers its steps 1 to 7 where the section has 9, and my own window showed 9. No finding rested on it, and every lane said so rather than filing it. |
| 3 | 2026-08-31 | 3, cold — identical brief, packet rebuilt from disk with the fact sheet corrected and the unrunnable region widened to three claims | 3 | 3 | 1 | n/a | **Seven verified, seven fixed. Cap reached (3 for a standard), and it is a VIOLENT cap: five of the seven landed on text loops 1 and 2 wrote**, checked against those loops' ledger rows rather than recall. The run is oscillating and a fourth loop is not filed — routing at the end of this row. **Four defects were found independently by all three lanes, and all four are one loop-2 fix.** Loop 2 rewrote § 6.2 to name the second config key and left the paragraph broken four ways: a sentence fragment (*"The glob — and untold, it falls back to an extension list."*), a *"that key"* with two live antecedents eight lines after both were named, a false claim that setting one key and not the other costs the full gate, and a count of **two** keys where the hook reads **three**. One lane found the asymmetry the count was hiding and it is the finding that mattered: an unset `docsGlob` costs COVERAGE — the extension-list fallback § 6.1 forbids — while an unset `docsMode` costs only TIME, and only where the gate spells its flag as neither `--docs` nor `--lint`. A conformer reading loop 2's text would set the cheap key and leave the breach. § 6.2 is rewritten around that asymmetry and names `ants.gate.command`, which nothing on this machine named anywhere. **Two lanes disputed a packet fact of mine and were right**: the fact sheet said the hook reads two keys, and its own full copy of the hook shows three. **A third loop-2 fix dropped a condition**: the rewritten precedence sentence read *"Both fire on ANY docs-only push"*, so a hook author encoding it grants the skip after a red push — the case condition 1 exists to exclude. **A fourth was loop 2's attribution**, wrong by one document: the LocalWebServerManager claim is carried by `workflow.md` § 6, and `commits.md` § 4.2 restates nothing, which this document's own opening sentence says. **The two pre-existing findings are the run's most valuable.** One lane found § 6's headline still in the flat *"not the whole pipeline"* form — and `workflow.md` § 6 records that exact wording being corrected THERE on 2026-08-25, so the repair landed in the summary and the owning text kept the defect for six days. That is the fix-one-copy failure with the copies the wrong way round, and § 1.1 is the rule it breaks. One lane found § 7's *"may skip the local run entirely"* against `commits.md` § 2.3, which requires the user for a by-hand skip of this gate — and this document's own table says the skip is taken by hand or not at all, so a conformer meeting all three conditions had two lawful and opposite moves. § 7 now separates the two: a hook that checks the conditions and exits 0 has conformed; reaching for `SKIP_LOCAL_CI=1` because you believe they hold is § 2.3's bypass and needs the user. **Routing, per § At the cap.** Size is not the cause — this is among the shorter standards in the set and every lane reached § 8 and the closing table on every loop. What oscillated is the CONNECTIVE TEXT this run wrote: the rules themselves were inherited from `commits.md` § 4.2 and converged there over nine loops, while every sentence written to join them to their new neighbours took two or three passes. **Do not re-run this gate on the document as it stands.** A split is not the remedy and is not proposed: there is one subject here. The document ships; an authoring edit that changes direction re-arms the gate normally. |
<!-- MIRROR END -->
