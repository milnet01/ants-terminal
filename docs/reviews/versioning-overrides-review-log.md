# Review loop log — `docs/standards/versioning-overrides.md`

Kept out of the standard so the rules are not paid for alongside their
review history, matching the other standards in this project.

**Rows are never edited once landed.** They record what each pass found,
including where a later pass corrected it — a correction goes in the
current row, not by rewriting an old one.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-04 | 3, cold — genre pinned `standard`; packet carried `versioning.md` §§ 3-4, the global standards index's three cases, `releases.md` § 2, the three ROADMAP milestone headings, and verified facts for `RoadmapStore::open()`, `cut-release` and the three cited ids | 1 | 2 | 3 | n/a | **Six verified, six fixed; one dismissed as a packet defect. Four open questions, three of which resolved clean.** **Phase 1b caught the first Q1 before a lane was spent**: the draft claimed `releases.md` § 2 requires a release to lead with its breaking change, and § 2 says nothing of the kind — the clause was carried over from UT_Ants's file instead of read from source. Replaced with what § 2 states. **All three lanes independently reported the ANTS-1087 retraction as false, and all three were wrong for the same reason — my packet.** It gave them the item's status (`planned`) without its body, which reads "RETRACTED 2026-08-25 … do not cite this item as a commitment". The document was right; the finding is dismissed as unverified. **But the three lanes found the real defect underneath**, which is why it was re-tagged and kept: the item is still `planned` under § 1.0.0, so a release manager assembling a 1.0 checklist from that section meets a contradiction. The file now names the carrier and says to read the entry rather than the status. **Two lanes found the exit condition citing an item wider than its own bar** — ANTS-1088 is "bus factor ≥ 2 **+ governance doc**" while the condition asks only for commit rights, so two readers settle `1.0` differently on the one sentence this section exists to make checkable. The governance doc is now explicitly outside the gate. **One lane found the header enumerating "the two answers" while the file also carries a genuine override of § 4's level rule** — a releaser trusting the enumeration applies the global rule and publishes the wrong level. **One lane found the milestone table using two different completion tests in adjacent rows** (capabilities vs items), and **one found the PATCH gloss omitting breaking changes** — the single case the global rule sends to the MINOR, so a conformer falls back to it and bumps a milestone number for a release completing no milestone. That last one is the hole the override itself opened. **Collateral fixed outside the document:** ANTS-4873's headline ("no 1.0 exit condition is written down anywhere") was made stale by this run and is closed. **Open question left unresolved:** whether the milestone override survives the `1.0` boundary — the table's `1.0.0` row sits under a heading scoped to "inside `0.x`". Not a finding; nothing in the packet settles it. |
| 2 | 2026-09-04 | 3, cold — identical brief, packet rebuilt from disk with the ANTS-1087 status/body split quoted in full | 1 | 2 | 1 | n/a | **Four verified, four fixed; one dismissed; one collateral.** **Three of the run's lane findings so far were caused by my packet, not the document** — this loop produced the third. All three lanes called *"whose theme is the API freeze"* false, reasoning from the section TITLE I gave them; the section's Theme line reads exactly that. Dismissed, and the clause deleted anyway: three cold readers misreading one clause is evidence about the clause. **The best finding is the override contradicting its own audit row.** *"A release's level matches what it changed"* is the GLOBAL question this file abolishes, so that row rejects a correct PATCH carrying a breaking change — which § 4 forbids by name. **One lane found the header scoping both answers to `0.x`**; § 3 asks every project for its surfaces, so a conformer at `1.0` would stop maintaining the list. Its collateral was the standards-index row, which I had written the same way. **One lane found the catch-all able to override the deliberate exclusions**, making an exclusion no exclusion; it is now scoped to surfaces the file has not considered. **One lane's open question became a fix**: ANTS-1084's body is a promise, not an artefact, so *"the API is frozen"* was not checkable by a third party as § 4 requires. The condition now names the shipped item. **Process note for a later run:** the packet used hard-coded roadmap line numbers that went stale when `roadmap_log` re-rendered the file mid-run. Extract by heading, not by line. |
