# Review loop log — `docs/standards/mcp-error-codes.md`

Moved out of the standard so the rules are not paid for alongside their
review history. A standard is read on every consultation; this is read
when someone asks what a past loop already settled.

**Rows are never edited once landed.** They record what each pass found,
including where a later pass corrected it — a correction goes in the
current row, not by rewriting an old one. That is the whole reason the
log is kept: so a trap already paid for is not re-engaged.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-08-28 | 3, cold — genre pinned `standard`; packet carried the render gate's predicate, `gateScope`'s two empty states, INV-5, the live refusal message and `amend_field`'s contract | 3 | 6 | 1 | n/a | **Ten verified, ten fixed, none dismissed. First gate ever run on this file** (ANTS-4435), which is why it had no loop log until now. **Two lanes independently found the same defect**, the run's strongest signal: `no_changelog` called `format_mismatch` a YAML-changelog code "only", which two other rows contradict — so a verb author facing a no-writer-for-this-artifact refusal would mint a one-off code, the split this taxonomy exists to prevent. **Three Q1s were false claims about emitters, all confirmed against source.** `op_unsupported` defined itself as the code for any store-only op called on a non-migrated project, while `render` and `repair_trailers` remap to `project_not_registered` and `amend_field` to `unsupported_format`; `project_not_registered` claimed one op where three remap to it; and `unsupported_format` never documented its one backend-conditioned emitter. **The sharpest Q2 landed on the row this run was gating**, and on the sentence the run itself had just written: the ride-along remedy said a `note` whose FIRST line declares the trailer sets the column, where `rlNoteDeclaresTrailer` computes the start of the line carrying the match — so any line does, and a caller putting the label on line 3 gets no refusal and a set column against a document saying otherwise. The same narrowing was corrected in `mcp-behavioural-notes.md` as collateral, and survives in the shipped refusal message and one test comment, filed rather than edited under a docs skill. **Also fixed:** `bad_op_combo` claimed the unknown-`op` guard on `feedback_log` / `spec_log`, which emit `bad_mode` and never it; `locator_unsupported` glossed `bad_op_combo` as a *format* refusal, which its own row's definition denies; `no_roadmap` and `no_project` both claimed `test_audit_recheck` with an unresolvable `caller_cwd`, which source splits cleanly; `bad_section` and `section_not_found` named each other nowhere, the run's one Q3; and `bad_args` claimed every bad enum while `bad_kind` and its siblings own theirs. **One lane open question resolved clean and is not in the tally**: the row's touched-items scoping reads as unconditional, and is correct here because `commitAndRender` states the scope on all three of its renders. |
| 2 | 2026-08-28 | 3, cold — identical brief, packet unchanged (no source moved); scrubbed copy rebuilt with loop 1's row withheld | 2 | 3 | 2 | n/a | **Seven verified, seven fixed, none dismissed. Four of the seven landed on text loop 1 wrote** — 4a-min's measured pattern, and the reason a second loop is not optional. **All three lanes independently found the same defect**, the run's strongest signal in either loop: loop 1 corrected the ride-along rule to "any line" against `rlNoteDeclaresTrailer`, and the SHIPPED refusal message still says the note's first line — so the document now denied the code's own message with nothing to say which was stale. Both readings are actionable and they prescribe different notes. The row now names the message as the stale side and cites the item filed to fix it, which is what the earlier text could not do because the divergence was invisible until loop 1 created it. **Loop 1's other three pieces of collateral:** the `amend_field` remedy it added is refused by `field_shadowed_by_body` on exactly the migrated-from-file items the gate fires on, and never named `amend_body` as the way out — a documented dead end; its `op_unsupported` remap list omitted `backfill_dates`, the one op whose remap the neighbouring row had just been corrected to describe; and making `roadmap_log`'s unresolvable-`caller_cwd` branch explicit exposed a blanket "prefer `no_project` for these writes" rule two sections away that `roadmap_log` has never followed. **Two pre-existing Q3s, both about a conformer who cannot tell they have breached:** `headline_empty` stated its meaning unscoped while `missing_field` claims the same ground, the scoping living only in an example column; and § Adding a new code's step 2 forbade splitting "for stylistic reasons" while giving no test, where § 6 already carries one — promoted into step 2, with `not_audited` marked as predating it. **One Q1 was a stale count** with a real third emitter: `rg_failed` said two and then said three in the same cell, and `co_change_family` is the one nobody named. **One lane open question resolved clean and is not in the tally**: `public` is the only `visibility` value any item in the store carries, so the row's "public open item" does not under-describe the gate. |
| 3 | 2026-08-28 | 3, cold — identical brief, packet unchanged; scrubbed copy rebuilt after the pre-dispatch sweep | 1 | 3 | 4 | n/a | **Eight verified, six fixed, one dismissed, three filed as the tail (ANTS-4752). Cap reached (3 for a standard), and it is a VIOLENT cap: six of the loop's verified findings landed on text THIS RUN wrote**, each loop repairing the last, with nothing in the evidence suggesting a fourth would stop. Per § At the cap that ends this document's review as it stands; the tail names the split that would make a later gate converge — ~59 KB in ~230 lines, ~56 KB of it in § Categories alone, which is one section doing six jobs. **Loop 2's own repairs supplied most of the damage.** The `bad_args` override it added to exclude named enums was written as a trailing clause that joined the except-list, so it then forbade the combined absent-and-malformed fold that `missing_field` explicitly permits — the fix inverted the rule it was correcting. Its grandfather note named one analogue pair and left four live ones unclassified. Its `op_unsupported` remap list said a new op "picks one" and gave no criterion for which of three. And its `no_project` exception list, having just gained `roadmap_log`, read as closed while `changelog_log` folds the same condition into `no_changelog`. **One pre-existing Q2 was the sharpest**: `migrate_failed` justified folding a concurrent-writer refusal on the grounds that a separate code "would imply a different next step" — which is the trigger for MINTING under the very test loop 2 had promoted three sections away, so the document argued both sides of its own rule. **One finding was dismissed as inert on measurement, not on reading**: a lane found `gate_failures[]` can carry a fold key rather than an id where an item's id is empty, which would be handed to an id-only op — the store holds no item with an empty id, and the lane had itself named that as the test. **And a loop-3 fix was withdrawn before it shipped**: it justified the analogue pairs by a `suggestions[]` vs `candidates[]` payload split, and the emitter shows `target_ambiguous` carries `candidates` with no `suggestions[]` documented anywhere — caught by the execute-what-the-fix-adds step, which is the one rule in the phase that catches a plausible sentence. |
