# Review loop log — `docs/standards/mcp-feedback-files.md`

Kept out of the standard so the rules are not paid for alongside their
review history, matching `docs/reviews/mcp-tools-review-log.md` and its
siblings. A standard is read on every consultation; this is read when
someone asks what a past loop already settled.

**Rows are never edited once landed.** They record what each pass found,
including where a later pass corrected it — a correction goes in the
current row, not by rewriting an old one. That is the whole reason the
log is kept: so a trap already paid for is not re-engaged.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-06 | 3, cold — genre pinned `standard`; first gate ever run on this file, armed by ANTS-4900's derivation-order bullet | 3 | 5 | 1 | n/a | **Nine verified, nine fixed; none dismissed.** **All three lanes independently found the same four**, the strongest agreement available in a three-lane run. **The worst is that the document's only normative statement of WHERE a file lives was the old shared root** — a contributor following it would write into `/mnt/Games/Scripts/Linux/`, one level above the corpus the glob now runs over, producing exactly the stranded second record the same document's new ANTS-4900 bullet exists to prevent. The location rule is now the corpus directory (`claude.mcp_feedback_root`, else the parent of `caller_cwd`), with the literal path demoted to a dated example. **Foreign-id resolution was documented nowhere** — grep for `foreign`/`sibling`/`prefix` returned nothing — while ANTS-3519 has shipped it since July and ANTS-4905 widened it hours before this run: an implementer building from `mapped_id_status`' description alone renders every contributor project's ids `unknown`, which is the whole corpus outside Ants Terminal. **The Tooling table still said `assign_id` was "single-target (batch deferred)"** while the triage section three hundred lines up documents `op:assign_id_batch`, shipped as ANTS-4671 and used four times by this very session. **And the run's own gated edit collided with its neighbour**: the ANTS-4900 bullet said the root's children are "probed first" three lines below the ANTS-4471 sentence saying the config key is "consulted first" — two rules each claiming primacy, forking on precisely the machine that has the key set. That is 4a step 4's case and this commit's own collateral; the guard is now one numbered five-step list. **The best finding came from verifying a lane's open question rather than from a lane**: the document states a reader/compactor version-gate asymmetry (`>= 2` vs exact `== 2`) and calls it "intentional, not drift" — and ANTS-3621 removed it, because the exact gate refused a `: 3` file and pointed the caller at a no-op migration, leaving no way forward. Two lanes read that passage and recorded it as internally consistent. The text now states the agreeing predicates and says what restoring the asymmetry would cost. **Two more Q2s:** "`DOOM_Ants/` is the sole exception" sat three lines under a table marking two rows leaf-mismatch, and neither the nested nor the dot-leaf class was named there — now three classes, listed; and the block-extent rule said a block runs to "the next `#`" where ANTS-3695 makes an H1 a boundary only when it is a session heading or the file title, so a pasted shell transcript would end a finding and the compaction ops delete to that extent. **One Q3:** `assign_id_batch`'s element list carried an `optional note` that appears nowhere else in the document, leaving its rendering to be invented next to a line whose *value* is classified — the rendering is now stated (`- **Note:**` under the id line, never part of the value). **One collateral fix outside the subject**: `mcp-config-keys.md` described the key as a candidate source on a miss, where it also REDIRECTS a derived write; it now says both and defers the order to this file. Three lane open questions settled clean: `scanBoundaries` exists (8 hits), the corpus directory is outside any git repository so the not-tracked claim holds, and the spec-id list is the v2 core rather than a census — now said. |
| 2 | 2026-09-06 | 3, cold — identical brief, packet rebuilt from disk | 3 | 6 | 0 | n/a | **Nine verified, nine fixed. VIOLENT by § At the cap's measure — four of the nine landed on text loop 1 wrote**, and every one of the four was a fix that ADDED an assertion, which is 4a-min's pattern exactly. **All three lanes found the same two.** Loop 1 rewrote the location rule and left the document's OPENING DEFINITION saying the corpus is the glob at the shared root — so the sentence a corpus scanner is built from still named the directory the fix had just moved away from. And loop 1's new location bullet stated the precedence KEY-FIRST forty-five lines above its own numbered order, which states it PARENT-FIRST: the exact fork loop 1 had written that order to remove, reintroduced three lines from where it says so. A third: loop 1 qualified the H1 boundary rule in § The un-triaged delta and left `compact_resolved`'s own copy unqualified — the qualification matters most in the op that WRITES, and that op deletes to the extent it computes. A fourth was loop 1's own imprecision: "nearest wins" among immediate children, which are all equidistant, where the code takes the first hit in iteration order. **Two pre-existing Q2s with teeth**: a third statement of the derivation, at § Default-path derivation, omitted the config-key redirect entirely; and the emoji set was described as the machine-readable values of `feedback_query`'s render, where that render also emits the STRING values `unknown` and `foreign_repo` — the ones the foreign-id path loop 1 documented produces on the normal case, so a consumer building a six-emoji enum fails on exactly it. **One pre-existing Q1 found by verification**: the Tooling table claimed heading retirement uses "the same first-failure vocabulary the finding gate uses (`no_ids` …)", and the two differ — `feedbackfile.cpp` emits `no_ids` for the heading gate and `no_shippable_id` for the finding gate. **And one measured claim replaced**: the document said the corpus is H1-dominant with "MAME and RetroArch doing this repeatedly", which a count of the live corpus refutes — 24 files, 24 H1s, one per file, every one the title, not a single mid-file H1. That claim is what made a contributor's H1 session heading look sanctioned, and an H1 that is neither an ISO date nor the title is body text the finding above it absorbs. **One dismissal**: three lane open questions about `scanBoundaries` resolved clean (it exists, `feedbackfile.cpp:63`). |
| 3 | 2026-09-06 | 3, cold — identical brief, packet rebuilt from disk and its stale derivation window re-cut | 0 | 4 | 4 | n/a | **Eight verified, eight fixed; one dismissed. Cap reached (3 for a standard); the run files its tail and exits. CALM by § At the cap's measure** — one of the eight landed on text this run wrote, against four of nine in loop 2. The document held more defects than the cap held loops, which is the reading that says ship. **Not one Q1**: every defect was two passages disagreeing, or a rule a conformer could not tell they had breached. **All three lanes found the same two.** "Contributor headings are non-structural — the delta parser does NOT read them" is the v1 rule, stated unscoped in a v2 document whose parser enumerates `### ` blocks and terminates them at `## ` — so a contributor told to use whatever heading the file already uses can file a finding that is enumerated by nothing, absent from the delta, absent even from `suspected_untagged[]`. And the H1 title was called **free-form** in § File skeleton while two other passages state a LITERAL boundary predicate: the two shipped tests genuinely differ, `op:set_title` taking the first H1 whatever it reads and the extent scanner matching `# Ants MCP Feedback` case-sensitively — and 2 of the 24 live files carry titles the literal does not match. Both tests are now stated as what they are, with why the divergence costs nothing where it currently falls. **Two Q3s a conformer could not have settled**: `heading_line` inside `assign_id_batch` is resolved against the RUNNING buffer, so a `note` inserted by an earlier assignment shifts every line number below it and `feedback_query`'s numbers are safe only for the first that needs one; and "the derivation root" in step 3 of the resolution order appeared exactly once in the document and was bound nowhere — read as `caller_cwd` rather than its parent, that step never fires and ANTS-4900's failure is back. **Two more**: the marker section told a parser to fall back to CONTENT when the marker is absent, which reads an un-triaged legacy file — no marker, no table — as v2 and drops every finding that predates the `**Proposed ID:**` line; and the `### Re: <heading>` reply form was a MUST stated only in the maintainer's section, with a "mirroring the recheck convention" claim the recheck convention's own example contradicts. **One Q3 filed after three lanes across two loops asked the same question**: `feedback_pending` is gated on the project shipping this standard and the document never said so, so a contributor could not tell why the block never appears for them. **The one dismissal is a PACKET defect, not a document one**: a lane disputed the document's "24 files" against the packet's 23, and the packet was stale — a 24th file arrived mid-session, and a re-count confirms 24 files and 24 H1s. Two lanes also caught the packet shipping a pre-edit copy of the derivation window; re-cut for this loop, and lane C confirmed it byte-identical. **Second share, per § At the cap:** of the 26 verified findings across all three loops, 9 fall inside the span that armed the gate — so two thirds of this run was an audit of text no recent change had touched, and it paid for itself twice over. |
