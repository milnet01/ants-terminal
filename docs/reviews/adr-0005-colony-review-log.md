# Review loop log — `docs/decisions/0005-colony-multi-session-orchestration.md`

Kept out of the ADR so the decision is not read alongside its review
history, matching this project's convention for standards
(`docs/reviews/`). The ADRs in `docs/decisions/` carry no loop log of
their own.

**Rows are never edited once landed.** A correction goes in the current
row, not by rewriting an old one.

**Loop 1's row was written late**, on 2026-09-06, during loop 2's
commit: the command that should have created this file was chained after
a `git rm` that failed, so the whole chain stopped and the file was never
written. Recorded here rather than silently backfilled, because a row
that arrives late is exactly the thing this log exists to make visible.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-06 | 3, cold — genre pinned `adr`; first gate on this document, run before anything was built under it | 4 | 2 | 6 | n/a | **Twelve verified, twelve fixed; none dismissed. Three of the twelve would each have stopped Phase 2 dead.** **All three lanes independently found the same defect**, and it decided the reporting channel: `session_message` addresses a PROJECT, never a session, and ANTS-4887 folds every worktree back into its main checkout — so the orchestrator and all N workers would have shared ONE inbox with no sender identity, in which any worker could read and `ack` another's report. The worker's report moved into the task record, which already identifies task and owner. **Two lanes found `launch`'s `cwd` is validated against the focused project root**, so the sibling worktree D1 prescribes is refused outright — the table had listed launch as a solved enabler. Widening validation to a registered worktree, and NOT via the blanket `allow_outside_root` bypass, became Phase 2's stated content. **One lane found the `raw:true` opt-out** on the control-char filter, in the very bullet arguing the exposure was acceptable. **One found the build lease had no home** (D4's no-migration argument rests on a per-ITEM `extras` column, and a machine-global lease owns no item) and, separately, that **the documented two-builds-at-once failure is produced by git HOOKS, which are not sessions and hold no store connection** — together those moved the lease to an `flock`. **One found D3 and D7 describing two different worker lifecycles.** **One found the post-merge-red path unspecified** under an auto-merge policy. **One found the gate argued for but never named**, which would have made D8's measurement meaningless. **Two found phases 3-5 named nowhere** while D8 made them conditional. **The twelfth was the orchestrator's, found while checking a lane's open question, and it inverted a premise:** the ADR said a worker's roadmap write puts an unrelated diff on its own branch; since ANTS-4887 it re-renders into the ORCHESTRATOR's checkout instead, with nothing on the worker's side showing it happened. Worse than a conflict, and it strengthens D2. **Renamed to Colony in the same pass**, so verbs, skills and the roadmap section share one name from the start. |
| 2 | 2026-09-06 | 3, cold — identical brief, packet rebuilt from disk and extended with the machine facts, the test standard and the derive-side language coverage | 1 | 3 | 7 | n/a | **Eleven verified, eleven fixed. Re-armed by D6, D9 and D10, all added after loop 1 — and the loop found that adding a decision without withdrawing the one it replaced is this document's characteristic failure.** **All three lanes found the same Q1, and two left the packet to prove it against the store's own spec:** "concurrent writers block and retry rather than failing, so no retry logic is owed" is FALSE — ANTS-3756 INV-16 says a write that cannot take the lock within `busy_timeout` fails and reports, never retried silently, and a deferred read-then-write returns `SQLITE_BUSY` WITHOUT honouring the timeout unless it opens `BEGIN IMMEDIATE`. **That sentence came from the orchestrator's own packet**, asserted rather than read, and the ADR repeated it — so the lease claim, heartbeat, report and declaration would each have shipped with no failure branch, and a contended claim returning nothing is indistinguishable from losing the race, which is two workers on one task. **All three found D9's rename refusal contradicting D10**, which exists to permit renames; one showed it was incoherent on its own terms, since D9 says no partitioning scheme detects case 3, so a FILE-overlap test blocks safe pairs while dealing the disjoint-file pairs that are actually dangerous. Withdrawn outright. **Two found the `flock` path was never NAMED** — three phases build its acquirers, `$XDG_STATE_HOME` is commonly unset, and two spellings of one intent exclude nothing, silently, until two `cc1plus` storms meet the earlyoom ceiling. **One found a deadlock that would have shipped:** the orchestrator holding the lock across the gate then pushes, and `pre-push` waits for the lock the orchestrator itself holds, with no timeout. The lock covers the BUILD only now, released before tests and before any push. **One found D5's red-path recovery calling for `git revert -m 1` while D9 rebases rather than merge-commits**, so the revert has no object; it resets to the pre-integration commit, safe precisely because integration is serial. **One found the pre-merge gate's TREE undecided**, with three readings, one of which borrows the main checkout for a branch build while D2 has it rendering `ROADMAP.md` — and ANTS-4839 publishes that render onto whichever branch is checked out. **One found D9 and D10 in no row of the phase table**, so a builder scheduling from it ships a task record with no declaration slot and a merge with no sweep; they are split across phases 1, 3 and 4. **One found `language` scoping contradicting the always-declare list** — a Lua binding's stale references live in `.cpp`, so scoping by source language skips exactly what the entry exists to catch. **Two more:** "lane" was undefined while being one of three auto-merge criteria, and requeue said nothing about the worktree and branch it leaves behind, both keyed on the task id. **One measurement corrected without being filed:** `mainwindow.cpp` is 8,020 lines and `auditdialog.cpp` 6,332, against the 6,162 and 5,749 the ADR carried — both grown by about a third since the 2026-04-27 review the roadmap items still quote, which sharpens the argument they support. **The packet was wrong twice** and both lanes said so: it omitted `launch`'s `allow_outside_root` field and asserted the busy-timeout behaviour it should have windowed. Recorded because a packet defect that survives is indistinguishable from a document defect to the next reader. |
