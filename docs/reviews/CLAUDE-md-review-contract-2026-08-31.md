# `CLAUDE.md` — review-contract loop log

`CLAUDE.md` is loaded into every session on every turn, so its review
history is kept here rather than appended to the document. The document
carries no loop-log section, and its absence is not a finding.

Genre: **standard** (pinned). Subject: `/mnt/Games/Scripts/Linux/Ants_Terminal/CLAUDE.md`.

Trigger: global `CLAUDE.md` rule 14 — ANTS-4759 added a paragraph to
§ Versioning & release telling a conformer to author a CHANGELOG summary
rather than reach for `changelog_log op:"add_from_roadmap"`. That changes
what a conformer writes, so the test comes out **yes** and the gate is owed.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-08-31 | 3, cold — genre pinned `standard`, packet 27.7 KB carrying the ANTS-4759 measurement, `releases.md` § 2 verbatim, ANTS-1868's resolution, and windows on the gate script, `cut-rc.sh` and `commits.md` | 0 | 3 | 1 | n/a | **Four verified, four fixed; two dismissed as immaterial. One Q1 caught at Phase 1b, before a lane was spent.** **All three lanes independently found the same Q2**, the strongest signal available here: § Versioning & release opened with *"Every bump touches `CMakeLists.txt`, `CHANGELOG.md` (new dated Keep-a-Changelog section)"* while a passage 43 lines below reads **"Never leave the bump to author a CHANGELOG section — `new-rc` owns that roll"**, and a third says `promote` does the dating. A session running `cut-release --bump-only` would hand-author a dated heading, which is exactly the drifted state `new-rc` hard-refuses and `promote` then double-dates. Pre-existing, and invisible to the author because the two passages sit 43 lines apart in one section. **Two lanes found the gated paragraph's own collateral**: `Completed ROADMAP.md items migrate to CHANGELOG` sits 54 lines *above* the new rule, and *migrate* is precisely the verbatim copy the new paragraph exists to stop — 4a-min's *"name the sentence this fix makes wrong"* was answered "none" when writing it, and this is what that costs. **Two lanes found the new rule contradicting its own carve-out**: the bold line forbade copying *the* roadmap headline outright while the sentence below kept `add_from_roadmap` for a headline that already reads as delivered — and *"prefer `op:add` for any defect item"* under-covered the measurement, since **6 of the 14 were `enhancement`**, not `fix`. Re-scoped to *a headline that states a PROBLEM*, which is the boundary the evidence actually supports. **One lane found a Q3 nobody else did**: the sub-spec roster omits `ci-build.md`, `menus.md`, `config-hot-reload.md` and `roadmap-data-model.md`, so a conformer adding a `QMenu` follows `dialogs.md`, never opens `menus.md`, and cannot tell they breached it — this preamble being the only always-loaded roster. Fixed by naming `docs/standards/README.md` as the index rather than by listing four more. **The Q1 was the orchestrator's own**: building the packet meant counting the commits behind the 14 bullets, and the figure written into four places was *six* where `git log -S` per id gives **seven**. Corrected before dispatch. **Two dismissed as true-but-immaterial**, both recorded: *"All ten corpus files are migrated"* (there are 21, and one declares an undefined `: 4`) — but `feedback_query` is version-aware and reads each file's own marker, so no session acts on that count; and the unmirrored `releases.md § 2` citation, which the document's own § Project standards already cites the same way throughout. **Three open questions resolved clean and are not in the tally**: 0 of 2394 stored headlines are multi-line, so `rcHeadlineOneline` folding cannot defeat the byte-identical compare; no hook or CI job gates on the script's exit, so *advisory* is true; and the uncovered-items half still reaches its own `exit 1`, verified by run. |
| 2 | 2026-08-31 | 3, cold — identical brief, packet rebuilt from disk after loop 1's fixes | 1 | 3 | 1 | n/a | **Five verified, five fixed (one in a neighbouring document); three dismissed or routed. Three of the five were loop 1's own fixes** — the documented pattern, and the largest class again. **All three lanes independently found the same one**: loop 1 repaired the section-opening line to *"never a verbatim headline copy"* — an ABSOLUTE — while the new paragraph 66 lines below carves out `add_from_roadmap` for a headline that already reads as delivered. Loop 1 had scoped the bold rule and left its own restatement unscoped, which is the fix-one-copy failure inside one section. **Repaired structurally rather than reconciled**: the opening line is now a pure POINTER carrying no scope claim of its own, so there is one statement of the rule in the document instead of two that can drift. That is 4b's delete-N−1, and a sweep afterwards found no other statement of the rule anywhere in the repo. **Two lanes found the tool sentence overclaiming, from opposite ends.** One: *"both are advisory"* is true of `cut-rc.sh` only — the script itself now exits non-zero, so a conformer wiring the required bump-time run into a `set -e` chain, on this document's word, gets the bump aborted. Two: the document said the script reports *"the copies"* when it compares byte-identically, so a lightly reworded problem-headline breaches the rule, gets a clean run, and ships the bug under `### Fixed`. Both stated now. **One lane found a pre-existing Q2 loop 1's three lanes missed**: *"Project sub-specs (no global counterpart exists for any of these)"* against `dependencies.md`'s own entry calling itself *"the project-side companion to global `standards/dependencies.md`"* — a conformer writing a version-policy rule puts it in the wrong document, where no other project can see it. **One lane found loop 1's other fix over-broad**: *"every file in that directory binds you"* against `mcp-errors.md`, which the same document retires as superseded. **Routed to a neighbouring document and fixed there**: `docs/standards/commits.md`'s delta half still described the gate script as asking one question. **Surfaced, not fixed**: that file's mirror half carries 9 links a public reader cannot follow (`releases.md` ×5, `local-gate.md` ×4) because neither sibling is mirrored into this project — 8 of the 9 predate this session, the mirror half may not be hand-edited, and which standards get mirrored is a design decision. **Packet defects, mine, all three lanes flagging the first**: fact F2 was headed *"the six commits"* over a body correctly saying SEVEN, and the subject header said 577 lines against 584 on disk after loop 1. |
| 3 | 2026-08-31 | 3, cold — identical brief, packet rebuilt from disk with loop 2's two packet defects repaired (F2's heading, the subject line count) | 1 | 3 | 1 | n/a | **Five verified, five fixed. CAP REACHED (3 for a standard); the run files its tail and exits. A CALM cap — 2 of the 5 landed on text this run wrote (40%), so the document held more defects than the cap held loops, and shipping is right rather than the run oscillating.** **The Q1 is the sharpest finding of the whole gate, and it is loop 2's own fix**: loop 2 repaired the sub-spec roster by pointing conformers at `docs/standards/README.md` as *"the index"* — and that README omits **seven** of the 24 files, **four of which (`ci-build.md`, `config-hot-reload.md`, `menus.md`, `roadmap-data-model.md`) are named in no document at all**. The fix moved the roster to a file that is not one, which is strictly worse than the gap it replaced: a conformer now follows an explicit pointer to a document that will tell them nothing governs. CLAUDE.md now names the DIRECTORY as the roster — true today and true as files land — and the README half is filed as ANTS-4762 with a suggested parity check, since nothing reports an unindexed standard. **One lane found loop 1's fix over-generalised**: *"the `CHANGELOG.md` section is rolled by `new-rc` … never by the bump"* against the passage 46 lines below telling you to run the coverage gate at bump time *because afterwards a missing entry lands in a closed section* — which only makes sense if the bump DOES author bullets. The word *section* could not separate them, both passages using it for `[Unreleased]` itself. Split into the **version heading** (never the bump's) and the **`[Unreleased]` bullets** (authored at bump time), in both places. **One lane found a route the rule never named**: `changelog_log op:"add_batch"` auto-detects an id-only entry to `add_from_roadmap`, so the entry's SHAPE decides rather than an op you chose — a conformer batching completed items emits exactly the banned copies while believing the rule reaches only an op they did not call. **One lane found the gate over-trusted**: it flags every byte-identical copy including the ones this rule PERMITS, and the paragraph named only the opposite blind spot, so the list read as complete — a conformer correctly keeping `add_from_roadmap` turns the gate red every cycle and is cued to reword a bullet the rule tells them to keep. Now stated as *reports byte-identity, not the rule; review each hit rather than rewording on sight.* **Two lanes disagreed on one passage and the disagreement was itself informative**: one filed the section-opening deferral as still contradictory, the other checked it and explicitly declined to file. Taken anyway at one word's cost. **Three open questions resolved clean**: `op:"add"` drops no required element (the Layman line is a ROADMAP field, not a CHANGELOG one); `rcHeadlineOneline` folding cannot defeat the compare (0 of 2394 headlines are multi-line — settled for the second time, loop 1 having settled it first); and the quoted `### Fixed` string is absent from CHANGELOG.md **because this run fixed it**, now marked as the pre-fix state. |

**Outcome: converged-at-cap, calm.** Three loops, 14 verified findings, 14
fixed (one in a neighbouring document). Four items filed rather than fixed:
ANTS-4760, ANTS-4761, ANTS-4762, and the 13 shipped-but-uncited items the
coverage gate's other half reports. Per `review-contract` § At the cap, a
later run on this document is ordinary rather than wasteful.

**Size signal, per § At the cap.** The subject is 596 lines and covers build
flags, test harnesses, MCP authoring, release mechanics and design decisions
in one always-loaded file. Every loop found pre-existing contradictions
between passages 40–70 lines apart in the same section — the shape a document
gets when it is too large for one cold read to hold. Worth considering a
split; not acted on here.

---

## Second run — 2026-09-21 (ANTS-5274)

Genre: **standard** (pinned). Subject unchanged. Cap 3.

Trigger: global `CLAUDE.md` rule 14 — commit `7902f8f5` added
§ "Hot reload is the design default (user standing rule, 2026-09-21)",
a new standing rule stating what every new feature must say about
reaching a running terminal. That changes what a conformer writes, so
the test comes out **yes**. Row numbers continue this document's
history; the first run closed at row 3.

Phases 1a–1c were run by the authoring session and recorded in
ANTS-5274; 1d onward were run fresh, deliberately, because that session
held exactly what the cold brief withholds.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 4 | 2026-09-21 | 3, cold — genre pinned `standard`; packet ~14 KB carrying windows on `Pty::~Pty`, `sessionmanager.cpp`, the `tools/list` branch and the `TabSpecific` dispatch table, the drive-level `/mnt/Games/CLAUDE.md` § Writing and Editing Documents verbatim, and ANTS-4932's body | 2 | 1 | 1 | n/a | **Four verified, four fixed; one dismissed — and the dismissal is the most useful thing in the loop.** 1d used the project's own `doc_lint` (`doc_integrity`, `doc_citations`, `doc_dedup`, `doc_symbols`) rather than `check-doc-facts`: 0 findings, 18 unresolved symbols all external. **Classes it does not cover, so still open: verbatim quotations of other documents, version-string drift, and census counts.** The last of those is where three of the four findings landed. **All three lanes independently found the same Q1**, the strongest signal available: § Hot reload attributed the teardown to `` `PtyHandler` ``, which is the FILE STEM, not a symbol — `find_definition` over 1159 files returns zero and hints at `src/ptyhandler.cpp`, whose class is `Pty`. The rule's entire cost justification rests on that sentence, so a conformer checking it searches for a class that does not exist. Caught at Phase 1d by `doc_symbols` and deliberately NOT dismissed as external noise, which is what the other 17 were. **All three lanes also found the count bullet**: *"~13 MCP verbs need tab or terminal state; ~80 are file and sqlite work"* is grounded in nothing — the tree ships **95** verbs (`tools/check-readme-claims.sh`), the only formal classification is `CallerCwdContract::TabSpecific` with **seven** members, and ANTS-4932 measures the seam as **eleven `MainWindow` methods**, a different unit. 13 + 80 = 93. **Two lanes found the Q3 beside it**: the bullet orders a conformer to keep the surface *named* and names nothing — no register, no list, no file — so the breach is invisible to the conformer themselves, which is this genre's Q3 test exactly. **One lane found the Q2**: the census breaches the drive-level `/mnt/Games/CLAUDE.md` rule *"No counts, line numbers, or sizes … Name the thing instead"*, which binds here — precedence ruled the same day as CFG-0435 (deeper file wins on conflict, outer binds otherwise) after this run put the question to the session that owns that file. **4a step 3 earned its keep and changed the fix.** The obvious repair — "a verb needing tab state is classified `TabSpecific`" — was drafted, then the refuting case was run rather than the confirming one: all six non-`TabSpecific` `MainWindow` methods (`tabListForRemote`, `tabsAsJson`, `newTabForRemote`, `selectTabForRemote`, `setTabTitleForRemote`, `tokenSavingsSummary`) have callers in the remotecontrol layer, so verbs outside the seven DO couple to the window and the drafted sentence would have shipped false. Fixed as **two registers with distinct meanings** — the table for tab state, the eleven methods for any window dependency — which is what the evidence supports. **THE DISMISSAL: the packet was wrong and two lanes caught it.** Packet fact 9 reported no re-read path for `config.json` or `audit_rules.json`, from a single negative search, and flagged itself as the weakest fact there. Lane 2 spent its one permitted lookup disputing it and found `QFileSystemWatcher *m_configWatcher` at `src/mainwindow.h:658`; lane 3 independently found `audit_rules.json` loaded per run at `src/auditdialog.cpp:916`. Verified after: the watcher is constructed at `src/mainwindow.cpp:1089`, watches the config location and is connected to `MainWindow::onConfigFileChanged`. So the document's *"the cases that already work this way"* is **true**, lane 1's finding against it is dismissed as unverified, and the defect was the orchestrator's packet — which is the brief's *"a lane finding that contradicts the packet is evidence against the PACKET first"* working as designed. **Three open questions resolved clean and are not in the tally**: the `workstation` preset's `-j2` and `-j3` are two correctly-stated different quantities (build `jobs: 3`, test `execution.jobs: 2` in `CMakePresets.json`), and the 680-vs-681 line-count split between lanes is a trailing-newline artefact that moved no citation. **Filed rather than fixed**: ANTS-5276, the same wrong `PtyHandler` symbol in `docs/specs/ANTS-1897.md` (three occurrences, one a grep recipe that matches nothing as written) and `docs/specs/ANTS-1113.md` — neighbouring gated documents, so not repaired in passing. |

### Disclosure — this loop did not get a cold read, and says so

**All three lanes arrived holding the unscrubbed subject.** The document
is a project `CLAUDE.md`, so the harness injects its full text into every
subagent as project instructions before dispatch. Phase 2's scrubbed copy
and the lane-side `exclude_glob` defend the *path* to the bytes; the
harness delivers the bytes directly. Lane 2 passed the exclusion
correctly, said so, and it bought nothing.

All three also arrived holding the git snapshot naming both arming
commits — including `7902f8f5 CLAUDE.md: hot reload is the design
default` — and an auto-memory index naming the standing rule itself.

**What did hold: nothing in any lane's context named a previous review of
this document or its findings.** 3 of 3. The withholding of review
history worked; the withholding of the subject is inert for this genre.

Reported to the session owning `~/.claude`, pooled with six lanes from a
second project — 9 of 9 across two projects — and folded into the shared
cold-reader-contamination reference as CFG-0444.
