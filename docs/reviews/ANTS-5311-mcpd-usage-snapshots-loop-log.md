# ANTS-5311 — loop log

Rows for `docs/specs/ANTS-5311-mcpd-usage-snapshots.md`. Written by
`review-contract` as each loop closes; never back-filled.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-26 | 3, every lane held Q1–Q4 | 4 | 4 | 2 | 1 | Verified 11, fixed 11, dismissed 0. One found building the packet: a private constant named from the engine. Central, all three lanes independently: per-tool flooring made merged peer totals differ from per-file totals, so a fold would move the display; redesigned to per-snapshot figures with a separate `mcpd` block in token_usage, leaving ANTS-1284's total_saved identity intact. Also: SIGTERM path, O_CLOEXEC lock, lone-lock age rule, write cadence, second initialize, watcher on a missing dir, INV-8 scope, fail-first exemption for two regression guards. Four came from lanes' open questions, verified here. Disclosure: lanes arrived holding the three CLAUDE.md files and a git snapshot naming this spec as untracked; none held its review history. |
| 2 | 2026-09-26 | 3, every lane held Q1–Q4 | 1 | 3 | 1 | 0 | Verified 5, fixed 5, dismissed 2 (a wording slip naming `initialize` a method, and an unstamped `since` date nothing but the fold reads; neither changes what is built). All three lanes independently: the writer's accumulator could exceed the reader's 64-root cap and lose roots silently; now capped at the writer and rejected whole at the reader. Also: ANTS-3572 INV-8's no-file-watcher clause vs the chip watcher (amended in § 7); ANTS-1284 INV-6's response schema that does not exist in source; and two found verifying open questions — the terminal no longer receives `initialize`, so a peer-only fold runs on watcher events; and once-only claiming across two terminal processes (INV-10). Cap reached (2 for a spec); shipped to implementation. Own-fix share: 2 of 5 landed on loop 1's text; the armed change is the whole new document, so the share is unreadable by construction and no second share exists. By substance a calm cap: exposed consequences of the design, not repairs of repairs. Disclosure: lanes' git snapshot named loop 1's commit and its finding count. |
