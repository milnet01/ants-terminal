# ANTS-4500 — cold-eyes loop log

## Cold-eyes loop log

| Loop | Date | Model | Lanes | Q1 | Q2 | Q3 | Q4 | Verified | Fixed | Outcome |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 2026-09-20 | Opus 5 | 3 | 3 | 0 | 2 | 0 | 5 | 5 | Every finding was the author's own draft defect; none pre-existed. All three lanes independently found that `rcIsNonconformingIdToken` is a diagnostic and the addressability gate is `RoadmapParse::idTokenPattern` — § 4.4 rewritten. The synthesis counter had one witness where the real prefix has two; § 4.2 now pins a two-term floor and names the accessor it needs. `idPrefixFor()` orders by `high_water DESC`, so on a wholly-synthesised project the `-S` row would become the project prefix — verified in source, § 4.2 excludes it, INV-7 added. INV-8 added for the parser gate. 1 dismissed: an immaterial example the lane self-flagged. |
| 2 | 2026-09-20 | Opus 5 | 3 | 1 | 3 | 2 | 1 | 7 | 7 | VIOLENT CAP. 5 of 7 findings landed on loop-1 text. Loop 1's two-term floor was still store-only and so did not cover the file-ahead case it justified itself with; it is now three terms, the third plan-side. The -S counter KEY is redesigned: keying on '<prefix>-S' and excluding by suffix hid the real counter of a project whose declared prefix legitimately ends -S, and left a wholly-synthesised project with no real-prefix row at all. It now keys on '<prefix>#S' - '#' cannot appear in a declared prefix ([A-Za-z][A-Za-z0-9_-]{0,63}, src/main.cpp) - so the exclusion is structural, and migration ensures the real-prefix row. INV-3 was unfalsifiable: re-migrating an unchanged source re-matches every bullet and allocates nothing. Cap reached; routed to implementation. |
