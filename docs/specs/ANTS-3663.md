# ANTS-3663 — `doc_lint`: every deterministic doc check in one call

**Status:** spec draft, cold-eyes loop 1 folded (2026-07-27).
**Kind:** enhancement.
**Source:** ROADMAP.md ANTS-3663 (user-request-2026-07-27).
**Blocked by:** ANTS-3664 (the finding shape), and ANTS-3660 / ANTS-3661 /
ANTS-3662 existing to be composed.
**Composes with:** ANTS-3601 (`doc_integrity`) and ANTS-3636 (`doc_citations`),
the two producers that already ship and are adapted rather than rewritten.

**Layman:** One button that runs all the document checks together, and repairs
the handful it can repair without guessing.

---

## 1. Problem

A `/cold-eyes` § 1e pre-pass currently means five or six separate verb calls
over the same tree. Each re-walks the same directory, re-reads the same files,
re-computes the same fence masks, and returns a differently-shaped envelope the
caller then reconciles by hand. **That reconciliation is the actual cost**, and
it is paid on every loop of every review.

Two consequences, and the second is the one that bites:

1. **Six walks where one would do.** `doc_integrity` and the three new verbs all
   enumerate `*.md` under the same root with the same `QDirIterator` (cloned from
   `RemoteControl::docIntegrityEnumerate`, `src/remotecontrol.cpp:12317`), and
   each re-reads every document.
2. **Six vocabularies.** Without ANTS-3664 they would report a problem six
   different ways, and the caller — a skill, not a program — would flatten them
   into prose to compare, which is what `ColdEyesEngine::BriefManifest`
   (`src/coldeyesengine.h:143`) already does by discarding structure. ANTS-3664
   removes that for the three new verbs; this spec removes it for the two that
   ship, by adapting them.

## 2. Surface

### 2.1 One walk, five checkers

`doc_lint` enumerates once, reads each document once, and passes the text to
each enabled checker.

```jsonc
// request
{ "caller_cwd": "…",          // required
  "path": "docs/",            // file or dir; absent → the docs_dir default,
                              //   matching doc_integrity, NOT doc_citations'
                              //   missing_field (that verb is file-scoped)
  "checks": ["doc_integrity"],// default: all five; unknown name → bad_args
  "fix": false,               // opt-in; §2.3
  "max_findings": 500 }       // cap; truncated:true past it
```

The five legal `checks[]` values are exactly the `verb` strings ANTS-3664's
findings carry — one vocabulary, not two:

| `checks[]` value = `verb` | Engine |
|---|---|
| `doc_integrity` | `DocIntegrity::check` (adapter) |
| `doc_citations` | `DocCitations::check` (adapter) |
| `doc_dedup` | ANTS-3660 |
| `doc_symbols` | ANTS-3661 |
| `spec_lint` | ANTS-3662 |

Response: `findings[]` as `DocFinding::Finding` in the § 3 INV-7 order,
`counts` from `DocFinding::countsByVerbAndKind` (whole-run, before the cap),
`checked_docs[]`, `checks_run[]` — which distinguishes "check ran clean" from
"check not run", a distinction `counts` alone cannot express — and per-check
`counts` only.

**Not** the full per-check envelopes. The first draft promised them and it was
wrong twice: `doc_citations`' envelope carries `text[]` for every healthy
citation, thousands corpus-wide on a verb whose premise is frugality; and that
verb is *file*-scoped, so "the envelope it would have returned alone" is N
envelopes with N copies of every top-level field. A caller wanting the full
shape calls the verb directly — they remain independently callable (§ 5). The
one structured payload that survives is `doc_dedup`'s `pairs[]`, because
`DocFinding::Finding` has no second-location field (ANTS-3660 § 2.5).

### 2.2 The two adapters

`doc_integrity` and `doc_citations` ship with tested envelopes and live callers,
so **neither is rewritten**. Each gains a pure adapter in the verb layer:

| Source | → `DocFinding::Finding` |
|---|---|
| `DocIntegrity::Finding` | `kind` from its `Kind` enum (`dead_anchor` / `broken_link` / `toc_gap`); `file`, `line`, `message` map straight across |
| a non-`ok` `doc_citations` citation | `kind` from `status` (`missing_file`, `out_of_range`, `ambiguous`, `read_error`, `unresolved`); `line` from `doc_line`; `message` composed from `raw` and the status |

Two rules the adapters obey, both load-bearing:

- **An `ok` citation is never a finding**, including one whose
  `anchor_found:false` (ANTS-3654). That flag is *advisory* — it says a needle
  moved, not that the citation is wrong — and promoting it to a finding would
  contradict the parent spec, where it is explicitly not a status.
- **`unparsed[]` entries are not findings.** ANTS-3653's design contains them:
  a prose token that happens to parse (`6.2:1`, `counts.missing_file:3`)
  resolves nowhere and lands there precisely so it never reaches `ok`. They are
  contained noise, and ANTS-3659 § 5 scopes them out by name. Surfacing them
  here would re-import the exact drowning problem that spec was written to fix.

**This deliberately diverges from `only:"stale"`, and the divergence is the
point.** ANTS-3654 INV-14 has that filter *keep* an `ok` citation whose
`anchor_found` is false — it is a **view** over citations, built for a reader
scanning drift. `findings[]` is a list of **defects**, and an advisory flag
saying a needle moved is not one; ANTS-3654 § 2 is explicit that `anchor_found`
is never a status. Two different questions, two different answers, and a caller
wanting the drift view calls `doc_citations` with `only:"stale"` directly.

**Reading convention.** Adapters and consumers read an absent `auto_fixable` as
false and a present `line:0` as document scope — the two conventions ANTS-3664
INV-1 fixes. The `doc_integrity` adapter sets `autoFixable` from a small
per-adapter kind list (today: `toc_gap` alone), because that struct predates the
flag; ANTS-3664 § 2.1 records why that is accepted rather than pushed upstream.

### 2.3 The fix half stays narrow, by design

Auto-fixable is **exactly one kind**:

- **`toc_gap`** — a hand-maintained table of contents is regenerable from the
  document's own headings. There is one correct answer and the document already
  contains it.

The ROADMAP bullet also named a version string disagreeing with
`project(... VERSION X.Y.Z)` in `CMakeLists.txt`. **It is dropped, because no
checker in the composed set produces such a finding**: `DocIntegrity::Kind` is
`{DeadAnchor, BrokenLink, TocGap}`, `doc_citations` emits citation statuses, and
none of ANTS-3660/3661/3662 checks versions. A fix half for a finding nobody
emits is untestable by construction — the first draft's INV-5 demanded a fixture
no checker could generate. If version drift is wanted it needs a producer first,
filed on its own.

**Everything else is report-only by design, not by omission**, and each refusal
has a reason rather than a shrug:

- deleting one of two duplicated facts needs a decision about which is
  canonical, and the wrong choice destroys the authoritative copy (ANTS-3660
  § 2.4);
- an unresolved symbol may be a forward reference to something the doc is about
  to create, which the verb must not adjudicate (ANTS-3661 § 2.3);
- renumbering an `INV-N` breaks every citation of it, which `specs.md` § 5.5
  forbids and `/cold-eyes` Phase 4 forbids again;
- a missing section needs prose only an author can write.

`--fix` is opt-in per call, never the default, and applies only to findings
whose producer set `autoFixable`. A run with `fix:true` reports what it changed
as `fixed[]`, so the caller never has to diff to find out.

### 2.4 Partial failure

One walk, five checkers, and they do not agree today on what an unreadable
document means: `DocIntegrity::check` silently excludes it, while
`DocCitations::check` refuses `read_failed`. Under one walk that collision needs
a decision, so:

- **A document that cannot be read or decoded is excluded from
  `checked_docs[]`, listed in `skipped[]` with a reason, and the run
  continues.** One bad file in a tree must not fail a whole review pass.
- **A fix that fails to write** (read-only file, permissions) appends to
  `fix_errors[]` and leaves `ok:true`. INV-5's byte-identical assertion would
  otherwise pass a silently-failed write, which is the worst of both.

### 2.5 Files and hooks

Engine `src/doclint.{h,cpp}` in `ants_core_lib`, calling the five engines.
Verb layer mirrors `doc_integrity`: `cmdDocLint` plus `docLintEnumerate` and
`docLintBuildResponse`, and the registration set ANTS-3661 § 2.4 enumerates —
one `registerToolProvider` plus five `src/claudeintegration.cpp` hooks. Note
`doc_integrity` ships with **no** `selection_hint`; a five-checks-in-one verb is
the strongest candidate in this lane for one, so `doc_lint` adds it, making six
hooks rather than five.

## 3. Invariants

- **INV-1** — One walk, one read per **enumerated document**, whatever the check
  set. *Test:* a `Probe` counter over a three-document tree with all checks on →
  three document opens, not fifteen. The probe counts opens of enumerated
  documents only: `doc_citations` also opens the *targets* a citation points at,
  which are neither documents nor deduplicable, and a naive open-counter would
  fail a correct implementation the moment a fixture contained one citation.
  Target reads keep their own budget (ANTS-3636's `maxTargetReads`) and are out
  of this invariant's scope.
- **INV-2** — `checks[]` narrows findings *and* counts together; an unknown name
  refuses `bad_args`. *Test:* a fixture producing findings from two checkers →
  `checks:["doc_integrity"]` returns only those, with `counts` matching; plus
  `checks:["nope"]` → `bad_args`.
- **INV-3** — An `ok` citation is never a finding, including one with
  `anchor_found:false`. *Test:* a doc with a healthy citation, an
  anchor-missing-but-`ok` one, and a `missing_file` one → exactly one finding.
  The middle row is the one that fails against an adapter treating the advisory
  flag as a status.
- **INV-4** — `unparsed[]` entries never become findings. *Test:* a doc whose
  only citation-shaped token is `6.2:1` → zero findings, and the `doc_citations`
  sub-envelope still reporting `unparsed_total:1`. Without this row the verb
  re-imports ANTS-3659's problem.
- **INV-5** — `fix:true` changes only `autoFixable` findings, and `fixed[]`
  names each. *Test:* a tree with a stale TOC, a near-duplicate pair and an
  unresolved symbol → `fixed[]` of length one, and **the other two files
  byte-identical afterwards**. The byte-identical half is the assertion: a
  `fixed[]` of the right length proves nothing about what was left alone.
- **INV-6** — Default is report-only: without `fix`, no file is written.
  *Test:* the INV-5 tree run without the flag → every file byte-identical, and
  a source-scrape confirming the write path is reachable only under the flag.
- **INV-7** — Findings are in a **total** order: `file`, then `line`, then
  `verb`, then `kind`, then `message`. *Test:* a tree producing findings from
  three checkers on one file, **including two findings from the same verb on the
  same line** (two dead links in one line, or two citations sharing a
  `doc_line`), → the stated order, byte-identical across two runs. The
  same-verb-same-line row is the point: `(file, line, verb)` alone is not total,
  both `doc_integrity` and `doc_citations` routinely emit several findings per
  line, and an unstable tail makes every caller's diff noise.
- **INV-8** — The verb-contract minimum: no `caller_cwd` →
  `caller_cwd_required`; root-escaping `path` → `bad_path`; well-formed
  non-existent in-root `path` → `ok:true` with empty findings. *Test:* three
  calls.

## 4. RAM / build cost

Peak is one document's text plus the union of the five checkers' per-document
state, held while that document is processed and dropped before the next — the
saving over five separate calls is the re-reads, not the peak.

The exception is `doc_dedup`, which is corpus-scoped by nature: its inverted
shingle index lives for the whole run (ANTS-3660 § 4). So `doc_lint`'s peak is
`doc_dedup`'s index plus one document, and disabling that one check via
`checks[]` returns the run to per-document cost. Stated because a caller
watching memory needs to know which check is the expensive one.

Caps are inherited, not re-invented: `maxDocsPerRun` and `maxDocBytes` from
`DocIntegrity::Options`, `maxPassages` and `maxPostings` from ANTS-3660,
`maxSymbolsPerRun` from ANTS-3661. `doc_lint` adds exactly one of its own —
`max_findings`, with `truncated:true` past it — because `findings[]` is the one
thing it produces that none of the five bounds, and ANTS-3636's discipline is
that every truncation sets a flag. `counts` is computed before the cap, so a
truncated response still reports the true totals.

One new translation unit; one new feature-test source into an existing bundle.

## 5. Out of scope

- **Any fix beyond the two in § 2.3.** The narrowness is the design.
- **Replacing the five verbs.** Each stays independently callable; this is a
  convenience and a shared walk, not a deprecation.
- **`AuditEngine` / `DebtSweep` findings.** Different lane, different consumer,
  and ANTS-3664 § 5 draws that boundary.
- **Running an LLM over anything.** Every check here is deterministic; the
  judgement calls are what the reviewer lanes are for.

## 6. Tests

`tests/features/doc_lint/` (engine, seeded temp trees — this verb is
filesystem-shaped by nature) and `tests/features/doc_lint_verb/` (hooks +
refusals). Label `features;fast`.

| INV | Covered by |
|---|---|
| 1 | `DocLint.Inv1OneReadPerDocument` |
| 2 | `DocLintVerb.Inv2CheckFilterAndUnknownName` |
| 3 | `DocLint.Inv3OkCitationIsNeverAFinding` |
| 4 | `DocLint.Inv4UnparsedIsNeverAFinding` |
| 5 | `DocLint.Inv5FixTouchesOnlyAutoFixable` |
| 6 | `DocLint.Inv6DefaultWritesNothing` |
| 7 | `DocLint.Inv7TotalDocumentOrder` |
| 8 | `DocLintVerb.Inv8RefusalMinimums` |

**Verified RED against feature-absent code before the implementation lands.**
INV-3, INV-4 and INV-6 assert absences and pass trivially against an engine
producing nothing; re-proven by mutation — promote `anchor_found:false` to a
finding, adapt `unparsed[]` into findings, and make the write path
unconditional — recording what each actually turns red.

## 7. Cross-doc impact

- **ANTS-3664, ANTS-3660, ANTS-3661, ANTS-3662** are blockers.
- **`doc_integrity` and `doc_citations`** gain adapters only; ANTS-3601's and
  ANTS-3636's own contracts, envelopes and tests are untouched.
- **`/cold-eyes` § 1e** can collapse its per-verb calls to one — **but the
  caveat is load-bearing.** ANTS-1581 settled that the skill is global and these
  verbs are not, so § 1e may name `doc_lint` only alongside a stated hand-rolled
  fallback. Without that sentence every non-Ants project regresses, which is the
  failure this whole family has to keep avoiding.
- **`docs/subsystems.md`** — `src/doclint.{h,cpp}` joins `ants_core_lib`.
- **`docs/standards/mcp-tools.md`** — no change; this follows the checklist.

## Cold-eyes loop log

| Loop | Date | Lanes | C | H | M | L | I | Outcome |
|---|---|---|---|---|---|---|---|---|
| 1 | 2026-07-27 | foundation+compositor depth; cross-doc (joint batch review of ANTS-3660/61/62/63/64) | 1 | 5 | 4 | 2 | 0 | 12 verified, all fixed. CRITICAL: one of the two auto-fixable kinds — a drifted version string — has **no producer** anywhere in the composed set; it was carried over from the ROADMAP bullet without checking, and INV-5's test demanded a fixture no checker could generate. Dropped, leaving `toc_gap` as the sole auto-fix. Four more that would have shipped wrong: INV-1's open-counter would have **failed a correct implementation**, because `doc_citations` opens cited *targets* as well as documents; INV-7's `(file, line, verb)` ordering key is not total, and both adapted verbs routinely emit several findings per line; the promised per-check envelopes contradicted § 4's memory claim and are undefined for a file-scoped verb, so they are replaced by per-check counts; and `checks[]` had no enumerated value set. Added a request/response schema block, a partial-failure contract (§ 2.4), and `max_findings`. |
