# ANTS-3636 — `doc_citations`: resolve a doc's `path:line` citations and return the cited text

**Status:** spec draft, cold-eyes loop 1 folded (2026-07-26).
**Kind:** implement.
**Source:** ROADMAP.md ANTS-3636 (OneUp_Ants_MCP_Feedback.md, 2026-07-26 addendum).
**Composes with:** ANTS-3601 (`doc_integrity` — shares the fence primitives),
ANTS-1637 (`codebase_index` — the basename→path map).
**Pairs with:** ANTS-3596 (hand-hardened `~:line` citations in
`docs/specs/ANTS-2161.md` — the manual fix this verb generalises).
**Blocker for:** ANTS-3642 (request-side `expect` map), ANTS-3643 (corpus-wide
digest mode).

**Layman:** Design docs are full of "see `file.cpp`, line 200" pointers. Code
moves; the pointers rot silently. This adds one call that looks up every such
pointer in a document and shows what is actually on those lines today.

---

## 1. Problem

Global rule 13 requires every doc claim naming a file, line, function, or
constant to be checked against live source rather than recalled. `/cold-eyes`
runs that check on every loop, for every reviewer, on every doc. There is no
verb for it, so it is done by hand or skipped — and when it is skipped, the
loop ratifies a stale line number and the next reader is sent to the wrong
place.

The reporting session (OneUp) hit this directly: verifying one spec meant
resolving 33 `updater.py:NNN` citations, and they wrote a throwaway Python
script because no verb existed. `read_regions` is the closest fit but takes a
hand-extracted list of `{path, start_line, end_line}` — and extracting that
list *is* the tedious part.

**The corpus is large enough that hand-verification does not scale.** Measured
2026-07-26 over `docs/`. The rows use slightly different regexes, so each names
its own denominator rather than implying one shared total:

| Shape | Count | Of what |
|---|---:|---|
| `path.ext:N` citations (extension-restricted scan) | ~1,872 in 167 docs | — |
| …carrying a directory component (`src/foo.cpp:12`) | 537 | ~29% of the above |
| …**bare basename** (`foo.cpp:12`) | remainder | **~71% of the above** |
| Line **ranges** (`:208-228` or en-dash `:208–228`) | 668 | ~36% of the above |
| Multi-locus / open-ended (`:262/265/286`, `:5669+`) | 39 | ~2% of the above |
| Approximate (`:~197`, ANTS-3596's hand fix) | 10 | <1% of the above |
| Citations opening a code span (`` `foo.cpp:12` ``) | 1,830 | own scan |
| …preceded by an identifier code span (§ 2.5) | 404 | **22%** of the 1,830 |
| Bare continuations (`` `:548-549` ``, path inherited) | 547 in 78 files | own scan — **not** in the 1,872 (no dotted path to match) |

Densest single doc: `docs/specs/ANTS-1750.md` at 56 citations.

Four consequences follow, and they shape the design:

1. **Existence checking is not enough.** A citation that has drifted onto an
   unrelated statement still points at a line that exists. Only the cited
   *text* exposes that, which is why the text is the load-bearing output and
   not an option. A live instance, found while writing this spec:
   `docs/specs/ANTS-2090.md:69` reads ``` `compactEnvelope` (gated on
   `isFieldProjectionTool`, `mcpprojection.cpp:28`) ```, but
   `src/mcpprojection.cpp:28` is today a blank line between two unrelated
   functions. The line exists, so every existence-based check passes; the claim
   is nonetheless false. Tracked for repair as ANTS-3641.
2. **Bare basenames are the common case, not an edge case.** A resolver that
   only handled repo-relative paths would silently skip ~71% of the corpus —
   worse than no verb, because the empty result reads as "all clear".
3. **Citations live inside inline code spans.** Nearly every one is written
   `` `remotecontrol.cpp:335` ``. `doc_integrity` deliberately *masks inline
   code out* before harvesting links (`maskInlineCode`,
   `src/docintegrity.cpp:115`) because a link inside a code span is a code
   sample. For citations the rule inverts: the code span is exactly where the
   data is. This is the single most important reason the two checks cannot
   share a scrape pass.
4. **No mechanical signal covers most citations.** Only 22% carry a nearby
   identifier the verb can check the cited text against (§ 2.5). The other 78%
   can be *resolved and shown* but not *judged* — so the design must never let
   a filtered view imply they were verified (§ 2.1, § 2.6).

## 2. Surface

### 2.1 Verb shape

New verb `doc_citations`, **not** a mode on `doc_integrity`. The ROADMAP bullet
left this open ("decide at spec time"); the deciding facts are that
`doc_integrity`'s contract is a *defect list* (`findings[]` + `counts{}`
narrowed together by `kinds` — ANTS-3601 INV-18,
`RemoteControl::docIntegrityBuildResponse`) while this verb's primary output is
an *enumeration of every citation with its text*, most of which are healthy.
Folding an enumeration into a findings verb would either break `kinds`/`counts`
or bury the payload. Reuse happens one layer down, at the engine (§ 2.7).

Request:

```jsonc
{
  "caller_cwd":       "<required>",       // CallerCwdContract::Required
  "path":             "docs/specs/ANTS-2090.md",  // required, a FILE
  "only":             "all" | "stale",    // default "all"
  "max_range_lines":  3,                  // per-citation, clamped to [1, 20]
  "max_bytes":        131072,             // default 128 KiB, clamped to 4 MiB
  "etag_match":       "<opt>"
}
```

`path` must name a single **file**. Two deliberate divergences from
`doc_integrity`, both stated here because the two verbs are used together in
the same review loop and a silent difference is a caller trap:

- **A directory refuses `bad_args`**, where `doc_integrity` accepts one and
  walks it (ANTS-3601 INV-16). The reason is response size: `docs/` holds
  ~1,870 citations, and this verb's payload is per-citation *text*, not a
  defect count. `bad_args` is the taxonomy's code for "the value is present but
  ill-formed … for this verb" (`mcp-error-codes.md` § 1); the refusal message
  names the file-only requirement and points at ANTS-3643 for the sweep mode.
- **A well-formed but non-existent in-root `path` returns `ok:true` with an
  empty `citations[]`**, matching `doc_integrity` (ANTS-3601 INV-15,
  `src/remotecontrol.cpp:12334`) rather than refusing. Companion verbs should
  answer a typo'd path the same way.

Response:

```jsonc
{
  "ok": true,
  "path": "docs/specs/ANTS-2090.md",
  "doc_lines": 412,
  "citations": [
    {
      "doc_line": 69,                        // 1-based line in the doc
      "raw": "mcpprojection.cpp:28",         // as written, sans backticks
      "status": "ok",
      "path": "src/mcpprojection.cpp",       // resolved, project-relative
      "start_line": 28,
      "end_line": 28,
      "file_lines": 442,                     // target's total line count
      "text": [""],                          // line 28 is blank today
      "range_truncated": false,              // true → text clipped to max_range_lines
      "inherited_path": false,               // true → path came from § 2.2's sticky rule
      "approximate": false,                  // echo of the `~N` form; affects nothing
      "partial": false,                      // true → trailing loci dropped (`/265`, `+`)
      "anchor_symbol": "isFieldProjectionTool",  // absent when none precedes (§ 2.5)
      "anchor_found": false                  // ← the drift this verb exists to catch
    },
    {
      "doc_line": 118,
      "raw": "bad.cpp:3",
      "status": "ambiguous",
      "candidates": ["tests/audit_fixtures/cmd_injection/bad.cpp",
                     "tests/audit_fixtures/silent_catch/bad.cpp"],
      "truncated_candidates": true           // >8 matches; list is the first 8, sorted
      // no `path`, no `text`, no `anchor_*` — nothing was read
    }
  ],
  "returned": 2,                             // length of citations[] AFTER `only`
  "count": 9,                                // total citations found, BEFORE `only`
  "counts": { "ok": 7, "missing_file": 1, "out_of_range": 0, "read_error": 0,
              "ambiguous": 1, "unresolved": 0,
              "anchor_missing": 1, "unchecked": 5 },
  "truncated": false,
  "unparsed": [ { "doc_line": 88, "raw": "6.2:1", "reason": "unresolvable" } ],
  "unparsed_truncated": false
}
```

`counts` is always the **whole-doc** tally, never narrowed by `only` — it is the
context that makes a filtered list interpretable. `count` is likewise
pre-filter; `returned` is the post-filter length. (Deliberately unlike
`doc_integrity`'s `kinds`, which narrows counts too: there, counts *are* the
defect tally.)

`counts.unchecked` is the load-bearing honesty field: `status:"ok"` citations
with **no** anchor symbol — resolved and shown, but never judged. Per § 1
consequence 4 that is ~78% of a typical doc.

`only:"stale"` keeps every non-`ok` citation plus every `ok` citation with
`anchor_found:false`. It **drops** `ok` citations with no anchor, because there
is no drift signal for them — which is exactly why `counts.unchecked` is
emitted unfiltered alongside. `unparsed[]` is never filtered. A caller reading
only the filtered list and ignoring `counts.unchecked` will believe more was
verified than was; the field exists so that is a misreading rather than a
design flaw.

The dispatcher-injected `etag` (`mcp-tools.md` step 7) hashes the **response**,
so it covers drift in every cited file, not only the doc — an unchanged etag
means neither the doc nor any line it cites has moved.

### 2.2 Citation grammar

```
citation     := path ":" locus
continuation := ":" locus                 // path inherited (sticky rule below)
path         := [A-Za-z0-9_.@+-]+ ( "/" [A-Za-z0-9_.@+-]+ )*   // must contain a "."
locus        := "~"? N [ ("-" | "–") "~"? N ]                  // hyphen or EN DASH
```

- **En dash is not optional.** The corpus writes both `:208-228` and
  `:208–228`; a hyphen-only regex loses a large minority of the 668 ranges.
- **Trailing loci are dropped, not guessed.** `:262/265/286/300` and `:5669+`
  parse as their first locus with `partial:true` (39 occurrences, ~2%).
- **`~N` sets `approximate:true` and changes nothing else.** It is an echo so a
  reader can see the author hedged. Status is computed identically for tilde
  and non-tilde loci — there is no fuzzy-match tolerance, because there is
  nothing to be tolerant *against* (10 corpus occurrences, <1%).
- **Malformed loci are not citations.** `N < 1`, or an end below its start,
  → `unparsed[]` with `reason:"bad_locus"`. Neither shape occurs in the corpus
  today (both measured at 0); the rule exists so the read path never receives
  arguments it would refuse.
- **Sticky path.** A bare `:NNN` continuation inherits from the most recent
  **antecedent** earlier in the same document: the most recent entry that
  reached `citations[]` with a status other than `unresolved`. Its **already
  resolved** `path` is substituted and the § 2.4 ladder is *skipped* — an
  `unparsed[]` entry is never an antecedent, and an `ambiguous` one is (its
  ambiguity propagates: the continuation inherits `status:"ambiguous"` and the
  same `candidates[]`). The entry carries `inherited_path:true` so a reader can
  see the path was not stated. With no antecedent → `status:"unresolved"`.
  Attribution can be wrong when the author's antecedent was a prose mention
  rather than a citation; the flag is the mitigation, and the alternative
  (dropping 547 real citations) is worse. A fenced block **clears** the sticky
  path, so a code sample cannot become the antecedent of the prose after it.

### 2.3 Scrape rule (masking is inverted vs. `doc_integrity`)

- **Fenced blocks are skipped**, via `MarkdownScan::fenceMask`
  (`src/markdownscan.h:58`) — a `path:line` inside a ```` ```cpp ```` sample is
  illustrative text, not a claim about the repo. This is the one primitive
  shared with `doc_integrity`, including its ANTS-3638 list-container awareness.
- **Inline code spans are scanned, not masked** — the inverse of
  `maskInlineCode`, which `doccitations` therefore does not call.
- The scan runs on the **raw** line, so a citation is harvested wherever it
  appears — bare prose (`ROADMAP.md:19418 was …`), inside emphasis
  (``**`a.cpp:1`**``), or inside link text (``[`a.cpp:1`](x)``). No markdown
  construct other than a fence suppresses a citation.

### 2.4 Path resolution ladder

Applied in order; first hit wins. Every terminal step names where the entry
lands, so the ladder is total.

| # | Condition | Outcome |
|---|---|---|
| 0 | Absolute (`/…`), or lexically escaping root (`../..`), or resolving to a **symlink** | `unparsed[]`, `reason:"out_of_root"` — no read |
| 1 | Contains `/` → project-relative from the repo root | file exists → read; absent → `missing_file` |
| 2 | Bare basename, exactly one match in `basenameIndex` | read |
| 2b | Bare basename, ≥2 matches | `ambiguous` + `candidates[]` (sorted, first 8, `truncated_candidates` beyond) — no read |
| 3 | Bare basename, not indexed, but `<root>/<basename>` exists | read |
| 4 | A path part was present and none of the above resolved | `unparsed[]`, `reason:"unresolvable"` |
| 5 | No path part at all (bare `:NNN`, no antecedent) | `citations[]`, `status:"unresolved"` |

Step 3 is what catches `CMakeLists.txt:148`, `ROADMAP.md:19418` and
`README.md:…` — root files outside the index's `src/`+`tests/` walk. (If a
project sets `source_roots` in `.ants/project.json` (ANTS-2160), the index walk
widens accordingly and step 3 simply fires less often; the ladder is unchanged.)

Step 4 is the **only** producer of `unparsed[]` entries from a shape match, and
step 5 the **only** producer of `unresolved`. A prose false positive like
`` `6.2:1` `` (path part `6.2` — it contains a dot, so the grammar admits it)
lands at step 4, not in `citations[]`. Nothing is silently dropped.

**Ambiguity is reported, never resolved by guessing.** `src/` is flat (no
subdirectories) so in-lane collisions are impossible today, but the indexed
tree holds 14 `good.cpp` and 14 `bad.cpp` under `tests/audit_fixtures/`
(measured 2026-07-26). `TestAuditEngine` merges such collisions by picking the
longest path (`src/testauditengine.cpp:1568-1591`); that heuristic is fine for
a ranking aid and wrong for a verification tool, where a confidently-wrong line
of text is worse than a flagged unknown.

**Security boundary.** A citation is untrusted text inside a file the verb
reads, so step 0 is a **purely lexical** pre-filter modelled on
`DocIntegrity::resolveRelative` (`src/docintegrity.cpp:228`, the posture
ANTS-3601 INV-6 pins) — it must not stat, canonicalise, or otherwise probe an
out-of-root path. `PathValidation::validatePath` is deliberately **not** used
per-citation: it canonicalises (itself a probe on an existing path) and its
product is a whole-response `bad_path` envelope, the wrong granularity for one
citation among 33. It still applies, unchanged, to the request's own `path`
argument at the handler. Because a lexical check cannot see through a symlink,
step 0 additionally skips any resolved target that *is* a symlink
(`QFileInfo::isSymLink`) — closing the in-root-link-escaping-root case that
`validatePath`'s canonicalisation would otherwise have caught.

Note the deliberate asymmetry with the model: `resolveRelative` resolves
relative to the **citing doc's directory** (correct for a markdown link), while
a citation's directory-bearing path resolves from the **repo root** (correct
for `src/foo.cpp:12` written in a doc three levels down). Only the out-of-root
posture is borrowed, not the base.

### 2.5 Anchor-symbol check — a derived drift signal, not a replacement for `expect`

The ROADMAP bullet asks for an `expect` mode ("does the cited line still
contain this substring?"). The corpus supplies a weaker version for free: its
most common citation idiom names the symbol immediately before the location.

```
`RemoteControl::filterControlChars` (`remotecontrol.h:95-113`) strips C0
`uniqueSlug` (`roadmapindex.cpp:72-86`) DOES auto-append `-2`, `-3`
```

Pinned precisely, so two implementers build the same thing:

- **Anchor** = the nearest preceding inline code span on the **same line**
  whose content matches `^[A-Za-z_][A-Za-z0-9_:]*$`, and whose closing
  backtick is within **40 characters** of the opening backtick of the span
  containing the citation (measured on the raw line). A bare citation not in a
  code span has no anchor.
- **Needle** = the anchor's final `::` component (`A::b` → `b`). An anchor
  ending in `::` yields an empty component and is discarded — no anchor.
- **Match** = case-sensitive, whole-identifier (the needle's occurrence must
  not be flanked by `[A-Za-z0-9_]`, so `id` does not match `invalid`), searched
  over the **full resolved range** — not the `max_range_lines`-clipped `text`,
  so display width never changes a verdict. Trailing `\r` is stripped first
  (§ 2.8).
- `anchor_symbol` / `anchor_found` are **absent** when no anchor is found, and
  absent for every status that emits no text (`missing_file`, `out_of_range`,
  `read_error`, `ambiguous`, `unresolved`).

`anchor_found:false` is **advisory, not a `status`**: `status` answers "did the
citation resolve", this answers "does it still say what the doc claims".

**Measured coverage: 404 of 1,830 citations (22%).** This is the honest limit.
The check is worth its ~30 lines — it catches precisely the drift the bullet
describes, at zero authoring cost, on a fifth of the corpus — but it is a
*default*, not the `expect` mode. The residual gap (the other 78%, and the
scripted-re-verification case where a caller genuinely does hold an
expectation) is deferred to **ANTS-3642**, not quietly dropped.

### 2.6 Status taxonomy

| `status` | Meaning | `text` emitted |
|---|---|---|
| `ok` | Resolved, in range, read | yes |
| `missing_file` | Path resolved, file absent | no |
| `out_of_range` | File read, `start_line > file_lines` | no |
| `read_error` | File present but unreadable — permissions, a directory, or undecodable bytes | no |
| `ambiguous` | Basename matches ≥2 indexed paths | no (`candidates[]` instead) |
| `unresolved` | Bare continuation with no antecedent (ladder step 5) | no |

`read_error` exists because a single "cannot open" verdict would report a
permission-denied or binary target as *file absent* — a false claim in a tool
whose whole purpose is not making false claims.

### 2.7 Files and reuse

| File | Role | Target |
|---|---|---|
| `src/doccitations.h` / `.cpp` | Engine: scrape → parse → resolve → read | `ants_core_lib` (`CMakeLists.txt:269`, beside `src/docintegrity.cpp:296`) |
| `src/remotecontrol.cpp` | `RemoteControl::cmdDocCitations` — root resolve, `PathValidation` on `path`, build `basenameIndex`, call engine | existing |
| `src/claudeintegration.cpp` | Five hooks — see below | existing |
| `src/mainwindow.cpp` | Provider wiring | existing |

**All five `claudeintegration.cpp` hooks are mandatory**; an implementer who
registers the verb and stops ships a red suite:

1. `registerToolProvider` + `inputSchema` (`type:"object"`,
   `additionalProperties:false`); `description` ≤ 800 B with the overflow in a
   sibling `detail` (`mcp-tools.md` step 11).
2. `kindForName` (`:10019`) — `tests/features/mcp_tool_prefix_tags` walks every
   registered name and fails the build on an unbucketed verb.
3. The token-cost table (`:9927`, where `doc_integrity` sits).
4. `callerCwdContractFor` (`:11478`) → `Required`; `registerToolProvider`
   `Q_ASSERT_X`s on drift between the two.
5. `isEtagSupportedTool` (`:11662`).

```cpp
namespace DocCitations {

enum class Only { All, Stale };

struct Options {
    // Basename → project-relative paths, built by the CALLER (§ 2.4 step 2).
    // Passing it in keeps the engine Qt6::Core-pure and unit-testable without
    // a CodebaseIndex on disk; empty → steps 0/1/3/4/5 only.
    QHash<QString, QStringList> basenameIndex;
    Only only          = Only::All;
    int  maxRangeLines = 3;          // clamped to [1, 20]
    int  maxCitations  = 400;
    int  maxUnparsed   = 200;
    int  maxDocLines   = 20000;      // input guard; see § 3
    int  maxBytes      = 128 * 1024; // clamped to ReadRegion::kMaxBytesCeiling
};

// Reads `docAbsPath`, resolves every citation under `rootCanonical`, returns
// the § 2.1 envelope. Writes nothing; holds no cross-call state.
QJsonObject check(const QString &rootCanonical, const QString &docAbsPath,
                  const Options &opts = {});

}  // namespace DocCitations
```

**Reused:**

- `MarkdownScan::fenceMask` — fence skipping (§ 2.3).
- The `basenameIndex` is built by the handler from the **cached** codebase
  index: `CodebaseIndex::fromJson()` over the file at
  `CodebaseIndex::cachePathFor(rootCanonical)`, then one pass over
  `Index::files` keyed on `path.section('/', -1)`. No new index, no new cache,
  no new disk artifact. A cache that is absent, unparseable, or version-
  mismatched yields an empty map and the ladder degrades to steps 0/1/3/4/5
  (bare basenames fall through to `unparsable`) — the verb never cold-builds
  the index itself, because a 4-second build is the wrong thing to do inside a
  read verb. `session_orient` refreshes it at session start (ANTS-2140), which
  is when it matters. When `Index::filesTruncated` is set (the `kMaxIndexFiles`
  cap), the map is correspondingly partial and more basenames land
  `unparsable`; that is visible in `counts`, never silently resolved wrong.

**Explicitly NOT reused — `ReadRegion::extract`.** It was the obvious
candidate and does not fit, for three independent reasons: it opens and reads
the file on every call (defeating the line cache below), it never reports the
target's total line count (so `file_lines` and the `out_of_range` predicate
have no source), and its `truncated` is byte-based where `range_truncated` is
line-based. The engine therefore reads each target once into the cache and
slices from it — which yields `file_lines` for free, since the read already
walked the whole file.

## 3. RAM and build cost

- **Target-file line cache**, keyed by resolved path, so a doc citing one file
  30 times reads it once. Bounded at **16 files or 8 MiB, whichever binds
  first**; past the cap, further targets are read without being cached — no
  eviction, no LRU churn, no unbounded growth. Sized against the realistic
  worst case: 56 citations into a handful of files, the largest of which
  (`src/remotecontrol.cpp`) is ~18k lines.
- **Input guard**: a doc over `maxDocLines` (20 000) is scanned to that line
  only and sets `truncated:true`. Without it, `path:"ROADMAP.md"` (~19.5k
  lines, ~1,800 citations) is a legal call.
- **Response** bounded by `maxCitations` (400), `maxUnparsed` (200) and
  `maxBytes` (128 KiB default — lower than the read-family 512 KiB because the
  value here is a compact review artifact, not a bulk read). Exceeding any of
  them sets `truncated:true` (`unparsed_truncated` for its own cap).
- **Text normalisation**: a trailing `\r` is stripped from every emitted line
  and before the § 2.5 comparison, so a CRLF target does not produce phantom
  mismatches.
- **Build**: one new `.cpp` in an existing static lib; no new external
  dependency, no new target.

## 4. Invariants

- **INV-1** — A `path:line` citation inside a fenced block is not harvested.
  *Test:* fixture with `` `foo.cpp:1` `` inside a ```` ```cpp ```` fence and
  another in prose → exactly one citation, the prose one.
- **INV-2** — A citation inside an inline code span **is** harvested (the
  inverse of `doc_integrity`'s link rule), as is one in bold or in link text.
  *Test:* fixture with `` `src/a.cpp:2` ``, ``**`src/a.cpp:3`**`` and
  ``[`src/a.cpp:4`](x)`` → three citations.
- **INV-3** — Both range separators parse: `:10-12` and `:10–12` (en dash)
  yield `start_line:10, end_line:12`. *Test:* fixture with one of each → two
  citations with identical loci.
- **INV-4** — A bare basename resolving to exactly one indexed path resolves;
  one matching ≥2 is `status:"ambiguous"` with `candidates[]` sorted and no
  `text`/`path`. *Test:* `basenameIndex` seeded with one unique and one
  duplicated basename → one `ok`, one `ambiguous` carrying both candidates.
- **INV-5** — A bare basename absent from `basenameIndex` but present at the
  repo root resolves (ladder step 3). *Test:* fixture citing `CMakeLists.txt:1`
  with an empty `basenameIndex` → `status:"ok"`.
- **INV-6** — An absolute, root-escaping, or symlinked citation target is never
  read and never probed; it appears in `unparsed[]` with
  `reason:"out_of_root"`. *Test:* fixture citing `/etc/passwd:1`,
  `../../outside.h:3` and an in-root symlink to an out-of-root file → all three
  in `unparsed[]` with that reason, zero in `citations[]`, and no read of any
  target (assert via a probe counter, or by giving the symlink's target content
  that would otherwise appear in `text`).
- **INV-7** — A bare `:NNN` continuation inherits the most recent antecedent's
  **resolved** path with `inherited_path:true`; an `unparsed[]` entry is never
  an antecedent; with no antecedent the continuation is `status:"unresolved"`.
  *Test:* fixture ordering `src/a.cpp:1`, `` `6.2:9` ``, `` `:2` `` → the third
  inherits `src/a.cpp` (not the unparsed token); a fixture whose first citation
  is a bare `:5` → `unresolved`.
- **INV-8** — A fenced block clears the sticky path: a bare `:NNN` after a
  fence whose only preceding citation was before that fence is `unresolved`.
  *Test:* fixture ordering citation → fence → bare `:9` → `unresolved`.
- **INV-9** — `:262/265/286` and `:5669+` parse as their first locus with
  `partial:true`. *Test:* fixture with both forms → `start_line` 262 and 5669,
  both `partial`.
- **INV-10** — `~N` sets `approximate:true` and changes no other field or
  status. *Test:* two fixtures identical but for the tilde → byte-identical
  entries apart from `approximate` and `raw`.
- **INV-11** — A resolved-but-absent file is `missing_file`; a start line past
  EOF is `out_of_range`; an unreadable or undecodable target is `read_error`;
  none emits `text`. *Test:* fixture citing `src/gone.cpp:1` (directory-bearing,
  so it reaches ladder step 1), `src/a.cpp:9999`, and a mode-000 file → the
  three statuses, `text` absent on all.
- **INV-12** — A range longer than `max_range_lines` returns the first
  `max_range_lines` lines with `range_truncated:true` and `end_line` unchanged.
  *Test:* `:1-10` with `max_range_lines:3` → 3 lines, flag set, `end_line` 10.
- **INV-13** — `anchor_symbol` is the nearest same-line preceding identifier
  code span within 40 characters, compared on its final `::` component,
  case-sensitively, whole-identifier, over the **full** resolved range
  regardless of `max_range_lines`; both fields absent when no anchor is found
  or the status emits no text. *Test:* table of fixtures covering: match,
  no-match, substring-only (`id` vs `invalid` → `false`), `A::b` qualified,
  `Foo::` trailing-colons (no anchor), an anchor 41 characters away (no
  anchor), and a `:1-10` range whose needle sits on line 8 with
  `max_range_lines:3` → `anchor_found:true`.
- **INV-14** — `only:"stale"` keeps non-`ok` entries and `ok` entries with
  `anchor_found:false`, drops `ok` entries with no anchor, never filters
  `unparsed[]`, and leaves `count`/`counts` at their whole-doc values.
  *Test:* fixture producing 3 anchored-ok, 1 anchor-missing-ok, 2 unanchored-ok,
  1 missing_file, 1 unparsed → `returned:2`, `count:7`, `counts.ok:6`,
  `counts.unchecked:2`, `unparsed` length 1.
- **INV-15** — A shape-matching token whose path part resolves nowhere lands in
  `unparsed[]` with `reason:"unresolvable"`, not `citations[]`. *Test:* fixture
  containing `` `6.2:1` `` → `citations` empty, one `unparsed` entry whose
  `raw` is `6.2:1` (the grammar's capture, not the surrounding prose).
- **INV-16** — `DocCitations::check` writes nothing to disk and holds no
  cross-call state. *Test:* run twice over an unchanged fixture →
  byte-identical output, no new files.
- **INV-17** — No more than `maxCitations` entries, `maxUnparsed` unparsed
  entries, or `maxBytes` of text are emitted, and a doc longer than
  `maxDocLines` is scanned no further; each sets `truncated:true`
  (`unparsed_truncated` for its own cap). *Test:* four fixtures, one per cap,
  each run at a lowered cap.
- **INV-18** — The verb refuses without `caller_cwd` (`caller_cwd_required`),
  refuses a root-escaping `path` with `bad_path`, and refuses a directory
  `path` with `bad_args`; a well-formed but non-existent in-root `path` is
  **not** a refusal (`ok:true`, empty `citations[]`, matching ANTS-3601 INV-15).
  *Test:* four verb-level calls, one per outcome.
- **INV-19** — The verb is registered with `CallerCwdContract::Required`, is in
  `isEtagSupportedTool`, is bucketed in `kindForName`, and carries a token-cost
  entry. *Test:* source-scrape of `mainwindow.cpp`, `claudeintegration.cpp`,
  `remotecontrol.cpp` — the `rc_get_text_byte_cap` pattern used by
  `tests/features/doc_integrity_verb` (see `docs/specs/ANTS-3601.md` § 2.6).
- **INV-20** — The line cache reads each distinct target at most once until the
  16-file / 8-MiB bound binds, and past it reads uncached rather than evicting.
  *Test:* fixture citing one file 5 times → 1 open (probe counter); fixture
  citing 20 distinct files → 20 opens, ≤16 retained.
- **INV-21** — `max_range_lines` clamps to `[1, 20]` and `max_bytes` to
  `ReadRegion::kMaxBytesCeiling`; out-of-range requests clamp rather than
  refuse. *Test:* calls with `max_range_lines:0`, `:500`, and
  `max_bytes:99999999` → effective 1, 20, and 4 MiB.
- **INV-22** — The `tools/list` entry exists with `inputSchema.type ==
  "object"`, `additionalProperties == false`, and a `description` ≤ 800 bytes.
  *Test:* the schema-validity assert `mcp-tools.md` § Tests mandates.
- **INV-23** — A trailing `\r` is stripped from emitted `text` and before the
  INV-13 comparison. *Test:* CRLF fixture whose anchor sits at end-of-line →
  `anchor_found:true` and no `\r` in `text`.
- **INV-24** — A malformed locus (`N < 1`, or end below start) is not a
  citation: it lands in `unparsed[]` with `reason:"bad_locus"`. *Test:* fixture
  citing `src/a.cpp:0` and `src/a.cpp:10-5` → two `unparsed` entries, zero
  citations.

## 5. Tests

`tests/features/doc_citations/` — `spec.md` (pointing here) plus
`test_doc_citations.cpp`, added to an existing bundle's `SOURCES` list (the
`test_claude` bundle, `ants_add_gui_bundle(test_claude` at
`CMakeLists.txt:1198`, where the other MCP-verb feature tests live). **Not**
`add_executable` — per `tests/features/README.md`. Label `features;fast`.

Coverage: INV-1…INV-17 and INV-20…INV-24 drive `DocCitations::check` over
`QTemporaryDir` fixtures with a seeded `basenameIndex` (no CodebaseIndex on
disk needed). INV-18 drives the verb handler. INV-19 and INV-22 are
source-scrapes.

Per the project convention, the test is verified **RED against pre-fix code**
(before `src/doccitations.cpp` exists it does not compile — the feature-absent
RED that ANTS-3601's test used) before the implementation is restored.

## 6. Out of scope

- **Request-side `expect` map** → **ANTS-3642**. § 2.5's derived anchor covers
  22% of citations; the scripted-re-verification case the ROADMAP bullet names
  is genuinely unserved until this ships.
- **Directory / whole-corpus sweeps** → **ANTS-3643**. Needs a digest response
  shape (counts + stale subset, no text), which is a different contract.
- **Rewriting drifted citations.** A verb that edits line numbers in a doc is a
  different risk class; the drift may mean the *claim* moved, not the line.
  Not tracked — a deliberate non-goal.
- **Non-markdown inputs.** Source-comment citations (`// see foo.cpp:12`) are
  out; the workflow this serves is doc review. Not tracked — a non-goal.
- **A reverse "which docs cite this file?" index.** That is a cache with an
  invalidation contract, and nothing has asked for it. Not tracked.

## 7. Cross-doc impact

- `CLAUDE.md` — no change: the MCP preamble points at `tool_info {catalog:true}`
  for the live verb list (ANTS-2037), which picks the new verb up automatically.
- `CHANGELOG.md` — an `Added` entry at ship time.
- `docs/subsystems.md` — a **new `doccitations` lane entry**, beside the
  `docintegrity` entry (`docs/subsystems.md:78`), naming its `ants_core_lib`
  membership and the verb it powers. The catalogue is one entry per module and
  `indie_review_partition` derives one review lane per entry, so folding into
  the neighbouring `markdownscan` (`:73`) or `docintegrity` entry would deny
  the new module a review lane.
- `docs/specs/ANTS-3601.md` — no edit; both divergences (§ 2.1) are stated
  here, on the newer verb.

## 8. Cold-eyes loop log

**Loop 1 (2026-07-26)** — 3 lanes (design / accuracy / conformance), two-tier:
all 3 breadth on Haiku, 2 escalated to depth. Accuracy lane accepted clean
(32 verification calls, 0 findings). Tally: **CRITICAL 4 · HIGH 7 · MEDIUM 12 ·
LOW 12 · INFO 3** — 35 verified, 1 dismissed. All verified findings fixed.

Substantive fixes: `CodebaseIndex::serve` returns a `QJsonObject`, not an
`Index` — the named reuse could not have worked, replaced with
`fromJson`/`cachePathFor` plus a stated degradation path; the
`unparsed[]`/`unresolved`/`missing_file` three-way contradiction resolved by
making the § 2.4 ladder total; `ReadRegion::extract` dropped as a reuse (it
re-reads per call, cannot yield `file_lines`, and its `truncated` is
byte-based) in favour of slicing from the cache; `PathValidation` demoted to a
handler-only check with a lexical step-0 pre-filter plus a symlink guard; two
unacknowledged divergences from ANTS-3601 (non-existent path, directory path)
either aligned or stated; the five mandatory `claudeintegration.cpp`
registration hooks enumerated; `read_error`, `bad_locus`, `maxDocLines`,
`maxUnparsed` and CRLF handling added.

The load-bearing correction: § 2.5's anchor idiom was asserted "dominant" and
measures **22% (404/1,830)**. `only:"stale"` as first drafted would have
dropped the unanchored 78% of healthy citations, making a filtered view read
"all clear" over citations that were never judged — the exact failure mode § 1
warns about. Fixed by keeping `counts` whole-doc, adding `counts.unchecked`,
and deferring the real `expect` mode to ANTS-3642 rather than presenting the
derived check as its equal.
