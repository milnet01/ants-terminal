# ANTS-3660 — `doc_dedup`: find the same passage written twice

**Status:** implemented (2026-07-28) — cold-eyes loops 1, 2 and 4 folded, plus loop 4's deferred tail (ANTS-3677). `src/docdedup.{h,cpp}` + the `doc_dedup` verb ship; all 9 invariants covered and proved RED by mutation.

**Kind:** implement.
**Source:** ROADMAP.md ANTS-3660 (user-request-2026-07-27).
**Blocked by:** ANTS-3664 (the finding shape). ANTS-3666 is **met** (2026-07-28).
**Blocker for:** ANTS-3663 (`doc_lint`), which composes it.
**Composes with:** ANTS-3601 (`doc_integrity`, whose verb-layer walk this
copies) and ANTS-3603 (`MarkdownScan`, for fence awareness).

**Layman:** Find the same passage written twice in two places, automatically,
instead of paying a reviewer to notice it.

The hold was lifted by ANTS-3666, which measured the corpus so that § 1.1 is
that run's output and § 2.3's constants are read off real numbers rather than
guessed. The ANTS-3677 fold-in re-ran the measurement rather than reasoning from
the recorded table, and that was the right call: it found INV-1 and INV-7
genuinely contradictory (fixed by scoping each to a different half of the
algorithm), § 7's two "the calibration will answer this" questions still
unanswered (both now measured), and **four numbers quoted off the
punctuation-KEPT rows of a spec that pins punctuation stripped**. § 1.2 records
the last re-measure, against the shipped engine.

---

## 1. Problem

This is the one `/cold-eyes` § 1e dimension with no mechanical check at all, and
it is the expensive one. The skill's Phase 4 states that a run which will not
converge usually has a duplication problem, and that reconciling two copies is
the *wrong* fix — they diverge again on the next edit, and later loops find them
one at a time. Its Budget section is blunter: every duplicated fact is re-read by
every lane on every loop *and* is a standing source of future findings.

**Nothing in the repo does this.** Measured 2026-07-27: `shingle` has zero hits
across `src/`. Three similarity implementations exist and none compares document
text — `SimilarCode::findSimilar` (`src/similarcode.h:74`) scores a free-text
query against *symbol signatures*; `src/remotecontrol.cpp::rcHeadlineJaccard()`
compares roadmap *headlines*;
`src/indiereviewengine.h::suggestedMerges()` runs
normalised Levenshtein over *review-lane summaries*.

### 1.1 What this corpus's duplication looks like — MEASURED (ANTS-3666)

ANTS-3666 measured `docs/` — 275 files, 105k lines — across three segmentation
rules × shingle sizes 2/3/4 × punctuation kept/stripped, 18 combinations in
19.6 s. Every number below is that run's output. Nothing here is an estimate,
and the constants in § 2.3 are read off it rather than chosen.

**The two earlier passes disagreed because they segmented differently, not
because either miscomputed.** The verb-contract pair, at 3-grams:

| Paragraph rule | Score |
|---|---|
| marker line dropped, continuations kept — this spec's original § 2.1 | **0.024** |
| list-aware: marker line joined with its continuations | **0.545** |
| list content excluded entirely | **0.000** |

Loop 1's 0.53 is the list-aware figure; loop 2's 0.038 is the original rule's.
Each pass was right about a different rule. **The original rule is the defect:**
it drops every `- **INV-N** —` marker and compares the remainder as a headless
fragment, scoring this corpus's clearest duplication at 0.024. § 2.1 now
specifies the list-aware rule, and that is a correctness fix, not a tuning
choice.

**Both pairs were also attributed to the wrong documents.** The real ones, cited
by content rather than by line because a line number in a spec is stale by the
next edit — these two moved by 128 and 78 lines between ANTS-3666 and the
ANTS-3677 re-measure, and `documentation.md` § 1.7 already requires this:

| Duplication | Pair | punct kept | punct stripped |
|---|---|---|---|
| the `caller_cwd_required` / `bad_path` verb-contract minimum | `ANTS-3662` ↔ `ANTS-3663` | 0.545 | **0.583** |
| the "one new translation unit … join the existing `ants_core_lib`" build-cost stanza | `ANTS-3659` ↔ `ANTS-3664` | 0.306 | **0.450** |

Both re-measured unchanged on 2026-07-28 after two days of edits to all four
documents, which is the evidence that the score is a property of the passages
and not of a particular checkout.

Neither `ANTS-3660` ↔ `ANTS-3662` nor `ANTS-3660` ↔ `ANTS-3661` — the pairs the
disputed table named — reaches the top five of its own probe. Loop 2's 0.652 is
not reproducible under any of the eighteen combinations.

Four consequences, each of which changed the design:

1. **Segmentation is not a tuning parameter, and is settled here.** It moves the
   flagship pair by 23×, more than any other choice in this spec. It is fixed in
   § 2.1 and is not per-call overridable.
2. **Punctuation must be pinned, and is stripped.** Its effect is uneven and
   that is the point: stripping moves the build-cost pair 47% (0.306 → 0.450)
   and the verb-contract pair 7% (0.545 → 0.583). The larger of the two is more
   than the gap between any two candidate thresholds, so on some pairs the
   setting decides whether they are found at all. Leaving it unstated is
   precisely what let two passes compute different numbers and each conclude
   the other had erred.
3. **The earlier draft had the exclusions backwards in one direction and right
   in the other.** Right: repeated boilerplate across sibling specs *is* the
   finding, and both real pairs are 19–31 words, so a `minWords` set to suppress
   noise deletes them. Wrong: **generated artifacts must be excluded.** Dated
   `AUTOMATED_AUDIT_REPORT_*` files and the `superpowers/` plan-banner mirror
   are 7 files and 109 passages, and they supply **more than half of every
   exact-duplicate pair in the corpus** — duplication that is what those files
   *are*. Excluding them drops exact duplicates (J = 1.000) from 59 to 24 and
   the whole report from 416 pairs / 137 clusters to 275 / 128 (§ 2.4). Those
   are the punctuation-stripped figures, i.e. this spec's pinned configuration;
   the loop-4 text quoted 79 / 40 / 21 off the punctuation-kept rows of the same
   run, which is how a table with eighteen rows misleads a reader who has
   already pinned one of them.

4. **Paraphrase is out of reach and the spec says so.** The four-way "Verified
   RED…" pair at ~0.17 states one idea in four rewordings and is lexically
   indistinguishable from unrelated prose on a shared subject. No shingle
   measure reaches it. § 5 states this as a limit so nobody reads a clean run as
   proof of no duplication.

### 1.2 Every figure above is now the SHIPPED engine's, not the probe's

ANTS-3666's script was a throwaway and approximated two things this spec
requires. Run side by side on one corpus, it reports 12,936 passages / 276 pairs
/ 129 clusters where `DocDedup` reports **12,995 / 275 / 128**. Neither is a
defect in the other; the engine is the conforming one, and the gap has exactly
two causes:

- **The fence primitive.** § 2.1 requires `MarkdownScan::fenceMask` — CommonMark:
  at most three leading spaces, and a backtick fence's info string may hold no
  backtick (ANTS-3655). The probe used a loose `^\s*(```+|~~~+)`, which masks
  *more* lines as fenced, so the conforming engine sees more prose. That is the
  +59 passages.
- **Pointer lines.** § 2.4 excludes them; the probe had no such rule. That is
  the −1 pair.

Recorded because the numbers are quoted in five sections and a future reader
re-running the harness will get the engine's, not the probe's. **The harness is
the authority from here** — `DISABLED_CorpusCalibration` prints every figure
this document asserts, including the exclusion delta, the per-threshold cluster
counts, the within/across split and the longest posting list, so none of them
has to be re-derived by hand again.

## 2. Surface

### 2.1 Passages

The unit is a **paragraph**. Segmentation is stated precisely, because two
implementations that segment differently produce different findings from the
same document:

- A paragraph is a maximal run of lines that are none of: blank, a heading
  (`^#{1,6} `), a list marker (`^\s*(?:[-*+]|\d+\.)\s`), a GFM table row
  (`^\s*\|`), or masked by `MarkdownScan::fenceMask`.
- **A masked fence line acts as a blank** — it breaks a run rather than joining
  the prose around it. A code sample is not a sentence in the paragraph that
  follows it.
- **A list item is one passage: its marker line joined with the indented
  continuations beneath it**, ending at the next marker, the next unindented
  line, or a fence. § 1.1 measured the alternative — treating the marker line as
  a pure break — and it scores this corpus's clearest duplication at 0.024
  instead of 0.545, because every `- **INV-N** — …` is then compared without its
  own subject. This rule is fixed, not per-call overridable.
- Headings and table rows break a run and are not themselves paragraph content.
  A bullet list is therefore not one giant passage, which is what a naive "run
  of non-blank lines" rule would produce.

Each paragraph is normalised before comparison, in this order: strip inline-code
delimiters and markdown emphasis; **replace every non-word, non-space character
with a space**; fold all whitespace runs to one space; lowercase. Order matters —
folding before stripping would leave the delimiters adjacent to words and change
the tokens.

**Punctuation is stripped, and saying so is load-bearing.** ANTS-3666 measured
the same pairs both ways: stripping moves the build-cost stanza 47%
(0.306 → 0.450) and the verb-contract pair 7% (0.545 → 0.583). The larger is
bigger than the gap between any two thresholds this
spec might pick. Stripping is the choice that puts both § 1.1 pairs above
`minSimilarity`; keeping punctuation leaves the build-cost class unreachable at
any threshold that also yields a triageable report.

### 2.2 Shingles and the score

A paragraph becomes the set of its overlapping word `shingleSize`-grams. Two
paragraphs pair when their Jaccard similarity — `|A ∩ B| / |A ∪ B|`, the measure
`SimilarCode::jaccard` (`src/similarcode.h`) already implements on a
different input — is **at or above** `minSimilarity`.

Word shingles rather than character ones, because character shingles score two
paragraphs on a shared subject as similar for sharing vocabulary. Overlapping
rather than disjoint, so a one-word insertion shifts a few shingles instead of
all of them.

**Scoring and candidate gathering are two different steps, and conflating them
is what made INV-1 and INV-7 contradict each other for three loops.** The split
is now stated once, here, and each invariant governs one half:

- **Candidate gathering** decides which pairs are *looked at*. It is the
  inverted index of § 4, and `maxPostings` (INV-7) prunes it.
- **Scoring** decides which of those pairs are *reported*. It is the exact
  Jaccard over each paragraph's **full** shingle set — the pruned shingles are
  still counted in both the intersection and the union — and `minSimilarity`
  (INV-1) is its threshold.

A prune that also removed shingles from the arithmetic would make the reported
similarity a function of how common a shingle happened to be corpus-wide, so two
identical paragraphs could score below 1.0. The reference implementation
ANTS-3666 ran did exactly that; it agreed with the exact score here only because
its prune never fired (§ 4).

### 2.3 Options, with values

Every constant has a value here. A tuning parameter named but not valued is not
a specification — the first draft named four and valued none, and its own
boundary invariant was unwritable as a result.

```cpp
namespace DocDedup {
struct Options {
    int shingleSize   = 3;      // words per shingle
    double minSimilarity = 0.40; // § 1.1 measured: catches both real pairs
                                 // (0.583 and 0.450) with punctuation stripped;
                                 // ~0.17 paraphrase is unreachable at any value
    int minWords      = 15;     // below this a pair is coincidence; the real
                                // finds are 19–31 words, so this must stay low
    int maxPassages   = 20000;  // paragraphs COMPARED; see INV-6
    int maxPostings   = 400;    // a shingle in more than this many paragraphs is
                                // a stop-shingle and is not used for candidate
                                // gathering (§ 4) — it carries no signal and is
                                // what makes the index degenerate
    // Inherited, same values as DocIntegrity::Options so the doc walks agree:
    qint64 maxDocBytes = 2 * 1024 * 1024;
    int    maxDocsPerRun = 2000;
};
}
```

`minSimilarity`, `minWords` and `shingleSize` are per-call overridable; the caps
are not, matching how `doc_integrity` treats its own.

**The calibration run happened — it is ANTS-3666, and § 1.1 is its output.**
These values are measured, not provisional. Against `docs/` at these defaults
the verb reports **275 pairs in 128 clusters**, and both § 1.1 pairs clear the
threshold (0.583 and 0.450).

**One half of the original acceptance criterion is not met, and the honest
answer is that the criterion was wrong.** It required output "small enough for a
reviewer to triage in one sitting (order tens of pairs, not hundreds)" *and* the
named pairs reported. ANTS-3666 shows these cannot both hold: the band from 0.40
to 0.75 was sampled and is **real duplication a reviewer would act on** — the
ROADMAP-flip stanza repeated across specs (0.739), two specs stating the same
`additionalProperties:false` invariant (0.625), the `join the existing
ants_core_lib` build-cost line (0.667). Raising `minSimilarity` to hit a smaller
number would discard true positives to make a report look tidy.

This spec pre-committed to that outcome — "if those cannot hold together, the
defaults are wrong" — and the defaults are not what was wrong. The criterion is
restated, **with a number, because "triageable in a sitting" is the same kind of
unfalsifiable phrase this spec deleted from § 2.4**:

> Over `docs/` at the § 2.3 defaults, the run reports **both § 1.1 pairs at
> their measured scores (0.583 and 0.450, ±0.005)** and returns **no more than
> 150 clusters**.

Measured 128 on 2026-07-28. The 150 is headroom on a corpus that grows, not a
tuned figure; what it guards is a regression that explodes the output, since
every threshold and segmentation defect this spec has already suffered showed up
first as a cluster count off by more than an order of magnitude (416/137 with
the § 2.4 exclusions disabled). `DISABLED_CorpusCalibration` (§ 6) is the
harness that checks it, and it prints the numbers whether or not they pass —
a rate nobody reads is not a measurement.

### 2.4 Three exclusions, and the one that was deleted

- **Below `minWords`** — a pair of very short paragraphs shares shingles for
  trivial reasons. Kept, but set low (§ 2.3) because § 1.1 measured the real
  finds just above it.
- **Generated and templated artifacts** — paths matching
  `AUTOMATED_AUDIT_REPORT` or a `superpowers/` directory. Added on evidence, not
  taste — dated audit reports sharing a Stack Overflow link list, and the
  `> **For agentic workers:**` banner stamped into every plan file. **§ 1.1
  owns the measurement; it is not repeated here.** It was, and the two copies
  had already diverged by the time this sentence was written: § 1.1 was
  corrected to the punctuation-stripped figures and this bullet was still
  quoting the punctuation-kept ones, in a document whose subject is the same
  fact written twice. Duplication between two generated files is not a defect in
  either; it is what generating them means.
- **Pointer lines** — kept, and for a reason worth stating: a pointer is the
  *shape the skill recommends* for an already-deduplicated fact ("state it once
  and make every other mention a pointer"). Reporting pointers would flag the
  fix as the defect.

  **With a predicate this time.** The first draft said "a single link or a
  `see § X` cross-reference" — two examples and no rule, which is precisely the
  charge that deleted the boilerplate exclusion below, levelled at a bullet four
  lines above it. A **pointer line** is a paragraph whose entire trimmed content
  is one markdown inline link `[text](target)` or one bare URL, optionally
  preceded by `see`/`See` and followed by a single `.` or `,`. Nothing else: a
  sentence that merely *contains* a link is prose and is compared. The `see § X`
  half needs no rule at all — it is three words, and `minWords` had already
  excluded it before this bullet was written.

**Not excluded: `doc-examples` regions (ANTS-3659).** § 7 deferred this to the
calibration run and the run did not answer it; measured 2026-07-28, the answer
is **ignore the mask**, on three numbers:

- Only **3 of 268** documents use a region at all.
- **67 pairs** have an endpoint inside one — but all 67 have *both* ends inside
  one, all 67 are in a **single document**, and they collapse into **one
  cluster** of the ~128. A quarter of the pair list is under 1% of the report a
  reviewer actually reads (§ 2.5), so "the regions dominate the output" is false
  under the shape this verb ships. (Measured with ANTS-3666's probe rather than
  the harness: the shipped engine has no region awareness to measure *with*,
  which is the decision being justified. § 1.2 covers why its pair total differs
  by one.)
- Those 67 are `ANTS-3636`'s `- **INV-N** — *moved to ANTS-3653*` tombstone
  rows — **contract prose, not illustrations**. A region is a coarse instrument:
  ANTS-3636 marks 1234 of its 1912 lines, and honouring the mask would suppress
  real duplication inside them while buying a report 0.8% shorter.

That is the whole of ANTS-3659 § 2.1's per-verb policy for this verb; there is
no `honourExampleRegions` option, because an option here is a way to get a
different answer to a question that has been measured.

**Deleted: the structural-boilerplate exclusion.** The first draft excluded
"a spec's header block, a loop-log row, a licence notice". It was defined by
three examples and no predicate, so two implementations would differ
arbitrarily — and § 1.1 showed it would delete real findings, because repeated
boilerplate across sibling specs is exactly what this corpus over-produces.
Header blocks and table rows are already excluded structurally by § 2.1's
segmentation rule; nothing further is needed.

### 2.5 Report-only, and it never picks

The verb reports pairs; **which copy is canonical it never decides.** That is
correctness, not caution: the canonical home is the section that *owns* the
fact, which is a question about document architecture, and a verb deleting the
wrong copy destroys the authoritative one. `doc_lint` (ANTS-3663) must never
auto-fix a `doc_dedup` finding.

Findings are `DocFinding::Finding` (ANTS-3664) with `verb:"doc_dedup"`,
`kind:"near_duplicate"`, `autoFixable:false`, anchored at the earlier passage.
The envelope additionally carries the structured pair —
`pairs[]: { a:{file,line}, b:{file,line}, similarity }` — because
`DocFinding::Finding` has no second-location field, so **the pair survives only
in this verb's own envelope**, not in a `doc_lint` `findings[]` row. A composing
caller that needs both ends reads `doc_lint`'s top-level `pairs[]`, which hoists
this array whole (ANTS-3663 § 2.1). There is no per-check envelope to read: that
composite returns one findings list, not N nested verb envelopes.
`file` rather than the ROADMAP bullet's `path`, matching `DocFinding::Finding`
and `DocIntegrity::Finding`; the bullet predates the shared shape.

**Pairs are also grouped into clusters, and the grouping is not cosmetic.**
N documents sharing one stanza produce N(N−1)/2 pairs but are **one** thing to
fix; reporting pairs alone makes a single repeated boilerplate line look like
forty findings. The envelope carries
`clusters[]: { passages:[{file,line}], size, maxSimilarity }` — connected
components over the reported pairs, each passage in exactly one cluster.
Measured on `docs/`: 275 pairs collapse to 128 clusters, and at tighter
thresholds the ratio widens sharply (112 pairs → 26 clusters at 0.80; 24 → 17 at
1.000). `pairs[]` is retained unchanged, because the cluster loses which two
passages actually scored what, and INV-4 still governs it.

**Within-document pairs are reported, and § 7 left this open too.** Measured
2026-07-28: **75 of 275** pairs are within one document and **200** across. The
split is not marginal and the within-document half is not noise — its
highest-scoring member is `ANTS-1289`, one paragraph written twice verbatim
seven lines apart at J = 1.000, which is precisely the finding a reviewer wants
and precisely the one an across-documents-only verb cannot see. A `a.file ==
b.file` pair is therefore legal; only `a == b` is not (INV-4).

### 2.6 Files and hooks

Engine `src/docdedup.{h,cpp}` in `ants_core_lib`, Qt6::Core-only.

**This engine is ANTS-3664 § 2.3's stated exception to the per-document
signature**, because it compares passages *between* documents and so cannot
score any one of them alone. It accumulates, then scores once:

```cpp
// Family convention (ANTS-3664 § 2.3): text in, never opens the document.
// Unlike its siblings this returns nothing per call — a pair needs two
// documents, so scoring cannot happen until the walk is done. The two calls
// are members rather than free functions because the accumulator between them
// IS state: free functions with the same signatures would need a file-scope
// corpus and stop being callable twice in one process.
class Accumulator {
public:
    void   add(const QString &text, const QString &relPath, const Options &opts = {});
    Result finish();
};
```

`Result` carries `pairs`, `clusters`, `passagesTotal`, `passagesCompared` and
`truncated` — § 2.5's envelope, one field per JSON key. `Options` carries
§ 2.3's tuning constants plus § 2.4's excluded path globs; the caller fills it
once and passes the same value to every `add`.

Splitting `add` from `finish` is what keeps the one-open-per-document guarantee
reachable: ANTS-3663 hands each document's text to all five checkers from a
single read, and a `doc_dedup` that took a *path* would re-open every file in
the tree, defeating the composition it is being written for.

Verb layer mirrors `doc_integrity` (`src/remotecontrol.cpp::cmdDocIntegrity()`): a
`cmdDocDedup` handler plus **one** pure static, `docDedupBuildResponse`. The
walk **calls `docIntegrityEnumerate` directly** rather than cloning it — a
third `*Enumerate` differing from the first two only in its verb name is the
duplication this verb reports, and ANTS-3661 § 2.4 already made the same
correction to its own draft.
Registration is the set ANTS-3661 § 2.4 enumerates: one `registerToolProvider`
with `CallerCwdContract::Required`, plus the five `src/claudeintegration.cpp`
hooks. Not restated here — one canonical home, per the rule this verb exists to
enforce.

## 3. Invariants

- **INV-1** — A **candidate** pair (§ 2.2 — two passages sharing at least one
  non-stop shingle) at or above `minSimilarity` reports; below it, none. The
  score is the exact Jaccard over both passages' **full** shingle sets, so a
  pruned shingle changes which pairs are looked at and never what one scores.
  *Test:* with `minSimilarity:0.40`, a constructed pair scoring exactly 0.40 and
  one scoring 0.39 → one pair. The boundary must be asserted from **both**
  sides: either alone passes under both `>` and `>=`, the trap ANTS-3653 INV-40
  records. Concrete values are what make this writable at all.
  The candidate qualifier is not a hedge added to make the invariant true — it
  is the seam § 2.2 draws, and without it this invariant and INV-7 are flatly
  contradictory: a pair whose every shared shingle is a stop-shingle is never
  compared, so an unqualified "at or above `minSimilarity` reports" is false by
  construction. § 5 states what the qualifier costs.
- **INV-2** — Fenced blocks never pair, and a masked fence line breaks a
  paragraph rather than joining it. *Test:* the same twelve-line code sample in
  two docs, nothing else shared → zero pairs; plus one doc where two otherwise
  identical prose runs are separated by a fence and another where they are not →
  the first yields two paragraphs, the second one.
- **INV-3** — All three § 2.4 exclusions hold, and structural boilerplate is *not*
  excluded. *Test:* a repeated 8-word note (under `minWords`), and the same
  pointer paragraph in two files → zero pairs each; **plus two sibling specs
  sharing an identical spec header block → one pair**. Three details are what
  make the rows assert anything, and each was found by a mutation surviving or
  nearly surviving:
  the pointer must be a **long** link (a `see § 4` is three words and `minWords`
  excludes it before the pointer rule is reached, so a short one measures
  nothing); a paragraph merely *containing* a link must still pair, or the rule
  could be "any paragraph with a link in it"; and the shared header block must
  itself clear `minWords`, or an implementation reinstating a header-block
  filter has nothing to delete and passes. The last row is the regression guard
  for § 2.4's deletion: such an implementation passes every other row here.
- **INV-4** — Each pair appears once, canonicalised so `a` precedes `b` in
  (file, line) order, and no paragraph pairs with itself. *Test:* three mutually
  similar paragraphs → three pairs, not six, with every `a` before its `b`.
- **INV-5** — Report-only: no finding is `autoFixable` and the engine writes
  nothing. *Test:* a run over a fixture corpus → every finding
  `autoFixable:false`, and `auto_fixable` **absent** from the wire (ANTS-3664
  INV-1 omits the key when false, so asserting `auto_fixable:false` fails
  against a conforming serialiser); plus a source-scrape of the engine for
  `WriteOnly` / `QSaveFile` / `QTextStream` / `ReadWrite` / `Append` → zero
  hits. The behavioural arm needs its own guard that the fixture produced
  findings at all, or the loop iterates zero times and passes.
- **INV-6** — `maxPassages` caps the paragraphs **compared**, not those
  enumerated: the walk still counts them all, so `passages_total` is a true
  count and `truncated:true` is set when it exceeds the cap. *Test:* a corpus of
  30 paragraphs at `maxPassages:10` → `truncated:true`,
  `passages_total:30`, `passages_compared:10`. Counting-then-comparing is what
  makes the two numbers consistent; a run that simply stopped could not report
  the first.
- **INV-7** — A shingle appearing in more than `maxPostings` paragraphs is not
  used for candidate gathering — **and is still counted in every score**
  (§ 2.2). *Test:* a corpus where one 3-gram appears in every paragraph and one
  genuine pair exists → the pair is still found, its similarity is the exact
  full-set Jaccard, and a candidate-comparison counter stays below the all-pairs
  count. The fixture must set `maxPostings` low enough to fire, because at the
  shipped 400 it does not fire on the real corpus (§ 4) and a test that runs it
  against `docs/` would assert nothing.
- **INV-8** — The verb-contract minimum `mcp-tools.md` § Tests requires of every
  `Required` tool, in the arms ANTS-3601 INV-15 establishes. *Test:* three verb
  calls — absent `caller_cwd`, root-escaping `path`, and a well-formed
  non-existent in-root `path`.

- **INV-9** — `clusters[]` is the connected components of `pairs[]`: every
  reported passage appears in exactly one cluster, `size` equals its passage
  count, and `maxSimilarity` is the highest score among its pairs.
  *Test:* three passages pairing A–B, B–C but not A–C → **one** cluster of size
  three, not two of size two; plus two unrelated pairs → two clusters. The
  transitive row is the assertion: an implementation grouping only
  directly-scored pairs returns two clusters here and passes every other row.

## 4. RAM / build cost

**This is the one verb in the family whose cost is not linear, and it is bounded
three ways.**

**Two structures are held for the duration of the run, and an earlier draft of
this section claimed only one.** The inverted index maps shingle → passage ids
and is what avoids the naive all-pairs pass. The per-passage shingle-id sets are
held too, because § 2.2's exact score needs **both** ends of a candidate pair at
once — the draft's "a shingle set is built per paragraph and discarded after
that paragraph's comparisons" describes a streaming design that cannot compute
`|A ∩ B|` for a pair discovered later in the walk. Retaining the sets is the
cost of scoring exactly, and it is named rather than absorbed.

Arithmetic from the stated caps rather than an assertion: at `maxPassages` 20,000
paragraphs averaging ~60 words, each contributing ~58 3-gram shingles, that is
~1.2 M postings in the index and the same ~1.2 M ids in the passage sets —
~2.4 M `int`s, on the order of 10 MB, plus the interned shingle keys. Transient,
dropped at end of call — no cache, nothing held across calls. Measured against
the real corpus the caps have headroom but not a lot of it: `docs/` produces
12,995 passages against the 20,000 cap (65%) and 580,149 postings against the
~1.2 M budgeted (48%). The budget is a ceiling this corpus already fills half
of, not an unreachable one. Lowering `maxPassages` is the lever if that is too
much on a constrained host.

Time is O(passages × average non-stop candidates). **The `maxPostings` prune
(INV-7) is insurance against a pathological corpus, not a live optimisation on
this one, and the earlier claim that it was needed here does not survive
measurement.** Measured 2026-07-28 by the shipped engine over `docs/` at the shipped settings:
12,995 passages, 471,079 distinct shingles, 580,149 postings — and **zero**
shingles exceed 400 postings. The longest posting list in the corpus is **176**,
or 1.4% of the passages. Turning the prune off changes nothing: the same 275
pairs, the same 128 clusters, not one pair gained or lost.

It ships anyway, for two reasons worth separating from the discredited one. A
corpus is not required to look like this one — a doc set that stamps one banner
into 5,000 files gives that banner's shingles a 5,000-entry posting list and an
unpruned run scans 25 M candidate slots for it. And the prune's cost when it
never fires is one length comparison per shingle. Cheap insurance against a
quadratic blow-up is worth carrying; what is not worth carrying is a spec
sentence asserting the fire has already happened. § 1.1 shows no such thing, and
saying it did is how a plausible cost model outlives the measurement that
refutes it.

Build cost is the family default (ANTS-3664 § 4). No deviation: Jaccard over
`QSet<QString>` needs no external dependency.

## 5. Out of scope

- **Deciding which copy is canonical, and any auto-fix** — § 2.5.
- **Semantic similarity.** Shingles find shared *words*. § 1.1's four-way pair at
  J=0.17 states one idea in four rewordings and no lexical measure reaches it;
  that is the `must_agree` class the cold-eyes fix ledger exists for. Stated so a
  clean run is never read as proof of no duplication.
- **Cross-repo comparison.** One project root, one walk.
- **Prose quality.** Two passages may be near-identical and both correct.
- **A pair sharing only stop-shingles.** INV-1's candidate qualifier, stated as
  a limit rather than buried in it. Such a pair is never compared and so never
  reported, whatever it would have scored. The exposure is bounded and measured:
  at `maxPostings:400` no shingle in this corpus qualifies (§ 4), so the set of
  unreachable pairs is currently empty; lowering `maxPostings` trades recall for
  time and this is the recall being traded. Named here so a clean run is not
  read as proof of no duplication — the same reason paraphrase is above it.

## 6. Tests

`tests/features/doc_dedup/` (engine, string-literal corpora) and
`tests/features/doc_dedup_verb/` (hooks + refusals). Label `features;fast`,
wired into existing bundles' `SOURCES`, per `tests/features/README.md`.

| INV | Covered by |
|---|---|
| 1 | `DocDedup.Inv1ThresholdFromBothSides` |
| 2 | `DocDedup.Inv2FenceAwareness` |
| 3 | `DocDedup.Inv3ExclusionsAndTheDeletedOne` |
| 4 | `DocDedup.Inv4PairsCanonicalAndUnique` |
| 5 | `DocDedupVerb.Inv5ReportOnly` |
| 6 | `DocDedup.Inv6PassageCapCountsAll` |
| 7 | `DocDedup.Inv7StopShinglePrune` |
| 8 | `DocDedupVerb.Inv8RefusalMinimums` |
| 9 | `DocDedup.Inv9ClustersAreConnectedComponents` |

**Verified RED against feature-absent code before the implementation lands**,
per the project convention. INV-2's and INV-3's zero-pair rows assert an absence
and pass trivially against an engine that pairs nothing; they are re-proven by
mutation — drop the fence mask, drop each exclusion, and *reinstate* a
boilerplate filter — recording what each run actually turns red, including any
mutation that turns nothing red.

**Plus `DocDedup.DISABLED_CorpusCalibration`** — not an invariant row and not run
by `ctest`. It walks the real `docs/` tree at the § 2.3 defaults and prints the
passage count, the pair and cluster counts at each threshold edge, the longest
posting list, and both § 1.1 probe pairs with their scores. It asserts § 2.3's
restated acceptance criterion, so the number is checked rather than admired, and
it prints on both outcomes: every constant in this spec came from a run like
this one, and the next person to change one needs the same instrument rather
than a re-derivation. `qInfo` is suppressed in the test bundles — it writes to
`stderr` directly.

## 7. Open questions — none; both are answered

Kept as a heading because ROADMAP cites it, and answered here in one line each
rather than restated, because restating them is the defect this verb detects.

- **Does it honour `doc-examples` regions (ANTS-3659)?** No. Measured, with the
  three numbers, in **§ 2.4**.
- **Within-document pairs, or only across documents?** Both. Measured, with the
  split, in **§ 2.5**.

The process note is worth more than either answer. Both questions said "the
calibration run will settle this"; ANTS-3666 ran, its result was folded into six
sections of this spec, and **neither question was measured** — the run had not
been asked for either number. A question deferred to a future run needs the
number it wants named in the deferral, or the run answers the questions it
happened to be built for and the deferral quietly expires. Loop 4 did not catch
it either: § 7 *reads* as settled.

## 8. Cross-doc impact

- **ANTS-3664** is a blocker; findings are `DocFinding::Finding`.
- **ANTS-3663** composes this verb and needs § 7's region answer before it
  ships. It hoists **both** `pairs[]` and `clusters[]` to its top level
  (its § 2.1) — hoisting only `pairs[]` would hand the composer, the caller this
  verb exists for, exactly the unreadable output § 2.5 adds clustering to
  prevent.
- **`doc_lint`'s fix path** (ANTS-3663 § 2.3, ROADMAP item ANTS-3669) must
  never auto-fix a `doc_dedup` finding — § 2.5's reasoning is the reason it is
  refused there by name.
- **`docs/subsystems.md`** — `src/docdedup.{h,cpp}` joins `ants_core_lib`.
- **`/cold-eyes` § 1e** gains this as its first mechanical duplication check,
  beside a stated hand-rolled fallback — the skill is global and this verb is
  not (ANTS-1581).

## Cold-eyes loop log

| Loop | Date | Lanes | C | H | M | L | I | Outcome |
|---|---|---|---|---|---|---|---|---|
| 4-merge | 2026-07-28 | cross-doc knock-ons from the ANTS-3669 merge; no reviewer dispatched | 0 | 0 | 1 | 1 | 0 | Two pointer changes, no contract change. § 4's build-cost stanza was one of six identical copies — the highest-scoring duplicate ANTS-3666 measured — so it is deleted in favour of ANTS-3664 § 4's family default, leaving only this spec's genuine deviation (Jaccard over `QSet<QString>` needs no external dependency). § 5's "ANTS-3669, the write half of `doc_lint`, must never auto-fix a `doc_dedup` finding" repoints to ANTS-3663 § 2.3, which absorbed the fix path when that split was unwound; the constraint itself is unchanged and still names this spec's § 2.5 as its reason. This document's own INV-8 was adopted corpus-wide as the pointer form for the verb-contract minimum. |
| 4-tail | 2026-07-28 | ANTS-3677 fold-in (loop 4's deferred MEDIUM/LOW, this document's share) | 0 | 3 | 3 | 1 | 0 | 7 folded, no new loop dispatched — the ledger already had them with file:line, and re-reviewing to rediscover written-down findings is the waste ANTS-3677 exists to avoid. **The fold-in re-ran the measurement instead of reasoning from the recorded table, and that is what earned its keep.** INV-1 ↔ INV-7 were genuinely contradictory; the fix is a seam, not a wording change — § 2.2 now separates candidate gathering (which `maxPostings` prunes) from scoring (exact Jaccard over full sets), and each invariant governs one half, with § 5 carrying the residual limit. § 4's "this corpus has many high-posting shingles" is **false**: zero shingles exceed 400 postings, the longest list is 176 of 12,915, and disabling the prune changes not one pair — the prune ships as insurance and § 4 now says so. § 7's two questions were answered by measuring (regions: 3/268 docs, 67/276 pairs, all one document and one cluster → ignore the mask, § 2.4; within-document: 78/198 split with a J=1.000 verbatim repeat at the top → keep, § 2.5); both had been deferred to a run that was never asked for either number. § 2.3's criterion got a number (≤150 clusters, measured 129) and a harness to check it. Four figures (79 passages, 40→21 exact duplicates, 91→21 at 0.80) were quoted off the punctuation-KEPT rows of a spec that pins punctuation stripped — restated as 109, 58→23, 111→25. Two cited line numbers had drifted 128 and 78 lines in two days; replaced with content anchors per `documentation.md` § 1.7. |
| 4 | 2026-07-28 | dedup depth (breadth pass returned CLEAN; **overridden** — its report covered only dimension 1 and never reached the ANTS-3666 rewrite) | 1 | 6 | 7 | 7 | 0 | 21 raised; the CRITICAL and the ANTS-3666 knock-ons fixed, the rest deferred to **ANTS-3677** under a weekly-budget stop. CRITICAL: § 2.6 gave the engine no entry point at all, so an implementer would re-open every document and break ANTS-3663's one-read guarantee — now an explicit `add`/`finish` pair, stated as ANTS-3664 § 2.3's deliberate exception since this checker alone compares *between* documents. Two defects were my own ANTS-3666 fold-in: `clusters[]` was added to § 2.5 without checking the composer's hoist list (ANTS-3663 hoisted `pairs[]` only, handing `doc_lint` exactly the unreadable output clustering exists to prevent), and INV-9 shipped with no row in § 6's table. The "punctuation moves scores 35–50%" claim contradicted my own § 1.1 table — it is 47% on one pair and 7% on the other; restated with both. The lesson for the ledger: ANTS-3666's three-question gate was run against § 4, and the cell that actually broke was a sibling document's envelope. |
| 2 | 2026-07-27 | producers depth (joint batch review) | 1 | 2 | 2 | 3 | 0 | 8 verified. CRITICAL: loop 1's calibration table — which loop 1's own fixes were built on — **does not reproduce**. Loop 2 re-derived both pairs from the shipped text and got 0.038 and 0.652 against loop 1's 0.53 and 0.44, in opposite directions. Cause is partly this spec's own § 2.1: it drops a bullet's marker line but keeps its continuations, so an `INV-N` bullet is scored as a headless fragment, and § 2.1 never states whether punctuation is stripped (a 6× swing on one pair). **Spec HELD on ANTS-3666**, a measurement spike, rather than folding a third set of guessed constants. Also found: INV-7's stop-shingle prune silently overrides INV-1's threshold (a pair whose shared shingles are all pruned is never compared); blockquote lines are in no break class; and § 2.6 restates the hook list one sentence after declaring it would not. The lesson recorded on ANTS-3666: a spec whose central constants are guesses cannot be reviewed, because a reviewer can only check a guess against another guess. |
| 1 | 2026-07-27 | dedup-depth + cross-doc (joint batch review of ANTS-3660/61/62/63/64) | 1 | 3 | 5 | 3 | 0 | 12 verified, all fixed. CRITICAL: every tuning constant was named and none valued, making INV-1's boundary test unwritable. The lane then **measured** the algorithm against this batch's own duplicated passages and found J = 0.17–0.53 — below any conventional threshold, with the two strongest real pairs at 19–31 words, under the `minWords` the draft would have set. That inverted the design: threshold recalibrated from the measured pairs (§ 1.1), the ill-defined structural-boilerplate exclusion deleted rather than tightened, and a `maxPostings` stop-shingle prune added because the O(p²) worst case was real on exactly this corpus. Also added: § 2.1 precise segmentation, § 2.6 files and hooks (the only spec in the batch missing one), § 7 open questions, and `maxDocsPerRun` which the ROADMAP bullet required and the draft dropped. |
| impl | 2026-07-28 | implementation fold-in (no review dispatched — these are findings only code could produce) | 0 | 1 | 2 | 0 | 0 | Implementation folded three more findings the spec could not have had before there was code. (1) § 4's RAM claim that a shingle set is "discarded after that paragraph's comparisons" describes a streaming design that cannot compute the exact Jaccard of a pair discovered later in the walk; the per-passage sets are retained and § 4 now names the cost. (2) § 2.6's `add`/`finish` were written as free functions, which would need a file-scope corpus and stop being callable twice in one process; they are members of `DocDedup::Accumulator`. (3) § 2.4's pointer-line exclusion had two examples and no predicate — the same charge that deleted the boilerplate exclusion four lines below it — and now has one. Every figure in §§ 1.1/2.3/2.4/2.5/4 was restated from the throwaway probe's output to the shipped engine's (§ 1.2), which differ for two stated reasons: the conforming CommonMark fence primitive and the pointer exclusion the probe lacked. INV-3's and INV-5's *Test:* clauses were rewritten to describe the fixtures that actually ship, both of which are sharper than the clause originally asked for. The `4-tail` row above is left FROZEN with the probe's figures, which were correct when it was written; § 1.2 is the reconciliation. |
