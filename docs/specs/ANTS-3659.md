# ANTS-3659 — an examples region the doc scanners honour

**Status:** spec draft, cold-eyes loop 1 folded (2026-07-27).
**Kind:** enhancement.
**Source:** ROADMAP.md ANTS-3659 (in-session-2026-07-27, first real-doc run of
`doc_citations`).
**Blocked by:** none — `MarkdownScan::fenceMask` (`src/markdownscan.h:77`) and
`DocCitations::scan` (`src/doccitations.h:120`) both already ship.
**Blocker for:** ANTS-3661 (`doc_symbols`), which meets the identical problem
one layer over: a document teaching symbol resolution names symbols that do
not resolve, on purpose.
**Composes with:** ANTS-3653 (the citation scan, whose fence rule this
parallels) and ANTS-3636 (the read path, whose antecedent tracker this resets).

**Layman:** Documents that *teach* how file references work have to write
made-up ones like "see `src/foo.cpp`, line 12". The checker reads those as
broken file references. This gives an author a way to mark a passage as
"illustrations, not real", so the checker stays quiet there — and stays loud
everywhere else.

---

## 1. Problem

`doc_citations` shipped 2026-07-27 and was pointed at the corpus the same day.
On `docs/specs/ANTS-3636.md` — the spec that *defines* the citation grammar — it
reports 37 citations and 22 `unparsed[]` entries. **Fifty-eight of those 59
tokens are illustrations the document authors deliberately. One is a real
citation** (`docs/specs/ANTS-3636.md:1805`, citing
`src/claudeintegration.cpp:10181` as evidence for the description-prefixing
loop).

That ratio is the whole problem, and it is not a complaint about correctness.
The verb is right: `src/foo.cpp` genuinely does not exist. Three consequences,
in the order they bite:

1. **The consumer drowns.** `/cold-eyes` § 1e runs `doc_citations` per in-scope
   doc and feeds its output to a reviewer as pre-verified mechanical findings. A
   lane handed 58 findings of which 0 are real does not learn that this document
   is fine; it learns that this check is noise. A deterministic gate that
   reviewers discount is worse than no gate, because it still costs the run.
2. **The affected set is exactly the docs that matter most here.** It is the
   `ANTS-3636` / `3653` / `3654` family — the documents *about* the citation
   grammar. Every future spec in this lane joins it, including this one, which
   cannot state its own fixtures without writing citation-shaped text.
3. **One illustration is already borrowing a real file's identity.** At
   `docs/specs/ANTS-3636.md:1829` a bare `` `:1-9999999` `` is written to
   illustrate the range-clamp bound. It is a continuation, so ANTS-3636 § 2.4's
   sticky rule hands it the antecedent from the genuine
   `src/claudeintegration.cpp` citation twenty-four lines earlier, and it
   resolves `ok`, returning that file's line 1 with `range_truncated:true`. An
   illustration is currently able to inherit a real path and report a healthy
   status against it. That is not noise; it is a wrong answer.

**Why the obvious cheap fix does not work.** The tempting opt-out is a
declaration of the fictional path names a doc uses — `src/foo.cpp`,
`src/a.cpp`, `src/gone.cpp` — checked against each citation's path. It fails on
measurement: **four of the five illustrations that resolve `ok` cite real
files.** `docs/specs/ANTS-3636.md:581` cites `CMakeLists.txt:148` to show which
resolution step catches a repo-root file; `docs/specs/ANTS-3636.md:1067` uses
`CMakeLists.txt:1` as a literal inside INV-5's `*Test:*` clause;
`docs/specs/ANTS-3636.md:131` and `:163` quote `src/mcpprojection.cpp:28` as a
*specimen of a rotted citation* (the spec's own § 1 notes that line "is today
blank"); and `docs/specs/ANTS-3636.md:1829` is the inheriting continuation
above. None of the four names a fictional path, so a name-keyed rule leaves all
four in place while claiming to have solved the problem.

**Why a per-citation opt-out does not work either.** The 58 tokens sit in more
than a dozen separate passages spread across a 1,873-line document. A marker per
citation is 58 edits and reads worse than the noise.

What is actually true of all 58 is positional: they sit in passages *whose
subject is the notation*. A region is the shape of the thing.

## 2. Surface

### 2.1 The marker

An HTML comment, alone on its line:

```
<!-- doc-examples: begin -->
...prose whose file references are illustrations...
<!-- doc-examples: end -->
```

Recognised by `^ {0,3}<!--[ \t]*doc-examples:[ \t]*(begin|end)[ \t]*-->[ \t]*$`.
Four spelling rules, each with a reason rather than a preference:

- **Alone on its line.** A marker sharing a line with prose would need the scan
  to decide whether the prose before it is inside or outside the region. There
  is no reading of that which is not arbitrary.
- **Up to three leading spaces, spaces only.** The same indent rule
  `MarkdownScan::fenceRe` uses, and space-only for the same reason a tab never
  opens a fence (ANTS-3598): a `\s` class admits `\r` and would let a stray
  carriage return open a region.
- **Lowercase keyword.** `doc-examples`, `begin`, `end`. One spelling to grep
  for. Case-insensitivity buys an author nothing and costs every consumer a
  second pattern.
- **Verb-neutral name, not `doc-citations`.** ANTS-3661's `doc_symbols` needs
  precisely this region over precisely this prose — a doc teaching symbol lookup
  names fictional symbols in the same passages. A verb-scoped marker would put
  two comments on one paragraph. Which verbs *honour* the region is each verb's
  own policy, exactly as ANTS-3653 § 3 already has `codeSpans` shared while its
  masking policy inverts between `doc_integrity` and `doc_citations`.

**Input precondition: lines arrive `\r`-stripped.** The trailing `[ \t]*$`
matches neither `\r` nor `\n`, so a CRLF document whose lines still carry `\r`
would silently never open a region — the exact failure the space-only indent
rule above exists to avoid. `DocCitations` already satisfies this
(`src/doccitations.cpp:299` chops a trailing `\r` in `splitLines`), but
`exampleMask` is a shared primitive whose other `MarkdownScan` callers split
their own input, so the precondition is stated rather than assumed.

### 2.2 The mask

A new primitive beside the other two in `src/markdownscan.{h,cpp}`:

```cpp
// ANTS-3659 — per-line "inside a doc-examples region" mask, one bool per input
// line. The marker lines are themselves masked true (they are region syntax,
// not prose). An unterminated region masks true to end-of-input, matching
// fenceMask's CommonMark leniency, and reports its opener in
// *unterminatedOpenerLine (-1 when balanced; nullptr accepted).
//
// `fence` is required, not optional: a marker inside a fenced block is sample
// text, and this spec is a document that shows the marker inside a fence. It
// must be the mask for THESE lines — fence.size() == lines.size().
//
// Lines must arrive with any trailing \r removed (§ 2.1).
QVector<bool> exampleMask(const QStringList &lines, const QVector<bool> &fence,
                          int *unterminatedOpenerLine = nullptr);
```

It lives in `MarkdownScan` rather than in `doccitations.cpp` because ANTS-3661
is a named second consumer, and because the ANTS-3649 hoist established that a
primitive two doc verbs both need must not exist twice.

Three degenerate inputs, decided rather than left to the implementation:

- **A `begin` inside an open region is ignored.** Regions do not nest. Nesting
  would need a depth counter to express something no author has a use for.
- **An `end` with no open region is ignored.** It is an authoring slip, but
  reporting it needs a second response field for a case that cannot suppress
  anything — a stray `end` masks nothing, so the failure is already visible as
  "my markers did nothing".
- **A marker line inside a multi-line inline code span is still a marker.**
  `MarkdownScan::codeSpans` may cross a newline (ANTS-3653 INV-33), so a span
  opening on one line and closing on a later one can contain a whole line that
  looks exactly like a marker. Honouring it is the accepted behaviour: making
  `exampleMask` consume `codeSpans` would have the lighter primitive depend on
  the heavier one, and on a document shape with no instance in this corpus.
  Fences, which *are* how samples are written here, are excluded above.

### 2.3 What the region suppresses

`DocCitations::scan` emits **neither a `citations[]` entry nor an `unparsed[]`
entry** whose `docLine` falls on a masked line. `docLine` — not the enclosing
span's opening line — is the test, per ANTS-3653 INV-33: a citation inside a
span that crosses a newline takes the line its own text begins on, and that is
the line the mask is indexed by.

**The two arrays are not the two halves of the measurement, and the layering
matters.** `ScanResult::unparsed` carries only `bad_locus` entries (ANTS-3653
INV-24 / INV-40). The 22 `unparsed[]` entries in the § 1 measurement are
`unresolvable` and `out_of_root` — classifications the *read* path assigns in
`DocCitations::check`, and at scan time all 22 are still `citations[]` entries.
Suppressing in `scan` therefore removes them too: a token the scan never emits
is a token `check` never resolves. `ScanResult::unparsed` is masked for
completeness, not because it carries any of the 58.

Two new members on `ScanResult`, following `unterminatedFence`'s existing
sentinel convention, each echoed by `DocCitations::check` under the JSON key
beside it:

| `ScanResult` member | JSON key | Meaning | Omitted from JSON when |
|---|---|---|---|
| `int unterminatedExamples = -1` | `unterminated_examples` | 1-based line of an unclosed `begin` | `-1` (every region closes) |
| `int examplesSuppressed = 0` | `examples_suppressed` | count of tokens the mask dropped | `0` |

`examples_suppressed` exists because `unterminated_examples` only catches the
*runaway* case. A region that is correctly closed but drawn too wide silently
blanks real citations, and without a count the response is indistinguishable
from a document that cites nothing. Both follow ANTS-3636 INV-28's
omit-when-false discipline.

**Suppressed tokens are invisible to every tally.** They are absent from
`count`, from every bucket of `counts`, and from `unparsed_total` — a suppressed
token was never a citation, so ANTS-3636 INV-26's three tally identities hold
unchanged over what survives. `examples_suppressed` is the only field that
counts them, which is why it is not derivable from the others.

### 2.4 The antecedent reset

Both marker lines reset the sticky antecedent, on the same rule and for the same
reason a fenced block does (ANTS-3636 INV-8: "A fenced block resets the
antecedent tracker"). A bare continuation after a region has no antecedent from
before it.

Without this, a bare `` `:45` `` after a region inherits whichever real path was
last cited before the region — the § 1 consequence 3 defect, merely displaced
past the marker instead of fixed. The reset fails safe: the continuation becomes
`unresolved`, which is a citation the author can see and correct, rather than
`ok` against a file they never named.

**This is a second computation site, not a field on `ScanResult`.** The
antecedent tracker lives in `DocCitations::check`, which already computes its
own `MarkdownScan::fenceMask` and `codeSpans` over the scanned prefix
(`src/doccitations.cpp:661-662`) independently of `scan`'s at
`src/doccitations.cpp:199`. `check` computes `exampleMask` there too, beside
them, and uses it exactly as it uses the fence mask. Suppression has already
removed the in-region tokens, so what `check` needs the mask for is only the
boundary between two surviving tokens.

## 3. Invariants

Ids continue the ANTS-3636 family's sequence, whose highest live id is INV-48
(verified against ANTS-3636, ANTS-3653 and ANTS-3654 — ANTS-3654 holds only
INV-13 and INV-14).

INV-49 through INV-52 and INV-54 assert against `DocCitations::scan`'s
`ScanResult` — so "absent" means the member holds its sentinel, and counts are
`citations.size()` / `unparsed.size()`. INV-53 asserts against
`DocCitations::check`'s JSON, because inheritance is the read path's.

- **INV-49** — `scan` emits neither a `citations[]` nor an `unparsed[]` entry
  whose `docLine` is masked; outside a region nothing changes. *Test:* a fixture
  with `src/a.cpp:1` before the region, `src/foo.cpp:12` **and a malformed
  `src/a.cpp:0`** inside it, and `src/a.cpp:2` after → `citations.size() == 2`,
  `unparsed.size() == 0`. The in-region pair must be one of each array, and the
  `unparsed[]` one must be a **`bad_locus`** token: `ScanResult::unparsed`
  carries no other reason (§ 2.3), so a fixture using a merely-unresolvable path
  like `6.2:1` puts two *citations* in the region, leaves `unparsed[]` empty
  either way, and asserts nothing about the second array.
- **INV-50** — A marker line inside a fenced block is not a marker. *Test:* a
  fixture whose ```` ```markdown ```` sample contains a `begin` marker, followed
  by `src/foo.cpp:12` in prose **after the fence closes** → the citation is
  emitted and `examplesSuppressed == 0`. Breaks when `exampleMask` is computed
  without the fence mask — which is the state this spec's own § 2.1 would put
  the corpus in, since it shows the marker inside a fence.
- **INV-51** — An unterminated region masks to end-of-input and sets
  `unterminatedExamples` to the 1-based line of the unclosed `begin`; it stays
  `-1` when every region closes; a stray `end` and a nested `begin` are both
  ignored. *Test:* four fixtures — an unclosed `begin` at line 3 with a citation
  at line 9 → `unterminatedExamples == 3`, no citations; the same fixture closed
  → `-1`, one citation; a lone `end` before a citation → `-1`, one citation; a
  `begin`/`begin`/`end` sequence wrapping one citation followed by a second
  citation → one citation and `-1`, proving the inner `begin` neither nested nor
  re-opened.
- **INV-52** — `examplesSuppressed` is the count of tokens the mask dropped, and
  is `0` when no marker appears. *Test:* INV-49's fixture → `examplesSuppressed
  == 2`; a marker-free fixture → `0`. Breaks when the count is taken from
  `citations[]` alone, which INV-49's fixture distinguishes precisely because
  its region holds one citation and one `bad_locus` token.
- **INV-53** — A region resets the sticky antecedent, as a fence does. *Test:*
  via `DocCitations::check` — `src/a.cpp:1`, then a closed region, then a bare
  `` `:45` `` → `unresolved`, not `ok` against `src/a.cpp`. The antecedent must
  be *real* and the continuation *outside* the region: an antecedent inside the
  region is suppressed anyway and the fixture would pass without any reset rule.
- **INV-54** — A line failing any of § 2.1's three mechanical spelling rules is
  not a marker and suppresses nothing. *Test:* four fixtures, each a lone
  candidate line followed by `src/a.cpp:1` → an uppercase
  `<!-- DOC-EXAMPLES: BEGIN -->`, a four-space-indented marker, a
  tab-indented marker, and a marker with prose after the `-->` → the citation
  survives in all four and `examplesSuppressed == 0`. Without this invariant a
  case-insensitive or trailing-prose-tolerant regex passes INV-49..53 intact,
  because those fixtures only ever write well-formed markers.

## 4. RAM / build cost

Two masks per `check` call, not one: `scan` computes `exampleMask` over the
scanned prefix, and `check` computes it again for the antecedent reset (§ 2.4) —
the same duplication the fence mask already pays at `src/doccitations.cpp:199`
and `:661`. Each is one `bool` per scanned line, allocated and discarded within
its own call, bounded by `Options::maxDocLines`. `QVector<bool>` is not
bit-packed, so at the 50,000-line ceiling that is ~50 KB per mask, ~100 KB
transient for both. No cache, no cross-call state, nothing to evict.

Time is one regex match per line — the same shape as `fenceMask`, linear in the
line count, with no backtracking risk: the pattern is anchored at both ends and
its only repetition is over `[ \t]`.

No new translation unit: `exampleMask` joins `src/markdownscan.cpp`, already in
`ants_core_lib`. No new external dependency. One new feature-test source file
into an existing bundle's `SOURCES`, per `tests/features/README.md` — not an
`add_executable`.

## 5. Out of scope

- **Whether `doc_dedup` honours the region** — ANTS-3660's call, and plausibly
  no: two identical teaching examples in two specs may be a genuine duplication
  worth surfacing. The mask is shared; the policy is per verb.
- **Retro-fitting markers across the corpus** — this spec applies them to the
  four docs that measurably need them (`ANTS-3636.md`, `ANTS-3653.md`,
  `ANTS-3654.md` and itself, § 7) and stops. A corpus sweep is speculative until
  another doc reports noise.
- **A verb-scoped variant** (`<!-- doc-examples: begin symbols -->`) — no
  measured need for a passage that is illustrative for one verb and literal for
  another. Add it when one appears, not before.
- **Inferring "this is an example" from placeholder-looking names** — refused
  outright, per the ROADMAP bullet: this repo has real files with generic names,
  and a heuristic that guesses would suppress real rot silently.

## 6. Tests

One feature-conformance directory, `tests/features/doc_citations_examples/`,
with `spec.md` naming this document as its contract. Label `features;fast`.
Wired into the same bundle as `tests/features/doc_citations_scan/` (target
`test_core`, `CMakeLists.txt:2024`), and sharing the fixture helper from
`tests/features/doc_citations/` rather than copying it.

INV-49..52 and INV-54 are string-literal fixtures against `DocCitations::scan`
with no filesystem access, matching ANTS-3653 § 5's rule. INV-53 needs
`DocCitations::check` and a temp dir, because inheritance is the read path's.

| INV | Covered by |
|---|---|
| 49, 52 | `DocCitationsExamples.Inv49RegionSuppressesBothArrays` |
| 50 | `DocCitationsExamples.Inv50FencedMarkerIsNotAMarker` |
| 51 | `DocCitationsExamples.Inv51UnterminatedAndDegenerateMarkers` |
| 53 | `DocCitationsExamples.Inv53RegionResetsAntecedent` |
| 54 | `DocCitationsExamples.Inv54MalformedMarkerIsNotAMarker` |

**Verified RED against feature-absent code before the implementation lands**,
per the project convention. Which rows can fail honestly and which cannot:
INV-49's citation count, INV-51's `unterminatedExamples`, INV-52's count and
INV-53's `unresolved` all fail RED for their stated reason. INV-50's and
INV-54's "the citation survives, `examplesSuppressed == 0`" rows **pass
trivially** today, because nothing suppresses anything yet — a negative
assertion cannot tell "correctly declined to mask" from "the feature does not
exist". Those rows earn their keep only under mutation, once the mask exists:
build an `exampleMask` that ignores the fence mask (INV-50) and one with a
case-insensitive, `$`-less pattern (INV-54), and confirm each row goes red under
its own mutation and no other — the discipline `tests/features/doc_citations_anchor/spec.md`
records for ANTS-3654.

## 7. Cross-doc impact

- **ANTS-3653 § 3** gains a fourth scrape-rule bullet pointing here, beside the
  fence and inline-span rules it already carries. Its invariants are untouched.
- **ANTS-3636 INV-8** (fence resets the antecedent) gains a fixture row for the
  region boundary, and its § 2.4 sticky rule gains a sentence naming the second
  reset trigger. The invariant's claim is widened, not renumbered.
- **`docs/specs/ANTS-3636.md`, `ANTS-3653.md`, `ANTS-3654.md` and this
  document** get markers applied around their example passages — the change that
  closes the ROADMAP bullet. This spec is in the list because it is in the same
  family and has the same problem: its own § 3 and § 6 fixture literals report
  as six `missing_file` citations and one `bad_locus` entry today.
  **Acceptance, stated as a re-run rather than a round number:**
  `doc_citations` over `ANTS-3636.md` returns `count:1` — the genuine
  `src/claudeintegration.cpp:10181` citation at doc line 1805 — with
  `unparsed_total:0` and `examples_suppressed:58`.
- **`docs/standards/documentation.md`** gains a short authoring note: how to
  write a teaching example that does not read as rot.
- **`/cold-eyes` § 1e** — no change required. The skill is global and this verb
  is not (ANTS-1581), so § 1e names `doc_citations` alongside a hand-rolled
  fallback already; the marker changes what the verb reports, not whether the
  skill can call it.
- **`docs/subsystems.md`** — no change: `exampleMask` joins `MarkdownScan` in
  `ants_core_lib`, which that catalogue already covers.

## Cold-eyes loop log

| Loop | Date | Lanes | C | H | M | L | I | Outcome |
|---|---|---|---|---|---|---|---|---|
| 1 | 2026-07-27 | surface (depth); invariants+tests; cross-doc | 2 | 4 | 6 | 6 | 0 | 18 verified, all fixed; 7 dismissed. Both CRITICALs were self-inflicted: INV-49/52's fixture used a token that is not a scan-layer `unparsed[]` entry, and § 1's headline said 6 real citations where 1 is real. Dismissed: no INV id collision with ANTS-3654; `\r` already stripped at `src/doccitations.cpp:299`; empty region, zero-line input and a null out-param all follow the `fenceMask` precedent. |
