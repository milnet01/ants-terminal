# ANTS-1881 — `roadmap_query` `mode:"headline_only"`

**Status:** spec draft, cold-eyes loops 1-10 folded (2026-08-26).
**Kind:** enhancement.
**Source:** ROADMAP.md ANTS-1881 (vestige-feedback-2026-05-26 Wishlist #1).
**Pairs with:** ANTS-1437 (introduced `mode` arg + `section_index`),
ANTS-1521 (`headline_oneline` companion field, ROADMAP-only),
ANTS-1517 (`include_body` opt-in pattern, ROADMAP-only),
ANTS-1499 (etag 304 pattern, ROADMAP-only),
ANTS-1720 (`fields=` projection, ROADMAP-only).

**Layman:** Add a "just the headlines" mode to the roadmap-query
tool so Claude can list what's in a section without pulling all the
multi-paragraph body text — roughly a 10× smaller answer on dense
bundle sections.

## 1. Problem

The 80 % case for `roadmap_query section=<slug>` is *"what is in this
section?"* — bullet bodies are not needed at that step. Today the only
two emission shapes are:

- **Default `mode:"bullets"`** — every bullet carries `id`, `status`,
  `headline`, `headline_oneline`, `kind`, `lanes[]`, `section_slug`,
  plus optional `body` / `format` / `synthetic` / `anchor` / `bold_id` /
  `id_inferred` (ANTS-4575).
- **`mode:"section_index"`** — *per-section* roll-up, no individual
  bullets.

There is no shape between the two. A caller that just wants the bullet
catalogue (id + status + headline + slug) inside a single section pays
~10× the bytes for fields they will discard. On dense bundle sections
where a single bullet body is 2–5 KB, the wire cost dominates the
caller's harness budget.

## 2. Surface

### 2.1 Arg

A further value for the existing `mode` arg on `roadmap_query`:
`mode:"headline_only"`. It joins `cmdRoadmapQuery`'s `kModes` allow-set
(`src/remotecontrol_roadmap_query.cpp`); the rejection envelope
(`code:"bad_mode"` + 64-byte / control-char `?` echo) is unchanged.
Modes added later, and the `sections` alias, belong to their own specs —
this document does not restate that list's membership.

```cpp
if (!kModes.contains(mode)) {
    // existing bad_mode emission
}
```

### 2.2 Per-bullet projection

Three selector shapes in `cmdRoadmapQuery` reach a bullet emit, and
each needs projection applied:

1. **The list path** — builds `m_roadmapCacheBullets` (full-file path)
   or `m_roadmapSectionCache[slug]` (section path) and emits via the
   filtered-array loop.
2. **The `id` branch** — builds `matches[]` by scanning
   `m_roadmapCacheBullets` for the matching id, and returns before
   reaching the list emit.
3. **The `ids` branch** — the same shape for an id set, de-duplicated.
   It projects, and then ANTS-4400's `input_index` loop writes a fifth
   key back onto each bullet. That ordering is the contract, not drift:
   INV-2 exempts this branch, and ANTS-4712 settled it there.

All three live in `src/remotecontrol_roadmap_query.cpp`. When
`mode == "headline_only"`, every one projects each output object to
**exactly four keys** — plus `input_index` on the `ids` branch alone,
per INV-2:

| Key                | Source                                              |
|--------------------|-----------------------------------------------------|
| `id`               | `Bullet::id` (QString)                              |
| `status`           | `Bullet::status` (status emoji QString)             |
| `headline_oneline` | `rcHeadlineOneline(Bullet::headline)`               |
| `section_slug`     | `Bullet::sectionSlug`                               |

No other keys are emitted (no raw `headline`, no `body`, no `kind`,
no `lanes`, no `format`, no `anchor`, no `bold_id`, no `synthetic`).
`input_index` on the `ids` branch is the one exemption, and INV-2 owns
its scope.
Empty string values are permitted (and expected for rollup / narrator
entries — see §2.3) — INV-2 governs the *key set*, not value
non-emptiness.

The narrowing is mode-driven, not flag-driven — `include_body`,
`format`, anchor opt-ins are silently ignored in this mode (consistent
with `section_index`'s ignore-set per INV-7 of ANTS-1437).

### 2.3 Combinator support

| Combinator                 | Behaviour                                  |
|----------------------------|--------------------------------------------|
| `status="active"`          | Honoured — narrows the bullet set, then projects each kept bullet to the four keys. |
| `filter`                   | Honoured — ANTS-3698 accepts it as an alias for `status`, read only when `status` is empty, so `status` wins when both are sent. Behaviour from that point is `status`'s row above, unchanged. Added by ANTS-4703: this verb's own envelope echoes the applied lifecycle as `filter`, so a caller reading a response and hand-writing the next call sends `filter:"active"` — which was an unrecognised arg, dropped silently, and answered with the FULL set under an echo that reads as confirmation a filter was applied. |
| `section="<slug>"`         | Honoured — same flow as bullets-mode. |
| `id="<ANTS-NNNN>"`         | Honoured — single bullet, projected. The id-branch in `cmdRoadmapQuery` projects its `matches[]` array to the four-key shape before emission; the envelope still carries `id`, `found`, `count`, `path`, `bullets[]`, `etag`, `mode` (echoed when caller passed `mode=` per the `hasModeArg` gate), plus `duplicate_ids[]` when applicable. |
| `offset`, `limit`          | Honoured — pagination operates on the *projected* set via `PaginationEngine::pageBullets`; envelope fields (`offset`, `limit`, `total`, `truncated`, `next_offset`) carry through. |
| `max_results`              | Honoured — ANTS-4701 accepts it as an alias for `limit`, read only when `limit` is undefined, so `limit` wins when both are sent. Behaviour from that point is `limit`'s row above, unchanged. Added because `workspace_search` spells this cap `max_results` and the name carried across was dropped into `ignored_args` and answered with the full set — which under this mode is every row. |
| `include_section_headers`  | Honoured. Rollup bullets (themselves gated by `include_section_headers:true`, per `cmdRoadmapQuery`'s `shouldDropUnnumbered` predicate) project to `{id:"", status, headline_oneline:"", section_slug}` — all four keys present, two with empty-string values. |
| `include_narrator_bullets` | Honoured. Same shape as rollup retention — narrator bullets (empty `id`, non-empty `headline`) project to `{id:"", status, headline_oneline, section_slug}`. |
| `fields=[...]`             | Honoured — every verb honours `fields=` (ANTS-4524); there is no allowlist to join and no per-descriptor wiring. Top-level projection still works (e.g. `fields:["bullets","etag"]`). Inside each bullet the four-key shape is the mode's contract, not a `fields` projection. |
| `etag_match`               | Honoured — see §2.5. |
| `include_body`             | Ignored (no body in this mode). Echo unchanged for caller transparency. |
| `ids`                      | Honoured — the `ids` branch projects its `matches[]` to the four-key shape, then ANTS-4400's annotation adds `input_index`, so this path emits five keys. INV-2 owns the exemption. The singular `id` branch takes no annotation and emits four. |
| `query`, `regex`, `whole_word` | Honoured — `applyQueryFilter` runs on the filtered set *before* projection, so the keyword still matches body text this mode does not emit. |
| `max_body_bytes`           | Ignored (no body to cap in this mode), as `include_body` is. |
| `section_etag_match`       | Honoured — read on the `section=` path, which this mode uses. Its short-circuit returns `{ok, unchanged, section, section_etag, path}` and no bullets, so the four-key contract does not arise. The section etag keys on the section's bytes and not on `mode`, so a caller holding one taken under `mode:"bullets"` is answered `unchanged` here; INV-4's cross-mode rule covers only the dispatcher's file etag. |
| `check_sync`, `scope`, `since`, `until`, `slugs_only`, `include_section_etags` | No effect here — the first four belong to `mode:"report"`, the last two to `mode:"section_index"`. Read and ignored rather than refused, so a caller passing one gets this mode's ordinary output. |

### 2.4 Envelope

```jsonc
{
  "ok": true,
  "mode": "headline_only",      // echoed only when caller passed mode= arg
  "path": "/abs/path/ROADMAP.md",
  "filter": "active",
  "bullets": [
    { "id":"ANTS-1881", "status":"📋",
      "headline_oneline":"…", "section_slug":"…" },
    …
  ],
  "etag": "<sha256-hex16 over the projected payload>",
  // pagination fields when applicable: offset, limit, total,
  // truncated, next_offset
}
```

The `legacy_format_sections[]` array and the per-section
`legacy_format:true` flag are `mode:"section_index"`-only
diagnostics (emitted inside the section_index branch of
`cmdRoadmapQuery`); bullets-mode never emits them, so
headline_only — which runs through the bullets-emission code
paths (full-file loop, section loop, id branch) — also never
reaches those emitters. No special carve-out language is needed;
the absence is mode-shape-natural.

The `mode` field is echoed in the envelope **only when the caller
passed `mode=` explicitly** — matching the bullets-mode id-branch
precedent (`cmdRoadmapQuery`'s `hasModeArg` gate in
`src/remotecontrol_roadmap_query.cpp`), not the section_index branch's
unconditional emit. Rationale: headline_only is a
bullets-projection variant; matching the bullets-mode echo gate
keeps default-call envelopes byte-identical with bullets-mode
default-call envelopes (§6 Migration's back-compat claim).

The `duplicate_ids[]` diagnostic IS retained — it's a top-level
roadmap-integrity warning that benefits any caller iterating
bullets, and its payload is trivially small (only present on
collision).

### 2.5 ETag interaction

`roadmap_query` is in `ClaudeIntegration::isEtagSupportedTool`
(`src/claudeintegration.cpp`), which is the precondition for the
dispatcher's etag-injection layer to fire. The etag is computed at
the dispatcher (`ClaudeIntegration::applyEtagPattern` — wraps every
`roadmap_query` response after `cmdRoadmapQuery` returns, so the
*projected* bytes are what gets hashed). Consequently:

- Repeating a `mode:"headline_only"` call with `etag_match` set to the
  prior response's etag returns `{ok:true, unchanged:true, etag:"<same>"}`
  whenever the projected payload is byte-identical to the previous one.
- A bullets-mode call with the headline-only etag does **not** 304
  (different payload bytes → different etag).

### 2.6 Cache reuse

No new cache fields. The existing `m_roadmapCacheBullets` is the full-
fat shape; the projection happens at emit time inside the bullet-emit
loop. The cache predicate (path + mtime + 100 ms TTL) is unchanged.

## 3. Invariants

- **INV-1** — `cmdRoadmapQuery` accepts `mode:"headline_only"`; a value
  outside its `kModes` allow-set returns `code:"bad_mode"` with the
  existing 64-byte + control-char `?` echo hygiene. *Test:* `tests/features/roadmap_query_headline_only/test_roadmap_query_headline_only.cpp::Inv1ModeAcceptedAndRejected`.
- **INV-2** — `mode:"headline_only"` projection emits exactly the
  key set `{id, status, headline_oneline, section_slug}` per bullet,
  plus `input_index` on the `ids` selector alone (ANTS-4712).
  Extra keys fail; missing keys fail. The `ids` exemption is
  deliberate, not tolerated drift: the projection runs before
  ANTS-4400's `input_index` annotation, and that ordering is the
  contract rather than an accident. Results on that path come back in
  DOCUMENT order, so a caller zipping them against its own input array
  mis-pairs silently — which is the defect `input_index` was added to
  close, and it closes it least well in the mode that carries the
  least. Stripping the key to hold a four-key shape would reintroduce
  that defect for the leanest caller. The singular `id` selector takes
  no annotation and emits four. Empty-string values are
  permitted for the rollup case (`id:""` + `headline_oneline:""`,
  per `isRollupBullet` in `src/remotecontrol_roadmap_query.cpp`);
  narrator entries carry `id:""` with non-empty `headline_oneline`
  (`isNarratorBullet` in the same file defines a narrator
  as empty `id` AND non-empty `headline`; from there
  `rcHeadlineOneline(b.headline)` is necessarily non-empty too).
  This matches bullets-mode's narrator emission, so INV-3
  combinator-equivalence on `id` arrays holds. Bullet-level
  `fields=` projection is NOT supported in this mode: the four-key
  shape is the *mode contract*, not a `fields`-narrowed default —
  `fields:["id"]` does NOT narrow the per-bullet object to one key
  (top-level `fields=` still works, see §2.3). Rationale:
  layering `fields=` over a mode whose entire purpose is a fixed
  narrow shape would re-narrow a deliberately narrow shape — the
  contract is owned by the mode, not the projection arg.
  *Test:* `…::Inv2KeySetExactlyFour`, `…::Inv2RollupEmpty`,
  `…::Inv2NarratorHeadlineNonEmpty`;
  `tests/features/roadmap_query_by_ids/test_roadmap_query_by_ids.cpp::Inv2IdsBranchKeepsInputIndex`
  anchors the `ids` exemption and its ordering. That anchor is a
  source scrape, like every test in both suites: the feature bundles
  do not link `cmdRoadmapQuery`, so no test in either suite calls the
  verb. The key set is therefore locked by reading the handler, not by
  observing a response.
  `…::Inv2FieldsDoesNotNarrowBullets` anchors on the mode value
  appearing in the sources; it does not exercise the `fields=` clause.
- **INV-3** — Combinator equivalence with bullets-mode. The bullet
  `id` array (in array order, positional equality — not set-based;
  grounded in the shared `m_roadmapCacheBullets` iteration order)
  under `mode:"headline_only" + status:"<f>" + section:"<s>"` equals
  the bullet `id` array under `mode:"bullets" + status:"<f>" +
  section:"<s>"` for every supported `<f>` and `<s>` — only the per-
  bullet shape differs. **Compared over the filtered set before
  pagination**, or with both calls passing a `limit` below the
  auto-truncate threshold: INV-6 makes the emitted pages differ in
  length by design, so a parity test over emitted `bullets[]` would red
  against correct code.
  *Test:* `…::Inv3CombinatorIdParity`.
- **INV-4** — ETag short-circuits over the projected payload.
  (a) A second `mode:"headline_only"` call with `etag_match` set to the
  first call's etag returns `{ok:true, unchanged:true, etag}` when the
  projected payload is byte-identical. (b) The same etag passed to a
  `mode:"bullets"` call does NOT 304 (response bytes differ → etag
  differs).
  *Test:* `…::Inv4SameModeShortCircuit` (sub-claim a) +
  `…::Inv4CrossModeNoShortCircuit` (sub-claim b). Both are source
  scrapes over `src/claudeintegration.cpp` — the dispatcher, where the
  etag is applied — rather than live calls; see § 4. Neither observes a
  304, so the short-circuit itself is unpinned by this suite.
- **INV-5** — Combinator coverage. Every argument `cmdRoadmapQuery`
  reads — its own `mode` selector and the dispatcher's `caller_cwd`
  aside — is accounted for in §2.3: either the table gives it explicit
  behaviour under `mode:"headline_only"`, or the table names it as
  having no effect here. An argument added to the verb owes a row.
  All three selector shapes of §2.2 project their output.
  *Test:* `…::Inv5IdSelectorProjected` asserts that the singular `id`
  branch's emit site is projected — a source scrape over the handler, not
  a live call, so it does not observe the emitted key set.
  `…::Inv5CombinatorCoverageSourceGrep` asserts only that the shared
  projection helper is *named* somewhere in the concatenated
  `remotecontrol` sources — which its own definition satisfies — so it
  vouches for nothing behavioural and does not compare the read-set
  against §2.3. The `ids` branch IS anchored, by INV-2's
  `Inv2IdsBranchKeepsInputIndex`. What stays open is the read-set
  comparison against §2.3, which no anchor makes.
- **INV-6** — Pagination operates on the projected set.
  `PaginationEngine::pageBullets` measures the projected `QJsonArray`,
  so more `headline_only` bullets fit in one auto-truncate slice than
  the same fixture's bullets-mode call. `total` reflects the full
  filtered set, not the per-page set (matches the existing pagination
  contract).
  *Test:* `…::Inv6PaginationOnProjectedSet`.
  *Restored by ANTS-3577 (2026-07-20):* the shipped code had drifted to
  projecting `page.slice` AFTER the fat measure (measuring the unprojected
  row and over-dropping); the fix projects `filtered` BEFORE pagination at
  both bullet branches, and the INV-6 test now pins projection-before-
  pagination rather than merely asserting `pageBullets` is called.
- **INV-7** — `tools/list` schema carries the value. The `mode` enum in
  the tool descriptor for `roadmap_query` (`src/claudeintegration.cpp`
  `tools/list` builder) contains `headline_only`, alongside the values
  other specs own, so client-side schema validators do not reject it.
  Per §2.1 this document does not restate the enum's membership.
  *Test:* `…::Inv7ToolsListEnumerates` — run the test the way a
  human would: `grep -nP 'modeEnum\.append\("headline_only"\)'
  src/claudeintegration.cpp` (the descriptor's enum population site,
  NOT the descriptor's prose `description` string — the prose may
  contain the literal `"headline_only"` too once §7 lands, but the
  test verifies the enum population, so the anchor is the
  `modeEnum.append("headline_only")` token itself). Matches in the
  `cmdRoadmapQuery` allow-set (`src/remotecontrol_roadmap_query.cpp`),
  in a test fixture, or in a comment do NOT count.
- **INV-8** — Top-level diagnostic parity by shared lineage.
  `mode:"headline_only"` projects from the same code paths as
  `mode:"bullets"` (full-file emit loop, section emit loop, `id` and
  `ids` branches), so it emits the same top-level diagnostic set:
  `duplicate_ids[]` is present iff the same fixture under
  `mode:"bullets"` would emit it, with byte-identical contents (the
  diagnostic is computed over the shared `m_roadmapCacheDuplicateIds`
  cache, not the projected bullets). `legacy_format_sections[]` and
  per-section `legacy_format:true` are emitted only inside the
  `mode == "section_index"` branch of `cmdRoadmapQuery` —
  neither bullets-mode nor headline_only reaches them, so they
  are naturally absent in both modes.
  *Test:* `…::Inv8DuplicateIdsParity` asserts that the
  `m_roadmapCacheDuplicateIds` emit is present in the handler and sits
  outside the `headline_only` branch — a source scrape, so the parity
  itself is reasoned from the emit's position rather than observed.

## 4. Tests

`tests/features/roadmap_query_headline_only/`:

- **`spec.md`** — mirrors INV-1..INV-8 in test-anchor form; several
  invariants carry more than one anchor.
- **`test_roadmap_query_headline_only.cpp`** — the INV anchors named in
  §3, each a scrape over the concatenated `remotecontrol` sources.

**The suite does NOT call the verb.** The feature bundles do not link
`cmdRoadmapQuery`, so every anchor here reads the handler's source rather
than a response — the same shape as
`test_roadmap_query_section_index.cpp`, which is a scrape for the same
reason. Label: `features;fast`.

**What that costs, stated because a scrape and a behavioural test read
alike in a green run:** no anchor in this suite observes an emitted key
set, a 304, a page slice or a `duplicate_ids` payload. Each invariant
above is pinned to the source construct that produces the behaviour, and
the behaviour itself is unobserved here. Linking `cmdRoadmapQuery` into
the bundle would change that, and is not this spec's deliverable.

Per the project test convention, each invariant's test is first
verified to FAIL against pre-fix code (i.e. before the third mode
value is wired up), then GREEN after the fix lands.

## 5. RAM / build cost

Zero new state. The projection is per-bullet at emit time
(`QJsonObject` construction of four scalars), strictly cheaper than
the current bullets-mode emit. Estimated diff size:

- ~40 LoC — three branches in `cmdRoadmapQuery` (the list emit loop,
  the `id` branch, the `ids` branch, per §2.2), calling one shared
  projection helper that lives with the other `rc*` projections in
  `src/remotecontrol.cpp`.
- ~60 LoC tool-descriptor schema update — enum + per-arg
  `modeProp["description"]` clause + top-level
  `roadmapTool["description"]` selection-hint addendum (three
  touchpoints, see §7).
- ~180 LoC test — `QTemporaryDir` fixture + the INV anchors named
  in §3.

Total: **~280 LoC** including tests.

## 6. Migration

None. Additive arg value with no default behaviour change. Existing
`roadmap_query{}` and `roadmap_query{mode:"bullets"}` calls continue
to emit byte-identical envelopes (except the `mode` echo, which is
present only when the caller passed it — pre-existing convention).

## 7. Tool-descriptor update

Three touchpoints in `src/claudeintegration.cpp`'s `roadmap_query`
descriptor block — all must be updated in the same change:

1. **`modeProp["enum"]`** — the per-arg enum gains the literal
   `"headline_only"` alongside `"bullets"` and `"section_index"`.
2. **`modeProp["description"]`** — the per-arg description string
   gains one technical clause documenting the new value (caller is
   the JSON schema reader / tool-validator).
3. **`roadmapTool["description"]`** — the top-level tool description
   currently runs ~70 lines of `QStringLiteral` enumerating the
   modes ("Optional `mode` — \"bullets\" (default) /
   \"section_index\" …"). Add a *short* mode-selection hint here,
   not a full payload-shape paragraph — the top-level description is
   the caller's "should I pick this tool" surface; the per-arg
   description owns the payload contract.

Suggested per-arg description clause (technical):

> `mode:"headline_only"` — return `{id, status, headline_oneline,
> section_slug}` per bullet, skip `body`/`lanes`/`kind` (~10× smaller
> payload on dense bundle sections; composes with `section=`,
> `status=`, pagination, and ETag).

Suggested top-level description addendum (selection hint):

> / \"headline_only\" (ID + one-line headline + slug per bullet — use
> when you only need the bullet catalogue, not the bodies; composes
> with `section=`).

## 8. Cross-doc impact

- `CLAUDE.md` — no change required (the tool-authoring contract list
  is unchanged; only an enum value is added).
- `docs/standards/mcp-tools.md` — no change required (the umbrella
  checklist references the existing enum-validation pattern).
- `tests/features/README.md` — no change (one new feature dir,
  follows the existing pattern).

## 9. Cold-eyes loop log

- **Loop 1 (2026-05-26).** Reviewer flagged 3 HIGH (heading drift from
  specs.md, missing `*Test:*` clauses, INV-2 rollup-key ambiguity),
  5 MEDIUM (link convention `[[…]]` undocumented, etag wiring lacked
  source cite, line-range pin drift risk, INV-5 vacuous, test-anchor
  count mismatch), 5 LOW (~10× claim provenance, `Bullet::status`
  typing, rollup gate, `PaginationEngine` naming, Status-line format).
  All verified against `docs/standards/specs.md`, `src/remotecontrol.cpp`,
  `src/claudeintegration.cpp`, `docs/specs/ANTS-1437.md`. Fixes:
  renumber sections per specs.md §3; rewrite INV-2 to govern key-set
  with empty-value permission; add `*Test:*` clauses to every INV;
  convert INV-5 from vacuous to "combinator coverage" positive claim;
  switch link convention from `[[…]]` to plain `ANTS-NNNN`; add
  `claudeintegration.cpp:7371` cite to §2.5; replace `line 1629–1643`
  pin with function-name pin; add `PaginationEngine` name to §2.3;
  Status line normalized to `spec draft (2026-05-26)`.
- **Loop 2 (2026-05-26).** Reviewer flagged 1 HIGH (`fields=` row
  lacked `isFieldProjectionTool` cite — corpus consistency rule),
  3 MEDIUM (no INV locking the diagnostic carve-out asserted in §2.4;
  line-number pin reintroduced at `claudeintegration.cpp:7371` despite
  loop-1 fix; INV-1 present-tense audit vs post-fix framing). All
  verified against `src/mcpprojection.cpp` (`isFieldProjectionTool`
  exists), `src/claudeintegration.cpp` (line drift risk confirmed),
  spec body. Fixes: add `mcp::isFieldProjectionTool` /
  `makeFieldsProp` cite to the `fields=` row; replace
  `claudeintegration.cpp:7371` with function-name pin only; reframe
  INV-1 with "After this change, …"; add INV-8 locking the
  `legacy_format_sections` / `legacy_format:true` / `duplicate_ids`
  diagnostic omission with a fixture-based test surface; update §4
  fixture description + anchor count (8).
- **Loop 3 (2026-05-26).** Reviewer flagged 1 HIGH
  (INV-8 over-carve: `duplicate_ids[]` is a roadmap-integrity signal
  that benefits headline_only callers and is payload-cheap; the
  "wire-cost optimisation" justification didn't cover it),
  2 MEDIUM (INV-7 source-grep too loose — would pass on the
  impl-side allow-set match alone; §2.5 elided the
  `isEtagSupportedTool` precondition that fires `applyEtagPattern`),
  2 LOW (Status line lacked the cold-eyes-loops cumulative marker
  per specs.md §5.7; missing **Layman:** line per specs.md §5.2).
  All verified against `src/remotecontrol.cpp` (`duplicate_ids[]`
  emitted on every bullets-mode branch — 2064/2265/2416/2526),
  `src/claudeintegration.cpp:7332` (`roadmap_query` in
  `isEtagSupportedTool`), `docs/standards/specs.md` §5.2 + §5.7.
  Fixes: split INV-8 — keep `duplicate_ids[]`, carve out only
  `legacy_format_sections[]` + per-section `legacy_format:true`;
  anchor INV-7's grep to `src/claudeintegration.cpp` + the `mode`
  enum context (impl-side allow-set match alone fails closed);
  add half-sentence to §2.5 naming `isEtagSupportedTool` as the
  precondition; update Status line to record the three folded loops;
  add **Layman:** line under the header block; also update §2.4
  prose to match the new split-carve-out story.
- **Loop 4 (2026-05-26).** Reviewer flagged 0 HIGH, 2 MEDIUM
  (INV-7 grep recipe still missing the *positive* anchor — the
  `roadmapTool["name"] = "roadmap_query"` bracket and the
  `modeEnum.append(…)` context line; §7 named only one descriptor
  touchpoint when there are actually two — per-arg
  `modeProp["description"]` AND top-level
  `roadmapTool["description"]` mode roster), 4 LOW/INFO (function-
  level anchor cite for `applyEtagPattern` ordering; ~50 LoC
  schema-update budget under-counts; ROADMAP-only cross-refs OK;
  bullet-level `fields=` projection not explicitly stated as
  unsupported). All verified against
  `src/claudeintegration.cpp:1805-2008` (both descriptor blocks
  exist; the top-level enumerates modes at lines 1834-1842, the
  per-arg one defines `modeEnum` separately). Fixes: tighten INV-7
  grep recipe with `roadmapTool["name"] = "roadmap_query"`
  positive bracket and a `modeEnum.append(…)` context anchor; split
  §7 into three numbered touchpoints (enum + per-arg description +
  top-level description roster); extend INV-2 with explicit
  `fields=` exclusion plus a second `*Test:*` anchor
  (`Inv2FieldsDoesNotNarrowBullets`); update Status line + cold-eyes
  log.
- **Loop 5 (2026-05-26).** Reviewer flagged 1 CRITICAL
  (INV-8 / §2.4 misattribute `legacy_format_sections[]` +
  `legacy_format:true` to bullets-mode — they are emitted only
  inside the `mode == "section_index"` branch of `cmdRoadmapQuery`
  at `src/remotecontrol.cpp:2019/2050`), 1 HIGH (§2.2/INV-2
  contradiction on narrator entries: §2.3 says narrators have
  non-empty `headline_oneline` but INV-2's "empty-string values
  permitted (rollup / narrator)" implies both empty), 3 MEDIUM
  (INV-3 should state positional vs set-based equality; INV-7
  reintroduced `(currently :1806)` line-number hedge despite
  loop-1's ban; §7's top-level-descriptor extension may produce a
  wall of text — should be a short selection hint, not full payload
  shape), 6 LOW/INFO (clarification of bullet-level `fields=`
  exclusion rationale, test-anchor naming consistency, etc.). All
  verified against `src/remotecontrol.cpp:1858-2066` (legacy_format
  emitters live inside section_index branch only). Fixes: rewrite
  INV-8 to a positive top-level-diagnostic-parity claim
  (`duplicate_ids[]` byte-identical between modes; legacy_format
  diagnostics naturally absent because they're section_index-only);
  fix INV-2 to distinguish rollup-empty vs narrator-non-empty
  `headline_oneline`, add separate test anchors for each; add
  "positional equality" to INV-3; drop `(currently :1806)` from
  INV-7; rewrite §7 prose with explicit "Three touchpoints" framing
  and separate technical vs selection-hint wording for the per-arg
  vs top-level descriptions; add `fields=` exclusion rationale to
  INV-2 body.
- **Loop 6 (2026-05-26).** Reviewer flagged 0 CRITICAL, 3 HIGH
  (INV-5 / §2.3 silently miss the `id=` selector emission path at
  `src/remotecontrol.cpp:2375-2419` — that branch builds its own
  envelope from a scanned `matches[]` array without going through
  the main bullets-mode emit loop, so projection must be applied
  there too; Cold-eyes log lists Loop 5 before Loop 4 — out of
  chronological order; loop-5 log entry references a "Two
  touchpoints" §7 state no longer in the doc), 4 MEDIUM (INV-3
  positional equality should ground in shared cache iteration
  order; INV-8 wording obscures the shared-lineage rationale;
  INV-4 single test anchor name is ambiguous between sub-claims (a)
  and (b); §5 LoC budget under-counts the descriptor work),
  4 LOW/INFO (test-anchor name mixing; quote-escape risk in §7
  descriptor text; etc.). All verified against
  `src/remotecontrol.cpp:2375-2419` (id-branch returns before the
  main emit loop), spec body. Fixes: add the id-branch as a second
  projection surface in §2.2 + add explicit row coverage in §2.3 +
  add `Inv5IdSelectorProjected` test anchor; reorder Cold-eyes log
  to chronological (loop 4 → loop 5 → loop 6); rewrite loop-5
  entry to match the spec's actual state; ground INV-3 positional
  equality in shared cache; reword INV-8 to "diagnostic parity by
  shared lineage" with explicit section_index-only carve-out;
  split INV-4 test anchors into `Inv4SameMode*` + `Inv4CrossMode*`;
  bump §5 LoC estimate from ~230 to ~280 with per-touchpoint
  breakdown.
- **Loop 7 (2026-05-26).** Reviewer flagged 0 CRITICAL, 1 HIGH
  (`mode` echo behaviour ambiguous: section_index emits
  unconditionally at `src/remotecontrol.cpp:2026`, bullets-mode id
  branch emits conditionally on `hasModeArg` at `:2409`; spec didn't
  say which precedent headline_only follows), 3 MEDIUM (INV-2
  narrator non-empty claim not grounded in a named predicate;
  §2.4 phrasing "projects bullets" misreads as "is a bullets
  emitter"; INV-7 grep recipe vulnerable to false-pass against the
  description-block literal `"headline_only"` once §7 lands),
  4 LOW/INFO (LoC budget consistent now; status emoji typing fine;
  isFieldProjectionTool cite sufficient; test-anchor `Inv4a*` /
  `Inv4b*` lowercase suffix inconsistent with full-word suffix
  style). All verified against `src/remotecontrol.cpp` (1721
  `isRollupBullet`, 1730 `isNarratorBullet`, 2026 + 2409 mode-echo
  precedents), `src/claudeintegration.cpp:1810+` (descriptor block
  contains "headline_only" literal post-fix). Fixes: state in §2.4
  that headline_only follows the bullets-mode `hasModeArg` echo
  gate (rationale: bullets-projection variant → bullets-mode
  default-call byte-identity); ground INV-2 narrator clause in
  `isNarratorBullet` predicate cite; reword §2.4 "projects bullets"
  → "runs through the bullets-emission code paths" to avoid
  misread; tighten INV-7 to require literal inside a
  `modeEnum.append(...)` call (not just a context line near one);
  rename test anchors `Inv4a*` → `Inv4SameMode*`, `Inv4b*` →
  `Inv4CrossMode*` for style consistency.
- **Loop 8 (2026-05-26).** Reviewer flagged 0 CRITICAL, 2 HIGH
  (INV-8 cites a line range `:1858-2058` despite loop-1's ban on
  line-number pins — should be function-name pin
  (`cmdRoadmapQuery`); INV-2 narrator clause cites
  `isNarratorBullet` predicate then says "narrator's defining
  condition is …, so `rcHeadlineOneline(b.headline)` is non-empty"
  — the predicate-cite attaches to a derived fact, should attach
  to the field it actually tests then derive the rest),
  3 MEDIUM (INV-7 test recipe should ship the actual `grep -nP`
  pattern not just "source-grep"; §2.3 id-row envelope list omits
  `mode` echo — should include it conditional on `hasModeArg`;
  source-column names in §2.2 mix parseBullets struct fields with
  JSON keys), 5 LOW/INFO (Cold-eyes log Loop 7 before Loop 6 —
  same ordering regression loop-6 already fixed once; descriptor
  prose still escapes quotes that won't be in the live string;
  etc.). All verified against `src/remotecontrol.cpp:1730`
  (`isNarratorBullet` tests `headline` non-emptiness, not
  `headline_oneline`), spec body. Fixes: replace `:1858-2058` with
  function-name pin (`cmdRoadmapQuery`); reword INV-2 narrator
  clause so predicate cite attaches to `headline` (the field it
  tests), then derive `headline_oneline` non-emptiness; tighten
  INV-7 recipe with the actual `grep -nP` command; add `mode`
  (conditional) to §2.3 id-row envelope list; reorder Cold-eyes
  log so Loop 6 precedes Loop 7.
- **Loop 9 (2026-08-26).** 3 lanes, cold, genre pinned `spec`; new run,
  trigger ANTS-4703 part 2. Q1 5 · Q2 2 · Q3 1 · Q4 2 — **ten verified,
  ten fixed, none dismissed.** Six lane open questions resolved clean.
  **All three lanes independently found the same three Q1s**, the
  strongest agreement this document has produced. The largest is that
  `cmdRoadmapQuery` was moved to `src/remotecontrol_roadmap_query.cpp`
  and every body citation still named `src/remotecontrol.cpp` — INV-7's
  exclusion rule among them, so its discriminator named a file that no
  longer holds the allow-set. Line-range pins were dropped rather than
  re-derived; loop 3 made the same call. Second, §2.3's `fields=` row
  cited `mcp::isFieldProjectionTool`, which ANTS-4524 **deleted** —
  there is no allowlist and every verb honours `fields=`. Third, INV-5's
  enumeration was stale in both directions: it named `fields` and
  `etag_match`, which the function does not read, and omitted a dozen it
  does. **The most dangerous was found by one lane only** — INV-1 and
  §2.1 asserted a three-value allow-set against the live `kModes`, and
  §2.1 carried it as a buildable `!=` chain, so an implementer
  re-applying the snippet would have rejected `bundles` and `report`.
  **INV-5 was re-derived rather than widened**: it now claims every
  argument the verb reads is either given behaviour in §2.3 or named
  there as having no effect, which is true, carries no enumeration to go
  stale, and is the first form of it a test could enforce. §2.3 gained
  rows for `ids`, the `query` family and `max_body_bytes`, and one row
  naming the other-mode arguments that are read and ignored — the silent
  default the old INV-5 denied existed. **Two test clauses were made
  honest rather than repaired**: `Inv5CombinatorCoverageSourceGrep`
  asserts only that the projection helper is named, and
  `Inv2FieldsDoesNotNarrowBullets` only that the mode value appears in
  the sources; neither can falsify its clause, and the code-side repair
  is out of a docs gate's scope. **Two lane findings were corrected
  before landing.** "Four emission surfaces" is three selector shapes —
  the list path projects at two sites, plus the `id` and `ids` branches
  — and `ids` is the one the document never named. And INV-2's `fields=`
  clause is *true*; the defect was its test, not its premise. **Packet
  defect, disclosed:** the orchestrator's source window was cut
  mid-sentence, so §2.2, §2.4, §2.6, INV-6 and INV-7 went unreviewed on
  their behavioural half this loop. **Collateral filed, not fixed:**
  sibling specs still cite the pre-split file and the deleted predicate;
  each has its own gate.
- **Loop 10 (2026-08-26).** 3 lanes, cold, packet rebuilt whole from disk
  with loop 9's truncated window repaired and windows added for the mode
  allow-set, the bullet predicates, the `id`/`ids` branches, the shared
  projection helpers and the `tools/list` descriptor. Q1 1 · Q2 4 · Q3 0
  · Q4 1 — **six verified, six fixed, none dismissed.** Three lane open
  questions resolved clean. **Cap reached (2 for a spec); the run files
  its tail and exits.**
  **Loop 9's INV-5 rewrite HELD** — all three lanes independently walked
  the verb's whole read-set against §2.3 and confirmed the claim, which
  is the check the old enumeration could never pass.
  **The run's most valuable output is a CODE defect, filed as ANTS-4712,
  not fixed here.** In the `ids` branch the projection runs first and
  ANTS-4400's `input_index` loop then writes a fifth key onto each
  bullet, so `mode:"headline_only" + ids:[…]` breaks INV-2's four-key
  contract. Nothing caught it because INV-5's only behavioural anchor
  exercises the singular `id`, and the source-grep anchor is satisfied by
  the helper's own definition — so it vouches for nothing behavioural.
  Both facts are now stated rather than implied.
  **Two findings landed on text loop 9 wrote**, which is the expected
  shape: the `ids` bullet asserted `input_index` while the four-key rule
  three lines below denied it, and the grouped ignore-row swept in
  `section_etag_match`, which is read on the `section=` path this mode
  uses and is honoured. Two more landed where loop 9's edits made
  pre-existing text contradictory (§5's touchpoint list, INV-8's path
  enumeration). Two were untouched by this run: INV-7 restated the
  descriptor enum's membership as three values where it ships five, and
  INV-3's parity claim cannot hold wherever INV-6's byte-measured
  truncation does — a parity test over emitted `bullets[]` would red
  against correct code, so INV-3 is now scoped to the pre-pagination set.
  **Cap character: calm rather than oscillating.** Roughly half the loop
  landed on this run's own text, but in *kind* — a row's reason, an
  enumeration — with none of loop 9's decisions reversed, and the two
  pre-existing findings were real. At 579 lines the document is well
  inside the size range where a split would be indicated, so the cap is
  the normal spec exit and not evidence against the document.
  **Deferred tail:** ANTS-4712 (the `ids` five-key defect and the two
  missing test anchors) and ANTS-4711 (sibling specs citing the pre-split
  file and the deleted `isFieldProjectionTool`). Neither is a docs fix to
  this document.

- **Loop 11 (2026-08-27).** 3 lanes, cold, genre pinned `spec`; packet
  rebuilt whole from disk with windows on the three emission surfaces, the
  shared projection helper and the `mode` schema text. Q1 3 · Q2 2 · Q3 0
  · Q4 1 — **six verified, six fixed, none dismissed.** Trigger: ANTS-4712
  widened INV-2 to name the `ids` branch's `input_index` exemption, which
  loop 10 had filed as its deferred tail. **All three lanes independently
  found the same four defects** — the strongest agreement this document's
  log records. **Three were this loop's own collateral, and all three are
  the same failure**: INV-2 was widened and the passages it contradicts
  were left standing — §2.2 item 3 still called the exemption an open
  question, §2.3's `ids` row still asserted parity with the `id` branch,
  and INV-5 still said no anchor exercises the `ids` branch after INV-2
  named one. Delete-first would have caught all three. **The most
  consequential was PRE-EXISTING and older than this run**: §4 prescribed
  a harness that dispatches `RemoteControl::cmdRoadmapQuery` in-process,
  which cannot link — the feature bundles do not link the verb, and both
  named suites are source scrapes. An implementer building §4 as written
  gets an unlinkable bundle. Its consequence is now stated rather than
  implied: no anchor in that suite observes a key set, a 304, a page slice
  or a `duplicate_ids` payload, and §4 says so, because a scrape and a
  behavioural test read alike in a green run. **The Q4 came from the same
  root** — INV-4, INV-5 and INV-8 each prescribed an assertion requiring a
  live response; each is restated as the scrape actually shipped. **The
  sixth was the orchestrator's, not a lane's**, and invisible to them
  because the packet carried no window on the test file: INV-4 named
  `Inv4SameModeEtagSameModeShortCircuit` and
  `Inv4CrossModeEtagCrossModeNoShortCircuit`, and neither exists — the
  suite spells them `Inv4SameModeShortCircuit` and
  `Inv4CrossModeNoShortCircuit`, as the feature spec had all along.
  Verified against `ctest -N`. **A fix's own added claim was caught by 4a
  step 3**: it called the INV-4 anchors scrapes over the handler, and they
  scrape the dispatcher, where the etag is applied. Collateral, out of
  scope and corrected there:
  `tests/features/roadmap_query_headline_only/spec.md`'s INV-5 row counted
  two emission surfaces where §2.2 names three. Three lane open questions
  resolved clean and are therefore not in the tally — `applyEtagPattern`
  does wrap after the handler returns (`src/claudeintegration.cpp`), §7's
  enum framing restates nothing, and INV-5's read-set claim was correctly
  left unfiled for want of a window.
