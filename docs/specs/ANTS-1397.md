# ANTS-1397 — `test_audit_*` MCP trio (skill-displacement migration)

**Status:** implemented (the verb family ships; a fifth verb,
`test_audit_recheck`, was added later under ANTS-1722 and is not covered
below). Cold-eyes loops 1 + 2 + 3 folded (2026-05-17); reconciled against
shipped code 2026-07-25 (ANTS-3612 pool correction + the INV-5
contract correction — see § 10), and again 2026-07-25 for the § 2
envelope shapes. Two things remain genuinely open rather than draft-era
prose: INV-12's never-wired in-flight gate (§ 9 Q3) and the § 7 skill
fast-path. Elsewhere, treat code as authoritative — § 2.6 in particular
is a design sketch, not a field list.
**Kind:** implement
**Source:** ROADMAP.md ANTS-1397 (append-only log — search the id, line numbers drift).
**Blocked by:** none.
**Pairs with:** ANTS-1351 (template — shared conventions for
envelope cap, contract, lock pattern, worker pool, namespace).
ANTS-1352 (`indie_review_dispatch`) is the third in the series.
**Distinct from:** ANTS-1351 (single blocking call); ANTS-1280
(end-to-end orchestrator that runs subagents server-side).

## 1. Problem

`/test-audit` skill is ~15 KiB SKILL.md + ~23 KiB references
(dimensions.md / framework-detection.md / grep-patterns.md) — ~38 KiB as
measured 2026-07-25; the draft's "9.5 + 22 = 32 KiB" predates several
skill revisions. ≈ **~8 K tokens first-session displaced** (corrected from
v3's overstated 14 K — loop 3 token H2). Subsequent sessions in the
same Claude session pay zero skill load.

Net per-sweep wins (after envelope+wrap costs):

| Source | Win |
|---|---:|
| First-session skill+refs displacement | ~8 K (one-time) |
| Per-chunk subagent brief shrink (3 KiB → 1 KiB × 17 chunks) | ~20 KiB ≈ 5 K tokens |
| Synth disk-input (~50 KiB report bodies stay on disk) | ~12 K tokens |
| Subtract: synth prompt + envelopes carry ~5 KiB to parent | -1.2 K |

**Per repeat sweep: ~16 K tokens; first sweep: ~24 K.** Loop 3
token-savings lane confirmed v3's 14 K claim was inflated on skill
load but DEFLATED on synth + per-chunk wins; honest restatement
above.

Per-chunk subagent dispatch cost (~68 K for a 17-chunk sweep) is
inherent and unchanged.

## 2. Surface

### 2.1 The verb family (matches cold-eyes / indie-review / debt-sweep)

| Verb | Phase | Return |
|---|---|---|
| `test_audit_partition` | Probe framework + enumerate test files; pack into chunks. | `{ok, framework, test_globs[], chunks[], total_files, chunks_count, dimensions_active[], pre_pass_findings_by_chunk{}?, pre_pass_cached, pre_pass_chunk_ids[]?, pre_pass_omitted?, pre_pass_omitted_bytes?, additional_frameworks[], partition_token, byte_count, offset, limit, total, truncated, next_offset?}` |
| `test_audit_brief` (lane=chunk_id) | Structured per-chunk manifest. | `{ok, chunk_id, source_paths[], dimensions[], framework_context{}, pre_pass_findings[], prior_false_positives[], byte_count}` |
| `test_audit_synthesis_prompt` | Render cross-chunk dedup + triage prompt. | `{ok, prompt, mode, dimension_summaries{}, top_dimensions[], file_index{}, severity_histograms{}, reports_read, chunks_total, chunks_returned, truncated, truncated_by_limit, next_offset?, byte_count}` |
| `test_audit_fold_in` | Render actionable findings via `RoadmapFoldIn::*` direct calls. | `{ok, block, allocated_ids[], written, written_count, failed_count, partial, release_block_heading, bytes_written, dry_run?, counter_path?}` |

### 2.2 `test_audit_partition` request/response

**Request:**
```json
{
  "caller_cwd": "/abs/path/to/project",
  "scope":      "auto | path:<subdir> | files:<csv>",
  "dimensions": "auto | csv:<d1,d2,...>",
  "chunk_size": 12,
  "quick":      false,
  "offset":     0,
  "limit":      -1
}
```

**Response:**
```json
{
  "ok": true,
  "framework": "pytest",
  "test_globs": ["tests/**/*.py", "test_*.py"],
  "total_files": 192,
  "chunks_count": 16,
  "chunks": [
    { "id": "c-001", "paths": ["tests/api/test_a.py", "tests/api/test_b.py"],
      "pre_pass_dimensions": ["flakiness", "duplication"] }
  ],
  "dimensions_active": ["performance", "flakiness", "duplication", "security"],
  "dimensions_skipped": ["coverage_gaps"],
  "skip_reason_per_dimension": { "coverage_gaps": "restricted by config dimensions.deny" },
  "additional_frameworks": ["jest"],
  "pre_pass_findings_by_chunk": {
    "c-001": [ { "file": "tests/api/test_a.py", "line": 47,
                 "pattern_id": "sleep_call", "dimension": "flakiness" } ]
  },
  "pre_pass_cached": false,
  "partition_token": "<qHash 4-byte hex>",
  "offset": 0, "limit": 16, "total": 16, "truncated": false,
  "byte_count": 4128
}
```

`pre_pass_findings_by_chunk` entries carry `{file, line, pattern_id,
dimension}` ONLY — never matched text (INV-13). It is omitted, with
`pre_pass_cached: true` set, in **two** cases (the second was added by
ANTS-2070 after this section was written):

1. **Paging** — on pages 2+ of a paged partition (e.g. `offset:10,
   limit:10` on a 30-chunk project), since the orchestrator already
   retrieved them on page 1 and can look up locally (loop 3 token M2).
   Saves ~13 KiB on multi-page partitions.
2. **Envelope size** — even on page 1, when including the map would blow
   the envelope budget. A caller must therefore treat `pre_pass_cached`
   as "fetch these another way", not as "you are past page 1".

When omitted, `pre_pass_chunk_ids[]` lists the chunk ids that *have*
pre-pass findings, so a caller can request briefs selectively.

**Not yet implemented:** `dimensions_skipped` and
`skip_reason_per_dimension` exist as `TestAuditEngine` struct members but
nothing populates them and the partition envelope builder never writes
either key. They are shown above as the intended shape; do not code
against them yet. `additional_frameworks[]` (ANTS-1623) *is* emitted.

Math: `chunks_count = ceil(total_files / clamped_chunk_size)`.
Example: 192/12 = 16 (corrected from v3's 187/12 → 17 inconsistency).

### 2.3 `test_audit_brief` request/response

**Request:**
```json
{
  "caller_cwd": "/abs/path/to/project",
  "chunk_id":   "c-001",
  "partition_token": "<echo from partition>"
}
```

**Response:**
```json
{
  "ok": true,
  "chunk_id": "c-001",
  "source_paths": ["tests/api/test_a.py", "tests/api/test_b.py"],
  "dimensions": ["flakiness", "duplication", "performance", "security"],
  "framework_context": {
    "framework": "pytest",
    "conftest_exports": ["user_fixture", "tmp_db_fixture"],
    "markers": ["slow", "integration"],
    "addopts": "--strict-markers"
  },
  "pre_pass_findings": [
    { "file": "tests/api/test_a.py", "line": 47,
      "pattern_id": "sleep_call", "dimension": "flakiness" }
  ],
  "prior_false_positives": [
    { "claim": "sleep in test_a is a deliberate timing fixture",
      "rationale": "…", "topic": "flakiness" }
  ],
  "byte_count": 1640
}
```

**No `brief` string field** (loop 3 opt H4 + user decision 2026-05-17).
Caller composes the subagent prompt from structured fields; the skill
markdown ships a 6-line "compose your brief like this" snippet
showing the canonical template. Saves ~3.4 KiB per sweep + a
string-template path on the hot path. Departs from ANTS-1281's
backwards-compat carry of the `brief` field (this is greenfield;
no legacy callers).

ANTS-1281 manifest-only rule preserved: `source_paths` lists
paths, never bodies.

### 2.4 `test_audit_synthesis_prompt` request/response

**Request:**
```json
{
  "caller_cwd": "/abs/path/to/project",
  "partition_token": "<echo>",
  "reports_dir": "<project-relative dir of per-chunk reports>",
  "calibration_anchor": { "raw": 412, "actionable": 38 },
  "allow_outside_project": false,
  "mode":   "summary | full | hybrid",
  "offset": 0,
  "limit":  -1
}
```

**Response:**
```json
{
  "ok": true,
  "prompt": "<single string the orchestrator hands to audit-triage>",
  "mode": "summary",
  "dimension_summaries": {
    "flakiness":   { "count": 22, "files": 14 },
    "duplication": { "count":  8, "files":  6, "cross_chunk": true }
  },
  "top_dimensions": ["flakiness", "duplication"],
  "file_index": { "tests/api/test_a.py": ["c-001"] },
  "severity_histograms": { "flakiness": { "HIGH": 4, "MEDIUM": 18 } },
  "reports_read": 17,
  "chunks_total": 17,
  "chunks_returned": 17,
  "truncated": false,
  "truncated_by_limit": false,
  "next_offset": -1,
  "byte_count": 3210
}
```

`mode` (ANTS-1455) selects `summary` / `full` / `hybrid`; an unknown
value refuses `bad_mode`. `top_dimensions`, `file_index` and
`severity_histograms` are ANTS-1488. `offset`/`limit` page the chunk
set — `chunks_total` / `chunks_returned` / `truncated` /
`truncated_by_limit` / `next_offset` describe that paging; `next_offset`
is emitted unconditionally, using `-1` as the no-more-pages sentinel.
`allow_outside_project` relaxes the `reports_dir` root anchor (INV-14),
and is what `reports_dir_outside_root` refuses without.

`reports_dir` is the ONE path this family routes through
`PathValidation::validatePath` (INV-14 — `scope:"path:"` and `files:`
are not anchored; ANTS-3627).
**Per-report content is fenced via `<chunk_report file="…">…</chunk_report>`
in the rendered prompt** (loop 3 security H-A) — defends against
prompt injection from a hostile per-chunk report (e.g. malicious
dep dropping a markdown file in `reports_dir`). Nested fence
markers escaped. Synth prompt MUST quote, never narrate, third-
party content.

### 2.5 `test_audit_fold_in` request/response

**Request:**
```json
{
  "caller_cwd": "/abs/path/to/project",
  "actionable": [
    { "dimension": "flakiness", "severity": "HIGH",
      "file": "tests/api/test_a.py", "line": 47,
      "summary": "time.sleep(2) in 14 tests",
      "fix":     "poll_until() helper" }
  ],
  "framework":     "pytest",
  "files_scanned": 192,
  "dimensions":    ["flakiness", "duplication"],
  "raw_findings":  412,
  "narrative_mode": false,
  "narrative_md":   "<prose block, required when narrative_mode>",
  "id_prefix":      "ANTS",
  "dry_run":        false
}
```

**Response:**
```json
{
  "ok": true,
  "block": "<the markdown block written, for caller display>",
  "allocated_ids": ["ANTS-1500", "ANTS-1501", "ANTS-1502"],
  "written": "/abs/path/to/project/ROADMAP.md",
  "release_block_heading": "### 🧪 Test Audit 2026-05-17",
  "bytes_written": 3814,
  "written_count": 3,
  "failed_count": 0,
  "partial": false
}
```

`test_audit_fold_in` calls **`RoadmapFoldIn::allocateIds(N)` ONCE
upfront for the full batch, then assembles ONE multi-finding
markdown block and calls `RoadmapFoldIn::insertBlock` ONCE** (INV-3).
Single read+write of ROADMAP.md (763 KiB at 2026-05-17 — loop 3
perf H1), not N. Eliminates the per-finding re-read and the
counter-race window (loop 3 perf H1 + security M-F).

If the single insertBlock fails, `code:"write_failed"`,
`allocated_ids` returned (the counter advanced; the orchestrator
sees IDs that won't appear in the roadmap until the next call —
acceptable side effect, alternative is rolling back the counter
which races against any concurrent `roadmap_log` caller).

### 2.6 Engine API

New `testauditengine.{h,cpp}` in `ants_core_lib`. No `AuditEngine`
dependency.

Namespace **`TestAuditEngine`** (PascalCase).

```cpp
namespace TestAuditEngine {
struct PartitionRequest {
    QString callerCwd;
    QString scope;
    QString dimensions;
    int chunkSize = 12;
    bool quick = false;
    int offset = 0;
    int limit = -1;
};
struct Chunk { QString id; QStringList paths; QStringList prePassDimensions; };
struct PartitionResult {
    bool ok = true; QString error; QString code;
    QString framework;
    QStringList testGlobs;
    QVector<Chunk> chunks;
    int totalFiles = 0;
    int chunksCount = 0;
    QStringList dimensionsActive;
    QStringList dimensionsSkipped;
    QHash<QString, QString> skipReasonPerDimension;
    QHash<QString, QJsonArray> prePassFindingsByChunk;
    bool prePassCached = false;
    QString partitionToken;          // INV-4 (qHash 4-byte)
    int offset = 0, limit = -1, total = 0;
    bool truncated = false;
    int byteCount = 0;
    QDateTime mtimeWalkComputedAt;   // INV-15
};
struct BriefRequest { QString callerCwd; QString chunkId; QString partitionToken; };
struct BriefResult {
    bool ok = true; QString error; QString code;
    QString chunkId;
    QStringList sourcePaths;
    QStringList dimensions;
    QJsonObject frameworkContext;
    QJsonArray prePassFindings;
    int byteCount = 0;
};
struct SynthRequest {
    QString callerCwd;
    QString partitionToken;
    QString reportsDir;
    QJsonObject calibrationAnchor;
};
struct SynthResult {
    bool ok = true; QString error; QString code;
    QString prompt;
    QJsonObject dimensionSummaries;
    int reportsRead = 0;
    int byteCount = 0;
};
struct FoldInRequest {
    QString callerCwd;
    QJsonArray actionable;
    QString framework;
    int filesScanned = 0;
    QStringList dimensions;
    int rawFindings = 0;
};
struct FoldInResult {
    bool ok = true; QString error; QString code;
    QString block;
    QStringList allocatedIds;
    QString written;
    QString releaseBlockHeading;
    int bytesWritten = 0;
    int writtenCount = 0;
    int failedCount = 0;
    bool partial = false;
};
PartitionResult partition(const PartitionRequest &req);
BriefResult     brief(const BriefRequest &req);
SynthResult     synthesize(const SynthRequest &req);
FoldInResult    foldIn(const FoldInRequest &req);
}  // namespace TestAuditEngine
```

> **Not exhaustive (corrected 2026-07-25).** The block above is the v1
> design shape; `src/testauditengine.h` is authoritative and has since
> grown fields this listing omits — `PartitionResult::additionalFrameworks`
> and `::nextOffset`; `BriefResult::priorFalsePositives`;
> `SynthRequest::{allowOutsideProject, mode, offset, limit}`;
> nine further `SynthResult` fields (see § 2.4);
> `FoldInRequest::{narrativeMode, narrativeMd, idPrefix, dryRun}`;
> `FoldInResult::counterPath`. Read the header rather than this block
> when implementing against the engine.

### 2.7 Worker thread placement

> **Correction (ANTS-3612, 2026-07-25).** The `m_auditPool` thread pool
> this section shares with ANTS-1351 was never built — it was always
> aspirational v2. ANTS-1351 INV-9 now describes what shipped: an ad-hoc
> `QThread::create()` per call plus an engine-side aggregate concurrency
> cap (`AuditRunner::kMaxConcurrentRuns = 2`) that refuses `server_busy`.
> The `server_busy` code below is therefore real, but it is emitted by
> `AuditRunner::runAudit`'s counting gate, not by a pool; and it bounds
> `audit_run` only — `test_audit_partition` has no such gate, so the
> "`/audit && /test-audit` co-run, saturate beyond 2" model below does not
> hold as written. Wiring the partition sweep to the same cap (or stating
> that it needs none) is open work on this spec, which remains a draft.

Partition's pre-pass regex sweep over ~2 MiB is a bounded in-process
walk (no child processes, unlike `audit_run`'s tool fan-out). Brief + synth + fold_in are fast (<200 ms) — run
on the dispatcher thread.

## 3. Invariants

- **INV-1 / partition is pure data, no shell.** File-system walk +
  pre-compiled `QRegularExpression` multi-pattern scan. Patterns are
  **compiled into the engine** as `QStringLiteral` regexes — nothing is
  read at runtime, which is what makes the engine self-contained (loop 3
  opt H2 — was an external skill-markdown read). The project-internal
  `docs/standards/test-audit-grep-patterns.json` is a *mirrored* source
  of truth, not a load path: a drift-guard test
  (`tests/features/test_audit_pattern_drift/`) asserts the two agree.
  Skill markdown documents the patterns inline as human reference. **Alternation construction puts longest-
  alternative-first AND factors common prefixes** (loop 3 perf M4
  — CLAUDE.md PCRE2 rule). Per-language-family combined regexes
  built once at startup.
- **INV-2 / brief is path-only, NEVER file bodies.** ANTS-1281's
  rule. `source_paths` lists paths; subagent reads them.
  `byte_count` ≤ 8 KiB per chunk.
- **INV-3 / fold-in delegates to `RoadmapFoldIn::*` (engine
  level) via SINGLE batched call.** `test_audit_fold_in` MUST
  NOT re-implement ID alloc, atomic write, or counter increment.
  Calls `RoadmapFoldIn::allocateIds(N)` ONCE for the batch + builds
  ONE multi-finding block + calls `RoadmapFoldIn::insertBlock`
  ONCE (loop 3 perf H1 + security M-F). NO MCP re-entry (loop 2
  impl M5). Source-scrape regression: no `QSaveFile` or
  `.roadmap-counter` access in `testauditengine.cpp`; one
  `RoadmapFoldIn::allocateIds` call per `foldIn` invocation.
- **INV-4 / `partition_token` via qHash** (loop 3 opt M9 + user
  decision 2026-05-17). Token =
  `qHash(callerCwd) ^ qHash(dimensionsSorted) ^ qHash(scope) ^
   qHash(maxMtime)` — **four** inputs; the design's fifth
  `qHash(engineVersion)` term was never implemented, so a token does not
  invalidate across engine versions. Emitted as 8-char hex via
  `QStringLiteral("%1").arg(h, 8, 16, '0')`.
  Collision 2⁻³² acceptable for staleness detection (NOT a
  capability — same-uid callers can reuse another session's token
  trivially, see § 9 Q-G). ~95 % cheaper than v3's SHA-256.
  `brief` and `synth` recompute expected token from request **before
  any per-chunk cache lookup** (loop 3 security M8). Mismatch →
  `stale_partition`.
- **INV-5 / `Required` contract on all verbs.** *(Corrected 2026-07-25 —
  the spec previously said `Optional`, which never shipped.)* All five
  `test_audit_*` verbs — `brief`, `fold_in`, `partition`,
  `synthesis_prompt` and `recheck` (ANTS-1722) — register
  `CallerCwdContract::Required` in `mainwindow.cpp` and are listed as
  `Required` in `ClaudeIntegration::callerCwdContractFor`, the latter
  citing "parity with audit_run". So the **dispatcher** refuses
  `caller_cwd_required` on *every* verb in this family before any handler
  runs — it is not scoped to the one RcGate-gated write, and the read
  verbs do **not** accept an absent `caller_cwd`. RcGate handler-level
  enforcement still applies additionally to the write verb
  (`test_audit_fold_in`).
- **INV-6 / dimension list is engine canonical AND injected into
  brief.** 18 dimensions in `testauditengine.cpp` as
  `static const QStringList kDimensions`. Brief carries active
  subset verbatim (§ 2.3 `dimensions[]`). Subagents do NOT depend
  on `references/dimensions.md`. **Schema dimensions arg is
  typed `string` (no enum)**; engine validates at request time
  and returns `unknown_dimension` if not in `kDimensions` (loop 3
  opt H1 — drops the redundant schema enum + the drift-guard
  test). Single writer.
- **INV-7 / per-chunk pre-pass cap.** Each chunk's pre-pass
  findings capped 20 entries (`kPrePassPerChunkCap = 20`).
  Excess dropped + DBGLOG warning. Total envelope: 17 × 20 × ~80 B
  = ~27 KiB.
- **INV-8 / Synthesis reads from disk.** `reports_dir` validated
  (INV-14). 64 KiB truncate per file; top-level only. Per-report
  content fenced via `<chunk_report file="…">…</chunk_report>`
  (loop 3 security H-A). Synth prompt is the only path that
  splices third-party content; fence makes it unambiguously
  data to the consuming subagent.
- **INV-9 / chunk size honours request.** Default 12. Clamped
  [4, 30]. Out-of-range → silent clamp + echoed. Total-files = 0
  → `{ok:false, code:"no_tests_found"}`.
- **INV-10 / partition envelope is paginated.** `chunks[]`
  paginated via offset/limit/total/truncated/next_offset using
  a hand-rolled `std::min` slice over `QVector<Chunk>` in `partition()` —
  no `PaginationEngine` involvement. `pre_pass_findings_by_chunk` filtered
  to page 1 chunks AND OMITTED from pages 2+ with
  `pre_pass_cached:true` (loop 3 token M2). Other three verbs
  always fit under 20 KiB. `truncated` here carries pagination
  semantics with `next_offset`; ANTS-1351's `samples_truncated`
  has different (irrecoverable) semantics.
- **INV-11 / fold-in respects rotation.** Generated bullets carry
  IDs from `.roadmap-counter` via `RoadmapFoldIn`. ANTS-1408
  archive-rotation rule applies once implemented.
- **INV-12 / NOT IMPLEMENTED — partition is ungated.** *(Corrected
  2026-07-25.)* The design called for a single in-flight partition per
  canonical projectRoot via the inline gate (ANTS-1351 § 2.4) keyed by
  `("test_audit_partition", canonicalProjectRoot)`. That was never wired:
  `verbInFlightTryAcquire` has exactly two call sites in the tree —
  `audit_run` and `indie_review_dispatch` — and `test_audit_partition`'s
  registration calls neither. **Consequently no verb in this family can
  emit `already_running`, and there is no slot for a reaper to reclaim.**
  Concurrent partitions on the same root simply both run. This matches
  § 2.7 / § 6, which already say the partition sweep is ungated.
  Whether to wire the gate (the sweep is an in-process walk with no child
  processes, so the case is weaker than for `audit_run`) or to ratify
  ungated is an open decision — see § 9 Q3.
- **INV-13 / pre-pass findings carry NO matched text.** Entries:
  `{file, line, pattern_id, dimension}` only. Engine type doesn't
  even have a "matched text" field (defence in depth). `pattern_id`
  is the stable id of the grep pattern; human description in
  `docs/standards/test-audit-grep-patterns.json`. **`pattern_id +
  file + line` is a read primitive under same-uid trust** (same-uid
  callers can already read the file; the spec is explicit so future
  readers don't treat `pattern_id` as a redaction — loop 3 security
  L-I).
- **INV-14 / path validation is PARTIAL — corrected 2026-07-25.**
  Only `reports_dir` routes through `PathValidation::validatePath` (the
  two call sites in `testauditengine.cpp`). `caller_cwd` gets a bare
  `canonicalFilePath()` + `isDir` check. **`scope:"path:<sub>"` is
  concatenated onto the project root with no anchor at all**, so
  `path:../..` walks outside the project; `files:<csv>` rejects only
  empty entries and backslashes, not `../`. The original text below
  describes the intended design, not the shipped one — the gap is
  tracked as **ANTS-3627**, and § 5's test 14, which would have caught
  it, was never built. Original intent: `caller_cwd` validated;
  canonical = `projectRoot`. `scope:"path:<subdir>"` validated.
  **`scope:"files:<csv>"` split on `,`; reject if any entry empty
  after trim; no escaping; reject backslash** (loop 3 security M-D
  — matches PathValidation's existing backslash-reject from
  ANTS-1295). `reports_dir` (synth) validated against projectRoot.
  TOCTOU window between validate and read accepted under same-uid.
- **INV-15 / mtime walk cached per partition; brief/synth use
  shallow recheck.** Partition caches full walk in
  `mtimeWalkComputedAt`. `brief`/`synth` recompute the token via a
  **bounded recheck: stat the top-level test_globs roots
  (typically 1-2 entries like `tests/` and `test_*.py` parent)
  and the test_globs expansion's modified-since
  `mtimeWalkComputedAt`**. If the recheck sees no change, the
  token is reused. If the test tree deepens such that root mtime
  is stale (a new file in a deep subdir doesn't bump the root —
  loop 3 perf M8 ambiguity), the staleness window is bounded by
  the partition_token's lifetime (the next full partition
  re-walks). Documented limitation: callers re-running tests
  frequently should re-call `partition` not `brief`. Rate-limit:
  recheck at most once per 5 s per partition_token (loop 3
  security M-E DoS mitigation).

  **Deep-tree mtime gap (ANTS-1447).** The shallow recheck stats
  only the top-level `test_globs` roots. Adding a file in a deep
  subdir (e.g. `tests/api/integration/`) does not bump the
  parent root's mtime on most filesystems, so a `brief` / `synth`
  call inside the token TTL can return findings computed before
  that file existed, until the next full `partition` recompute.
  This is an **accepted v1 limitation**, not a bug: the staleness
  window is bounded (one partition TTL) and never produces a
  *wrong* answer about files the partition did see — only a
  *stale set* missing a just-added file. The resolution for v1 is
  this documentation plus the "re-call `partition` after editing
  tests" guidance above. v2 options, to revisit only if a
  stale-brief symptom is reported (tracked under ANTS-1450):
  (a) inotify watch on the test tree (new infra cost);
  (b) recursive mtime aggregation (`du`-style) on the recheck;
  (c) keep the gap, lean on the partition TTL. No option is
  scheduled — option (c) is the de facto v1 behaviour.

## 4. Failure modes

| code | INV | Notes |
|---|---|---|
| `caller_cwd_required` | INV-5 | Every verb in the family — all register the `Required` contract, so the dispatcher refuses before any handler runs (corrected 2026-07-25; the spec previously scoped this to `test_audit_fold_in` alone) |
| `bad_path` | INV-14 | caller_cwd / scope:"path:" / "files:" / reports_dir escapes root or malformed CSV |
| `no_tests_found` | INV-9 | Empty scope or no framework detected |
| `unknown_dimension` | INV-6 | `dimensions:"csv:foo"` not in `kDimensions` |
| `stale_partition` | INV-4 | Token recompute differs from request token |
| `bad_chunk_id` | INV-4 | brief called with id not in partition |
| `reports_dir_outside_root` | INV-8 | synth `reports_dir` resolves outside the project root without `allow_outside_project` (renamed from the retired `reports_dir_missing`, ANTS-1455) |
| `reports_dir_unreadable` | INV-8 | `reports_dir` canonicalises but doesn't exist, isn't a directory, or is unreadable |
| `reports_dir_empty` | INV-8 | `reports_dir` is a readable directory with zero top-level `*.md` files |
| `write_failed` | INV-3 | RoadmapFoldIn batched insertBlock failed |
| `rate_limited` | — | Dispatcher-level: `partition`/`fold_in` are Expensive tier (10/min), `brief` is BriefAssembly tier (30/min), `synthesis_prompt` falls through to Cheap (60/min). Carries `retry_after_ms` |
| `mcp_disabled` | — | Dispatcher-level: the `claude.mcp_enabled` master gate is off |
| `missing_field` | — | A required field is wholly absent (partition/brief/synth/fold_in guards) |
| `bad_args` | — | A present field is ill-formed |
| `bad_mode` | — | synth `mode` outside `{summary, full, hybrid}` |
| `bad_actionable` | INV-3 | fold_in `actionable[]` malformed or empty — refused before any id is allocated |
| `narrative_md_required` | INV-3 | fold_in in narrative mode with an empty/absent `narrative_md` |
| `id_counter_failed` | INV-3 | `.roadmap-counter` unreadable or unadvanceable; envelope carries `counter_path` (ANTS-1527) |
| `no_project` | — | **`test_audit_recheck` only** (out of scope for this spec): its `caller_cwd` fails to canonicalise. `fold_in` does *not* emit this — it returns `bad_path` for the same condition |
| `no_roadmap` | — | **`test_audit_recheck` only**: no ROADMAP.md under the resolved root. `fold_in` surfaces a missing/unwritable roadmap as `write_failed`, not this |
| `server_busy` | INV-9 (ANTS-1351) | ANTS-3612 — `audit_run`'s aggregate concurrency cap (`kMaxConcurrentRuns = 2`) is taken; `retry_after_ms: 5000`. Emitted by the audit engine, not by a pool, and **not** currently emitted by any verb in this spec — see § 2.7's correction |

## 5. Test plan

`tests/features/mcp_test_audit_trio/` (new). **Bundle: `test_claude`**.

1. **Inv1PartitionNoShell + AlternationOrder** — no QProcess.
   Source-scrape: pre-compiled patterns; alternation ordered
   longest-first; common prefixes factored.
2. **Inv2BriefPathOnly** — fixture file with `MAGIC_BODY_MARKER`;
   assert `source_paths.length > 0` AND `MAGIC_BODY_MARKER` absent
   from any envelope field. (No `brief` string field to check.)
3. **Inv3FoldInBatchedSingleWrite** — source-scrape: no `QSaveFile`
   in `testauditengine.cpp`; exactly ONE `RoadmapFoldIn::allocateIds`
   call per foldIn (with `count=N`); exactly ONE
   `RoadmapFoldIn::insertBlock` call. Runtime: foldIn of 10
   findings → ROADMAP.md read+write count == 1.
4. **Inv4PartitionTokenDeterministic + Stale + CheckOrder** — same
   args twice → same 8-char hex token; touch a file → token changes;
   brief with stale token → `stale_partition` BEFORE any cache lookup.
5. **Inv5RequiredContract** — **ASSERTED INVERSELY, AND VACUOUSLY.**
   A test does ship: `mcp_test_audit_trio`'s `Inv5OptionalContract`. It
   asserts each verb "must NOT be Required" — the inverse of the shipped
   contract — and it passes only because its scrape window (bounded by
   the `"Required — refuse with caller_cwd_required"` and
   `"TabSpecific —"` comment literals) no longer reaches the test-audit
   block, which moved down as the contract table grew. So it would pass
   whether the contract were Required or Optional: it has been guarding
   nothing since the table grew. (The planned
   `mcp_caller_cwd_contracts/` CLS-1 extension was never written either
   — that suite has no `test_audit` reference.) Fix tracked as
   **ANTS-3628**: invert the assertions, drop the comment-literal window,
   and prefer folding into `mcp_required_contract_baseline/` where
   `audit_run`'s equivalent lives and no window is involved.

   **Also never built:** test 2 (`Inv2BriefPathOnly`), test 11
   (`Inv11FoldInRotation`) and test 14 (`Inv14PathValidationCsv` — the
   one that would have caught ANTS-3627). Shipped names also drift from
   the plan's (`Inv3FoldInBatchedDelegate` vs `Inv3FoldInBatchedSingleWrite`,
   `Inv15RecheckRateLimit` vs `Inv15MtimeRateLimit`); treat the shipped
   file as authoritative.
6. **Inv6DimensionInjection + RuntimeValidation** — brief carries
   `dimensions[]`. Engine returns `unknown_dimension` for
   `dimensions:"csv:foo"`. Source-scrape: no schema enum for
   dimensions; no `references/dimensions.md` Read in the engine.
7. **Inv7PrePassCap** — 50 matches in chunk → 20 emitted + DBGLOG.
8. **Inv8SynthDiskInput + FenceInjection** — write fake reports;
   assert consumed; assert per-report content fenced with
   `<chunk_report>`. Plant a hostile report containing
   "Ignore previous instructions"; assert it appears INSIDE the
   fence, not adjacent.
9. **Inv9ChunkSizeClamping** — 1/30/100 → clamped [4, 30] with echo;
   total_files=0 → `no_tests_found`. Math sanity: 192/12 → 16 chunks.
10. **Inv10PartitionPaginationCachedPrePass** — 30-chunk fixture,
    `limit:10` page 1 → `pre_pass_findings_by_chunk` present,
    `pre_pass_cached:false`. Page 2 → field omitted,
    `pre_pass_cached:true`.
11. **Inv11FoldInRotation** — foldIn(3 findings) advances
    `.roadmap-counter` by exactly 3 (single allocateIds call).
12. ~~**Inv12ConcurrencyOrphanReaper**~~ — **not applicable.** The
    in-flight gate this test exercised was never wired (INV-12), so
    there is no `already_running` to assert and no slot to reclaim. If
    the gate is ever added, restore this test with it; until then a test
    asserting the refusal would fail by construction.
13. **Inv13PrePassNoSecretLeak** — fixture file with
    `password="MAGIC_SECRET"`; assert `MAGIC_SECRET` absent from
    response JSON; engine type doesn't even have the field.
14. **Inv14PathValidationCsv** — `scope:"path:../etc"` → `bad_path`;
    `files:"a.py,,b.py"` → `bad_path` (empty entry);
    `files:"a\\.py"` → `bad_path` (backslash);
    `reports_dir:"../../etc"` → `bad_path`.
15. **Inv15MtimeRateLimit** — call brief twice within 5 s; second
    call rides cache (no stat syscalls beyond the rate-limit check).

Plus regression in `mcp_caller_cwd_contracts/`: add all five verbs
to the **Required** allowlist (corrected 2026-07-25 — this paragraph
still said "Optional", contradicting INV-5, § 4, test 5 and § 8);
assert refusal-shape on `test_audit_fold_in` when RcGate active.
Coordinate edit ordering with ANTS-1351.

## 6. Performance / RAM budget

**Partition wall time:**
- File-tree walk + glob: ~50 ms / 200 files.
- Pre-pass regex scan: 30 patterns × 200 files × ~10 KiB = 2 MiB.
  Alternation longest-first + common-prefix-factored (INV-1) →
  ~300 ms cold cache (32 GiB workstation); ~800 ms worst case
  (slower hardware). Single combined regex per file.
- Subsequent partitions inside token TTL ride cache (INV-15).
- Runs **inline on the dispatcher thread** — there is no worker and no
  pool (see § 2.7's ANTS-3612 correction; the four `QThread::create`
  sites in `mainwindow.cpp` belong to `audit_run`,
  `indie_review_dispatch` and the generic `rcDelegateWorker` — none to
  `test_audit_*`). The GUI is therefore
  blocked for the partition's duration; the ≤ 400 ms budget above is what
  keeps that acceptable, so treat it as a hard budget rather than a target.

**Brief wall time:** ≤ 50 ms (token recheck rate-limited per
INV-15; path list assembly).

**Synth wall time:** ≤ 200 ms (17 files × ~5 ms open + 1 MiB
read + prompt render with fencing).

**Fold-in wall time:** SINGLE read+write of ROADMAP.md (763 KiB
file). Per-call ~25 ms (read 763 KiB + splice + write + rename).
Was 30× this in v3's per-finding-write model (loop 3 perf H1).

**RAM:**
- Partition working set: 17 × 20 × ~80 B = ~27 KiB (no match text
  per INV-13).
- Brief: < 10 KiB transient.
- Synth: ≤ 100 KiB during read; ~5 KiB rendered prompt.
- Fold-in: ROADMAP.md held ~763 KiB transient during single splice;
  released at return.
- Pre-compiled regex cache: ~10 KiB.

**Concurrency vs ANTS-1351 (corrected by ANTS-3612):** there is no shared
`m_auditPool`. `audit_run` bounds itself with an engine-side counting cap
(`kMaxConcurrentRuns = 2`, refusing `server_busy`); the partition sweep is
an in-process walk with no child processes and is currently ungated. See
§ 2.7's correction note.

**Disk:** Synth reads `reports_dir` (~51 KiB); fold-in writes
ROADMAP.md (~763 KiB).

**Schema/engine cost:** `kDimensions` static < 1 KiB; pre-compiled
regexes ~10 KiB; project-internal grep-patterns.json resource ~3 KiB.

## 7. Skill-side change

> **Status: PROPOSED, not applied (ANTS-3625).** As of 2026-07-25 the
> live `~/.claude/skills/test-audit/SKILL.md` contains no "Fast path"
> heading and no `test_audit_partition` mention, so `/test-audit` through
> the skill still does the work the long way and § 1's token saving is
> not realised for skill callers. This is the exact twin of ANTS-3613
> (the `/audit` skill's fast path, since applied). The block below is the
> intended edit.

`~/.claude/skills/test-audit/SKILL.md` should gain a fast-path block:

```
## Fast path (Ants Terminal)

If `mcp__ants__test_audit_partition` is available, prefer the trio
(partition → brief → synth → fold_in) over the markdown pipeline.

Flag mapping:
- `/test-audit`           → `test_audit_partition{}` then per-chunk brief, dispatch, synth, fold_in
- `/test-audit --quick`   → `test_audit_partition{quick:true}` (grep-pass only; skip brief+dispatch+synth, go to fold_in with pre_pass_findings)
- `/test-audit --no-roadmap` → as default, skip fold_in
- `/test-audit --dimension perf,flakiness` → `test_audit_partition{dimensions:"csv:performance,flakiness"}`
- `/test-audit tests/api/` → `test_audit_partition{scope:"path:tests/api"}`

The `test_audit_brief` response has structured fields only (no
`brief` string). Compose the subagent prompt yourself:

    === Lane: <chunk_id> ===
    Active dimensions: <dimensions joined>
    Framework: <framework_context.framework>
    Files in this chunk (N):
    - <source_paths[0]>
    - <source_paths[1]>
    ...
    Pre-pass findings already located:
    - <pre_pass_findings[0]>
    ...

After the synth prompt, the orchestrator dispatches
`test-audit-chunk` subagents per chunk + the `audit-triage` subagent
for triage. MCP returns structured inputs; orchestrator drives
subagent dispatch.
```

Skill markdown NOT deleted — fallback + documentation of record.

## 8. Decisions

- **A verb family, not one verb.** Test-audit is intrinsically parallel.
- **Fold-in batched single write.** One `RoadmapFoldIn::insertBlock`
  with the full N-finding block; ROADMAP.md read+write once per
  call (loop 3 perf H1 fix).
- **All verbs Required** (corrected 2026-07-25 — "Optional" was the
  original design and never shipped; the registrations cite parity with
  `audit_run`). RcGate additionally at the handler for the write.
- **Pre-pass engine-side, no matched text.** Pure regex; secret
  leak impossible by type.
- **`partition_token` via qHash, not SHA-256** (user decision
  2026-05-17). Staleness check is not a capability.
- **No `brief` string field; structured-only response** (user
  decision 2026-05-17). Caller composes prompt from structured
  fields.
- **Pre-pass findings cached across partition pages.** Page 2+
  omits the field with `pre_pass_cached:true`.
- **Synth prompt fences per-chunk reports.** Defends against
  prompt-injection from hostile dep reports.
- **Namespace `TestAuditEngine` PascalCase.** Matches in-tree.
- **Inline in-flight gate (shared with ANTS-1351). NEVER WIRED** — the
  decision stands for `audit_run`, but no `test_audit_*` verb acquires a
  slot, so this family has no gate at all. See INV-12 and § 9 Q3.
- **Patterns ship as project-internal JSON resource.** Engine
  self-contained; skill markdown documents inline.
- **No `with_timing` v1.** Suite side-effects.

## 9. Open questions

- **Q1 / dimension list drift.** Manual review on additions.
- **Q2 / chunk packing.** Depth-first walk + size cap. No
  rebalancing v1.
- **Q3 / multi-tab interaction — OPEN.** The design answer was "same
  project: second call gets `already_running`", but INV-12's gate was
  never wired, so today both partitions simply run. Decide: wire the
  gate, or ratify ungated (the sweep spawns no child processes and is
  bounded by the ≤ 400 ms budget, so concurrent runs are cheap — the
  cost is duplicated work, not resource exhaustion).
- **Q-D / partition_token cross-session reuse.** Same-uid sessions
  share the token space. Not auth; same-uid callers can already
  read the same files. Documented in INV-4.
- **Q-E / mtime cache deep-tree gap.** Documented in INV-15.
  Callers re-running tests should re-call `partition`. Revisit if
  user feedback flags stale-brief bugs.

## 10. Cold-eyes loop log

**Loop 1 (2026-05-17):** v1 raised 5 H, 5 M, 5 L. v2 folded:
fold-in delegation, regex feasibility, deterministic token, lock,
Optional contract, pagination, dimension injection, no codegen,
test plan, spec polish.

**Loop 2 (2026-05-17):** three lanes. v3 folded: namespace
PascalCase, InFlightRegistry (later inlined v4), per-message cap
(later structural via type, v4), `truncated`/`samples_truncated`
split, PathValidation across all paths, thread placement, reports_dir
anchor, pre-pass redaction, token check before cache, fold-in
atomicity, lib placement rationale, test bundle named,
`RoadmapFoldIn` direct call clarified, drift guard, mtime cache,
SHA-256 truncation note.

**Loop 3 (2026-05-17):** five lanes. v4 folded:

- **Perf H1:** fold-in now batched single read+write (not N× per
  finding). § 2.5 + INV-3 rewritten.
- **Perf H2:** pool size `max(1, nproc/8)` → `max(2, nproc/8)` in
  ANTS-1351 INV-9; pool saturation envelope + `retry_after_ms`
  documented for this spec's partition (§ 2.7).
- **Perf M4:** alternation longest-first + common-prefix-factor
  rule (INV-1).
- **Perf M8:** mtime cache disambiguated (INV-15) — bounded
  shallow recheck + 5 s rate-limit + documented deep-tree gap.
- **Sec H-A:** synth prompt fencing (INV-8 + § 2.4).
- **Sec H-B:** stale-slot reaper (inherited from ANTS-1351 § 2.4
  via the inline gate).
- **Sec M-D:** CSV grammar pinned (INV-14).
- **Sec M-E:** mtime recheck rate-limit (INV-15).
- **Sec M-F:** counter race closed via single batched
  `allocateIds(N)` call (INV-3).
- **Opt H1:** schema enum for dimensions dropped (INV-6); runtime
  validation only.
- **Opt H2:** patterns ship as project-internal JSON resource
  (INV-1); engine self-contained.
- **Opt H3:** InFlightRegistry class dropped; inline gate per
  ANTS-1351 § 2.4.
- **Opt H4 + user decision:** `brief` string field dropped (§ 2.3);
  caller composes prompt.
- **Opt M9 + user decision:** partition_token via qHash, not
  SHA-256 (INV-4).
- **Token H1/H2:** § 1 framing corrected (8 K not 14 K skill
  displacement, once per session).
- **Token H3:** net per-sweep savings honest table in § 1.
- **Token M1:** brief duplication killed by H4.
- **Token M2:** `pre_pass_findings_by_chunk` cached across
  partition pages (INV-10).
- **Final consistency:** chunks_count math example fixed
  (192/12 = 16); engine API structs fully specified (§ 2.6);
  `unknown_dimension` mapped to INV-6 in § 4; INV-5 + § 4
  `caller_cwd_required` scope clarified (write verb only); INV-13
  notes `pattern_id+file+line` is a same-uid read primitive (L-I).

v4 ready for sign-off. Implementation can proceed; remaining items
deferred per § 9.

**Post-implementation reconciliation (2026-07-25).** A cold-eyes lane run
against the *shipped* verbs (rather than the draft) found this spec had
drifted in four substantive ways. All are fixed above; recorded here
because loop 3's "v4 ready for sign-off" line above no longer describes
the document's state.

- **INV-5 said `Optional` on all four verbs; every verb ships
  `Required`.** All five `test_audit_*` registrations use
  `CallerCwdContract::Required`, and `callerCwdContractFor` lists them as
  such citing "parity with audit_run". The spec's claim that read verbs
  accept an absent `caller_cwd` was the reverse of the truth — a caller
  building to it would omit `caller_cwd` and be refused on every call.
  This is the one finding that would have actively misled an
  implementer. Corrected in INV-5, § 4, § 5 test 5, and § 8.
- **The `m_auditPool` worker model was never built.** § 2.7 and § 6 both
  described partition as running on a pool shared with `audit_run`.
  ANTS-3612 established the pool was always aspirational; `audit_run`
  now bounds itself with a flat engine-side cap instead, and partition
  runs *inline on the dispatcher thread*. § 2.7 already carried the
  correction; a leftover line in § 6 still asserted the pool twenty
  lines after § 6's own correction contradicted it. Both now say the
  same thing, and § 6 states the GUI-blocking consequence honestly.
- **`reports_dir_missing` is a retired code.** ANTS-1455 renamed it to
  `reports_dir_outside_root` and added `reports_dir_unreadable` /
  `reports_dir_empty`. § 4 listed only the retired name.
- **Renamed / unimplemented response keys.** `dimension_hints` became
  `pre_pass_dimensions` (ANTS-1487) in both the § 2.2 example and § 2.6's
  struct; `dimensions_skipped` and `skip_reason_per_dimension` are
  declared but never populated and are now marked as such rather than
  shown as shipped; `pre_pass_findings_by_chunk`'s omission rule gained a
  second trigger (envelope size, ANTS-2070) plus `pre_pass_chunk_ids[]`;
  `additional_frameworks[]` (ANTS-1623) was undocumented.

Also: the heading "Four-verb trio" was self-contradictory *and*
undercounted — a fifth verb, `test_audit_recheck`, shipped under
ANTS-1722 and is out of scope for this spec. Eleven refusal codes across
the audit and test-audit families were missing from
`docs/standards/mcp-error-codes.md` and were added there in the same
pass, so § 4's rows now all resolve to a taxonomy entry.

**Loop-3 reconciliation (2026-07-25).** A third cold pass found this
spec's § 2 had never been reconciled to shipped code — a systematic
staleness rather than scattered errors, and the reason two earlier
passes kept finding more:

- **INV-10 named a mechanism that does not exist** —
  `PaginationEngine::pageBullets`. `testauditengine.cpp` has zero
  references to it; the paging is a hand-rolled slice. This was the only
  claim in either doc pointing a maintainer at a non-existent call.
- **All four verbs' documented envelopes were short of shipped.** synth
  alone omitted four request fields and nine response fields; brief
  omitted `prior_false_positives`; fold_in omitted four request fields
  and two response fields; partition omitted three. § 2.6's struct
  listing — which carried an explicit "all struct fields explicit"
  completeness claim — was stale in six places. That claim is now
  replaced with a pointer to `src/testauditengine.h` as authoritative,
  because a listing that invites trust and then drifts is worse than one
  that defers.
- **§ 8 still recorded the inline in-flight gate as a live decision**
  one section after INV-12 was corrected to NOT IMPLEMENTED — the same
  restatement pattern, caught again.
- **§ 5's test 5 was marked corrected when the test was never built.**
  The 2026-07-25 pass fixed its *wording* from Optional to Required; the
  `mcp_caller_cwd_contracts` extension it describes does not exist. Now
  marked NOT BUILT — the contract is right in code and unasserted in
  tests.
- § 4 gained the two dispatcher-level refusals (`rate_limited`,
  `mcp_disabled`) its sibling spec already lists.

Filed from this pass: **ANTS-3625** — the `/test-audit` skill fast-path
was never applied, the exact twin of the ANTS-3613 gap fixed earlier the
same day.

**Loop-4 reconciliation (2026-07-25).** The fourth cold pass on this doc
returned *more* CRITICALs than the third, not fewer — because it was the
first to check § 4's code attributions and INV-14's validation claim
against source rather than for internal consistency. Fixed:

- **§ 4 attributed `no_project` and `no_roadmap` to `test_audit_fold_in`.**
  Neither is emitted by `foldIn`: an uncanonicalisable `caller_cwd`
  returns **`bad_path`**, and a missing/unwritable roadmap surfaces as
  `write_failed`. Both codes belong to `test_audit_recheck` — the verb
  this spec declares out of scope. A caller catching `no_project` from
  `fold_in` would wait for something that never arrives. The same
  mis-attribution had been mirrored into
  `docs/standards/mcp-error-codes.md` by an earlier pass in this loop and
  was corrected there too.
- **INV-14 asserted a security property the engine does not implement.**
  `PathValidation::validatePath` is called exactly twice, both for
  `reports_dir`. `scope:"path:<sub>"` is concatenated straight onto the
  project root with no anchor, so `path:../..` walks outside the project;
  `files:<csv>` rejects only empty entries and backslashes, not `../`.
  The invariant now describes the shipped behaviour and flags the gap;
  the code fix is **ANTS-3627**. Note § 5's test 14 — the test that would
  have caught this — was among those never built.
- **§ 5 test 5 said "unasserted in tests"; the truth was worse.**
  `Inv5OptionalContract` ships and asserts the *inverse* of the shipped
  contract, passing only because its comment-literal scrape window
  drifted off the block it checks as the contract table grew. It would
  pass either way. Loop 3 read a vacuous test as no test — they look the
  same until you ask why it is green. Tracked as **ANTS-3628**; tests 2,
  11 and 14 are genuinely absent, and shipped test names drift from the
  plan's.
- **§ 2.4's response fence closed in the wrong place** — an artifact of
  loop 3's own edit, which rendered the prose that follows it inside the
  JSON block.
- Smaller: INV-10 kept a `paginationengine.h` citation fragment after the
  mechanism claim was corrected; § 2.5's fold_in request example (the
  block an implementer copies) omitted four accepted keys; the
  `calibration_anchor` example carried a `noise_rate_pct` the engine
  ignores; `next_offset` is emitted unconditionally with a `-1` sentinel,
  not optionally; § 4's `rate_limited` row missed that `synthesis_prompt`
  is Cheap-tier; and § 1's byte figures were ~38 KiB measured against
  "~32 KiB" claimed.

§ 7's PROPOSED banner is now APPLIED — ANTS-3625 shipped the
`/test-audit` fast path earlier the same day.
